# Agent Prompts

These YAML descriptors are preserved from the original specialist skill package.

Keep them outside `skills/<name>/agents/`: Codex treats skill directories with a
nested `agents/` directory as non-skill agent packages, which hides those skills
from the available skill list.
