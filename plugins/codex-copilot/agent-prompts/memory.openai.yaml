interface:
  display_name: "Memory"
  short_description: "Inspect memory state"
  default_prompt: "Use $memory to inspect recent cc memory entries, known references, and task progress."

policy:
  allow_implicit_invocation: false

