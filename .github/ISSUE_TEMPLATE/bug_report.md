---
name: Bug report
about: Report something in Control Tower that isn't working correctly
title: "[Bug] "
labels: bug
assignees: ''
---

## Description

A clear, concise description of what's wrong.

## Expected behavior

What should have happened instead?

## Actual behavior

What actually happened? Include the exact wording Control Tower showed you
(the tray sentence, the wizard state, the Admin-mode message) — this project
treats the exact status text as load-bearing, so precision here matters.

## Steps to reproduce

1.
2.
3.

## Environment

- Control Tower version:
- macOS version:
- `copilot`/`cc` CLI version (if known — `copilot --version`):
- Operator mode or Admin mode:
- Managed (MDM) or unmanaged machine:

## Relevant logs / `--json` output

If applicable, paste the relevant `copilot doctor --json` (or equivalent)
output. **Redact anything sensitive first** — do not paste secrets, tokens,
personal file paths, or org-identifying data. See the invariants in
`CLAUDE.md` (#4, #6) on why this matters.

## Additional context

Anything else that might help — screenshots, when it started, whether it's
reproducible every time.
