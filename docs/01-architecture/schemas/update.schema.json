{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://everyoneneedsacopilot.com/schemas/copilot-control-tower/update.schema.json",
  "title": "copilot update --json",
  "description": "Result of a reconciling ecosystem sync. `pruned` ops surface reconciling-sync deletions; `severity_trailer` + `shadowed_by` drive the un-dismissable security banner.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "result", "lock_before", "lock_after", "changed"],
  "properties": {
    "schema_version": { "$ref": "_envelope.schema.json#/$defs/schema_version" },
    "host": {
      "type": "string",
      "minLength": 1,
      "description": "Host identifier the update was run for, e.g. 'claude-code'. Present in the upstream design's example (design-control-tower-integration.md §1.2) but not marked required since cli-contract.md's prose table omits it."
    },
    "result": {
      "type": "string",
      "enum": ["applied", "up-to-date", "held", "blocked", "offline"],
      "description": "Overall update outcome.",
      "$comment": "CORRECTED 2026-07-07: the upstream design's `update --json` example (design-control-tower-integration.md §1.2) carries an explicit enum comment: `// applied | up-to-date | held | blocked | offline`. The prior draft left this as an unconstrained non-empty string; tightened to match."
    },
    "lock_before": { "$ref": "_envelope.schema.json#/$defs/git_sha" },
    "lock_after": { "$ref": "_envelope.schema.json#/$defs/git_sha" },
    "changed": {
      "type": "array",
      "description": "Per-item changes applied by the sync.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["dimension", "layer", "item", "op", "signed"],
        "properties": {
          "dimension": { "type": "string" },
          "layer": { "type": "string" },
          "item": { "type": "string" },
          "op": {
            "type": "string",
            "enum": ["added", "updated", "pruned", "unchanged"],
            "description": "`pruned` is the reconciling-sync deletion."
          },
          "from": { "type": ["string", "null"] },
          "to": { "type": ["string", "null"] },
          "signed": {
            "type": "boolean",
            "description": "Signature state of the introduced content. Security-relevant: a missing value is treated as unsigned (fail-closed); the CLI must always emit it."
          },
          "severity_trailer": {
            "type": ["string", "null"],
            "description": "Security-trailer text driving the un-dismissable banner; null when none.",
            "$comment": "Prose names the field without a type; encoded as nullable string."
          },
          "shadowed_by": {
            "type": ["string", "null"],
            "description": "Identifier of the layer/item shadowing this one; null when not shadowed.",
            "$comment": "Prose names the field without a type; encoded as nullable string."
          }
        }
      }
    },
    "held_for_approval": {
      "type": "array",
      "description": "Changes withheld pending approval (e.g. a held-major awaiting IT/user approval).",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["dimension", "from", "to", "reason"],
        "properties": {
          "dimension": { "type": "string" },
          "from": { "type": "string" },
          "to": { "type": "string" },
          "reason": {
            "type": "string",
            "description": "Why the change is held, e.g. 'major; ecosystem.yml policy=hold-majors'."
          }
        }
      },
      "$comment": "CORRECTED 2026-07-07: tightened to the shape shown in the upstream design's example (05-control-tower.md §6 / design-control-tower-integration.md §1.2): `{dimension, from, to, reason}`. The prior draft left this fully open; additionalProperties stays true since the example does not claim this is exhaustive."
    },
    "blocked": {
      "type": "array",
      "description": "Changes blocked from applying, e.g. capability-policy denials or signature failures.",
      "items": { "type": "object", "additionalProperties": true },
      "$comment": "cli-contract.md/design docs name `blocked:[...]` (capability-policy denials, sig failures) but the upstream design does not show a concrete example item shape for this array (only an empty `blocked: []` in the §1.2 example); left as an open array of objects until the CLI freezes it."
    }
  }
}
