# claims.yaml — CSE Verification & Benchmark Program claims register (v1)
#
# TASK-85 (B-2, phase-2-prd.md P0). Retires the initiative's V-2 violation
# (docs/40-initiatives/01-cse-auditability/README.md, "Validation Contract"):
# Phase 1's findings were produced by choosing operational definitions AFTER
# seeing the data. This file is the pre-registration V-2 requires. Per
# phase-2-prd.md §7: "claims.yaml is the single authoritative source for
# every operational definition. No metric may be quoted from this program
# without a register entry — including in the dashboard, in docs, and in
# future audit phases."
#
# SCOPE NOTE (read before extending this file): this is NOT yet the
# README's Phase-0 vision of "one machine-readable table of every claim
# across all four products" (~40 rows, operational_definition /
# falsification_condition / threshold / verdict schema). That full sweep is
# still open work. This v1 register is narrower and specific to what
# phase-2-prd.md §7 actually requires right now: the operational
# definitions this Verification & Benchmark Program itself quotes, plus one
# claim per program truth condition (T1-T8, phase-2-prd.md §2) and one per
# still-quotable Phase-1/re-audit corrected number. When Phase 0's full
# CSE-wide sweep runs, it should EXTEND this file (reuse `definitions:`,
# append to `claims:`) rather than replace it.
#
# ---------------------------------------------------------------------------
# SCHEMA
# ---------------------------------------------------------------------------
#
# definitions:
#   <name>:                 # referenced by claims[].definition_refs
#     description: <str>    # what the term means, precisely enough to
#                            #   re-implement independently
#     source: <path>         # (optional) the code that currently implements
#                            #   this definition, if one exists
#     note / caveat: <str>   # (optional) known fragility, open gaps, or
#                            #   cross-references to other definitions
#     ... additional structured sub-fields as the term requires (e.g.
#         `channels`, `variants`, `products`, `classes`, `levels`) — see
#         individual entries below.
#
# claims:
#   - id: <unique-slug>
#     statement: <str>              # the exact quotable claim, precise
#                                    #   enough to be falsified
#     definition_refs: [<name>, ...]  # keys into `definitions:` above;
#                                    #   `[]` only when the claim rests on
#                                    #   an unambiguous raw field, not a
#                                    #   contested operational definition
#     check: <command-or-"manual">  # how to (re-)verify this claim
#     status: passing|failing|unchecked|gated
#     evidence: <path>              # file (this repo or a sibling repo)
#                                    #   substantiating `status`
#     last_checked: <YYYY-MM-DD>    # required whenever status != unchecked
#
# STATUS ENUM (this register's own vocabulary — distinct from, but mapped
# to, the README's Phase-5 four-verdict scorecard language):
#   passing   — checked; the statement is currently true.        (~ proven)
#   failing   — checked; the statement is currently false.       (~ falsified)
#   unchecked — no check has been run against this statement yet. (~ not-yet-measured)
#   gated     — blocked on a prerequisite workstream/decision landing
#               first; cannot be meaningfully checked until then. (~ not-yet-measured,
#               blocked)
#
# V-2 note: this file is git-committed as its own pre-registration event.
# Per V-2, any `check`/`status`/`threshold`-bearing field must not be
# edited to match a result observed after the fact — corrections belong in
# a new commit with a changed `last_checked`, not a silent rewrite.
# ---------------------------------------------------------------------------

definitions:

  turn:
    description: >-
      One real user turn in a main session transcript: a JSONL record with
      type=="user", isSidechain != true, isMeta != true, and content that is
      not a tool_result (i.e. an actual human/user message, not a tool
      response wrapped in a user-role record). This is the definition
      tools/cse-audit/session_metrics.py (is_real_user_turn, n_user_turns)
      currently implements and the one this program's "turns" numbers use.
    source: "tools/cse-audit/jsonl_utils.py (is_real_user_turn); tools/cse-audit/session_metrics.py (n_user_turns)"
    caveat: >-
      This definition is NOT comparable to the April 2026 restructure doc's
      "671 turns/session" figure. April's counting script no longer exists
      (docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md,
      F-6), so its definition cannot be recovered. Re-measuring under THIS
      definition gives a median of 6.0 turns/session — a ~90-112x gap the
      re-audit corroborated as a definitional artifact, not a real
      before/after change. See claim turn-definition-incompatible-with-april.

  delegation_rate_tool_share:
    description: >-
      Of all tool calls in a session (main-session tool calls plus every
      sidechain/subagent tool call), the fraction executed inside a
      subagent context: n_subagent_tool_calls / (n_main_tool_calls +
      n_subagent_tool_calls). Answers "of everything that got done, how
      much ran inside a delegated subagent."
    source: "tools/cse-audit/session_metrics.py (SessionMetrics.delegation_rate_toolshare)"
    note: >-
      ALWAYS report side by side with delegation_rate_event_share, never
      alone. The two are both defensible readings of "delegation rate" and
      disagree by an order of magnitude (docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md,
      F-5); citing only one without the other reproduces the exact
      pre-registration failure this file exists to close.

  delegation_rate_event_share:
    description: >-
      Of the main session's own tool-call decisions only, the fraction that
      were "call an Agent/Task" rather than "do it myself":
      n_agent_delegations / n_main_tool_calls. Answers "of the orchestrator's
      own choices, how often did it choose to delegate."
    source: "tools/cse-audit/session_metrics.py (SessionMetrics.delegation_rate_eventshare)"
    note: >-
      ALWAYS report side by side with delegation_rate_tool_share. See that
      entry's note; this is the other half of the same F-5 pair.

  protocol_declaration:
    description: >-
      A main-session assistant message counts as protocol-declared if its
      first non-thinking text block, left-stripped, starts with the literal
      string "[PROTOCOL". Two denominators are tracked: `_all` (every
      main-session assistant message) and `_first_of_turn` (only messages
      whose parentUuid is a real user-turn uuid — the "strict" reading,
      since a protocol declaration is only meaningful as the FIRST reply to
      a user turn, not on every follow-up message within it).
    source: "tools/cse-audit/session_metrics.py (_process_assistant_record; protocol_declaration_rate_all, protocol_declaration_rate_first_of_turn)"
    caveat: >-
      Whether April 2026's original "3.5%" figure used the same string
      match is unrecoverable (F-6); the comparison is directional only.

  knowledge_read:
    description: >-
      A file under a registered knowledge-repo root counts as "read" in a
      session if EITHER of two channels fires against its path.
    channels:
      read_tool: >-
        A Read, Grep, or Glob tool_use call whose input path (file_path for
        Read; path/pattern for Grep/Glob) resolves to a location under a
        registered knowledge repo root. IMPLEMENTED —
        tools/cse-audit/product_usage.py (classify_knowledge_path,
        scan_corpus's Read/Grep/Glob branch).
      bash_mediated: >-
        A Bash tool_use call whose command string contains one of {cat,
        grep, rg, head, sed} in COMMAND POSITION (start of the command
        string, or start of a pipeline segment after &&/||/|/;/backtick/
        $(/newline — the same command-position rule as cli_invocation
        below) AND at least one subsequent whitespace-delimited token,
        after ~/$VAR/relative-to-cwd expansion, resolves to a path under a
        registered knowledge repo root. IMPLEMENTED (B-5, 2026-07-12) —
        tools/cse-bench/collectors/transcripts.py
        (_classify_knowledge_bash / _extract_bash_targets / the Bash
        branch of _scan_extra). This closes the H-2 gap identified in
        phase-1-findings.md ("no native Grep/Glob calls exist anywhere in
        the corpus — file search happens via Bash"). Any "never read"
        figure computed from read_tool alone remains available for
        comparison (tools/cse-audit/product_usage.py still implements
        that channel only) but is now known to be an UPPER BOUND on
        never-read, not the full measurement — see the collector's
        `distinct_files.bash_only` field for how many files this channel
        surfaces that read_tool alone would have missed.
    named_file_match_rule: >-
      The binary must be in command position per the bash_mediated channel
      above (not merely present in the argument list — e.g. `echo "run cat
      foo.md"` does not match). A directory-target grep/rg call (e.g. `grep
      -r pattern knowledge-copilot/`) counts as reading every file named in
      that call's OWN tool_result output (the files rg/grep actually
      reported matches in), not every file under the directory argument —
      a call that finds zero matches reads nothing under this rule.
    source: "tools/cse-audit/product_usage.py (read_tool channel); tools/cse-bench/collectors/transcripts.py (bash_mediated channel, B-5)"

  knowledge_never_read_denominator:
    description: >-
      "Never read" is meaningless without stating what it is a fraction OF.
      Two denominators are both registered and both quotable — NEVER quote
      a never-read percentage without naming which one it uses.
    variants:
      knowledge_md: >-
        Count of *.md files under knowledge-copilot/, excluding vendored/
        tooling directories (node_modules, storybook build output, .git,
        .obsidian). 906 files as measured 2026-07-12
        (docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html: "True size: 906 knowledge markdown
        files — of 15,244 total"). Never-read share under this denominator
        was originally reported as 67% (67.1% precisely) — SUPERSEDED, see
        RECONCILIATION 2026-07-14 below; the reproducible baseline under
        this register's own matching rule is 80.8% (denominator 915,
        2026-07-12).
      all_files: >-
        Count of every file under knowledge-copilot/ regardless of
        extension, using tools/cse-audit/product_usage.py's
        inventory_knowledge_repo() (excludes only .git, .obsidian,
        node_modules by default). Never-read share under this denominator:
        ≈88%. CONFIRMED 2026-07-14 (was OPEN GAP): B-5's re-run
        (tools/cse-bench/collectors/transcripts.py, denominator 1724)
        measures 88.7% never-read — a 0.8% relative deviation from the
        original ≈88%, close enough that this exclusion set (in particular,
        that it does not need the storybook-build-output exclusion the
        knowledge_md variant carries) is confirmed to match what the
        re-audit used. This denominator variant is a clean machine-vs-machine
        reproduction, unlike knowledge_md below.
    reconciliation_2026_07_14: >-
      knowledge-never-read-rate was FAILING (last_checked 2026-07-12)
      because B-5's mechanical re-run reproduced all_files (88.7% vs ≈88%,
      0.8% relative deviation) but NOT knowledge_md (80.8% vs 67.1%, ~21%
      relative deviation) -- flagged then as "needs reconciliation, out of
      scope." Reconciled here. Diagnosis, proven with numbers and direct
      filesystem/git inspection, not asserted: the 67.1% figure was a
      ONE-OFF MANUAL reconciliation of the same stale-path-join problem
      that inflated Phase 1's original 95.6% headline -- the reaudit report
      itself documents this was NOT durably fixed in the underlying
      tooling ("the never-read join has no rename tracking and will
      silently re-break on the next repo reorg,"
      phase-1-reaudit-report.html "Probe blind spots found but not
      fixed"), only patched by hand for that one measurement. Direct
      inspection of the live knowledge-copilot tree confirms the kind of
      reorg a human can reconcile by judgment but a non-guessing matcher
      correctly cannot: (1) one-to-many directory splits, e.g.
      01-company/10-org-and-team.md is now the directory
      01-company/10-org-and-team/{00-roster.md, people/}, and
      01-company/08-case-studies/beacon.md is now
      01-company/08-case-studies/beacon/{00-case-study.md, 00-index.md,
      reference/} -- both confirmed live as directories, not files, and
      both surfaced as `unmatched` by transcripts.py's own
      read_tool_channel in a fresh 2026-07-14 run; (2) basename-changing
      renames outside the numeric-prefix pattern the collector's
      _basename_key fallback already handles, e.g. knowledge-copilot git
      history since 2026-05-01 shows
      01-company/08-case-studies/uber-freight/00-case-study.md renamed to
      01-company/12-pursuits/uber-freight/00-pursuit.md (git log
      --diff-filter=R) -- the basename itself changed, so no basename-key
      match, current or fallback, can ever land it. Both classes are
      exactly what this register's own knowledge_read definition requires
      the matcher to refuse to guess at (named_file_match_rule: ambiguous
      or non-unique matches are reported unmatched, never guessed) --
      matching conservatism (the register's original hypothesis) is
      confirmed as the mechanism, and it is concentrated in knowledge_md
      because a one-to-many directory split changes the *.md inventory's
      shape (one denominator entry becomes several) in a way that barely
      moves the much larger all_files pool's percentage. Corpus growth
      (906 -> 915 -> 920 knowledge_md files across the three measurement
      dates) is real but minor -- a ~1.5% denominator change, not the ~21%
      relative deviation being explained. This is not a collector bug (the
      unique-basename-fallback design is a deliberate, correct refusal to
      guess at ambiguous or basename-changing renames -- guessing would be
      the actual bug) and the original 906/67.1% figure was not
      ill-specified (its definition -- 906 knowledge_md files, both read
      channels -- is precise) -- it is simply NOT REPRODUCIBLE by any
      mechanical, non-guessing re-run, the same class of problem as
      definitions.turn's unrecoverable April figure. Per that precedent,
      the honest move is to re-baseline: 80.8% (knowledge_md, n=915) and
      88.7% (all_files, n=1724), both dated 2026-07-12, are now the
      register's reproducible baseline; 67.1% remains on record as a
      historical, non-reproducible data point (directionally consistent --
      both agree the corpus is cold, not dead) and must not be requoted as
      an ongoing target, exactly as Phase 1's 95.6% was already treated.
      See claim knowledge-never-read-rate.
    caveat: >-
      Phase 1's original headline (95.6% never read) is SUPERSEDED and must
      not be requoted; it combined a buggy path-join (a mid-corpus repo
      reorg broke ~60% of recorded reads) with a vendored-file-polluted
      denominator. See claim knowledge-never-read-rate.

  cli_invocation:
    description: >-
      A tool call counts as invoking a CLI product if the target binary
      name appears in COMMAND POSITION — the start of a Bash command
      string, or the start of a pipeline segment after &&, ||, |, ;, a
      backtick, $(, or a newline — never merely as a substring, a path
      component, a grep pattern, or prose in an argument. This is the same
      command-position rule the bash_mediated knowledge_read channel reuses.
    products:
      cli_copilot: >-
        Entrypoints: /opt/homebrew/bin/copilot (absolute path) or the
        `cli_copilot` console-script name — both classified
        cli_copilot_unambiguous. A bare `copilot` token in command position
        is tracked SEPARATELY as cli_copilot_bare (ambiguous: whether the
        Bash tool's non-interactive shell sources the `copilot` alias below
        cannot be determined from the transcript alone) and is NOT counted
        toward unambiguous cli_copilot usage.
      cc_framework: >-
        Entrypoint: `cc`, resolving to ~/.local/bin/cc (mirrored at
        /opt/homebrew/bin/cc) — claude-copilot's tool, a DIFFERENT product
        from cli_copilot (see docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md
        F-14: "four programs share two names"). Classified cc_framework
        only when the following token is a registered cc subcommand (see
        CC_FRAMEWORK_SUBCOMMANDS in product_usage.py) or --version/--help;
        otherwise cc_ambiguous. Must NEVER be merged into cli_copilot
        counts.
    excludes:
      - "the interactive `copilot` shell alias (`cd /Volumes/Dev/Sites/COPILOT`) — a directory change, not a program; per F-14 it shadows the binary in an interactive shell"
      - "`gh copilot` (GitHub's own Copilot CLI extension) — excluded by checking for a preceding `gh` token"
      - "`/usr/bin/cc`, the C compiler — excluded via a compiler-style-flag / source-object-argument / CC=-assignment / cargo|clang|gcc heuristic (cc_compiler bucket)"
    source: "tools/cse-audit/product_usage.py (classify_bash_command, CLI_COPILOT_BIN_RE, CC_BIN_RE, _CC_COMPILER_HINT_RE)"
    terminal_coverage: >-
      Terminal (non-transcript) invocations are covered by two independent,
      cross-validated sources: ~/.zsh_history (536 days, 2025-01-21..2026-07-12)
      and Warp's block history (warp.sqlite `commands` table, 207 days,
      2025-12-17..2026-07-12; B-3 probe 2026-07-12). For the overlapping window
      every command-position hit is a timestamp-exact duplicate across both
      sources (12/12 concordant); the pre-2025-12-17 stretch (~329 days) is
      single-source (zsh only) because Warp did not exist on this machine.
      CORRECTION (B-3, 2026-07-12): the re-audit's "14 terminal invocations"
      counted 2 mid-line textual mentions; under this definition's
      command-position rule the true count is 12 (11x uspto 2026-04-03,
      1x conv 2026-06-09; +2x db 2026-06-14 appear only in zsh — recheck
      classification during B-5). Bare `copilot` in interactive history is
      always the cd alias (84/84 Warp hits corroborated via pwd deltas).

  cli_liveness_classes:
    description: >-
      Every CLI Copilot leaf command is classified into exactly one of four
      liveness classes, per config presence x git activity x transcript
      usage x shell history, as used in the 2026-07-12 re-audit
      (docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html).
    classes:
      alive: "shows all four liveness signals (config present, recent git activity, transcript usage, shell-history usage)"
      occasional: "shows some but not all liveness signals — used, but not on every signal"
      never: "shows zero usage signals in any corpus checked, including commands whose backing service cannot even authenticate"
      local_utility: "commands that operate purely on local state (git, fs, shell, docker, docs, …) and therefore have no live-integration signal to show in the first place — liveness classification does not apply to them the same way"
    caveat: >-
      This classifies CLI Copilot's 439 leaf commands (22 service groups),
      not the same figure as Phase 1's original 208-command count, which
      was a ~2x undercount corrected by the re-audit.

  parity:
    description: >-
      Codex Copilot's fidelity to its claude-copilot upstream, checked at
      two distinct levels — never conflate them when reporting a parity
      result.
    levels:
      version_tuple: >-
        Compares 3 version-string fields between the two repos'
        parity manifests (codex-copilot/parity/claude-baseline.json).
        EXISTING, IMPLEMENTED. Passing this check proves the manifests
        AGREE on version numbers; it says nothing about whether the
        underlying agent/command/skill CONTENT actually matches.
      content_hash: >-
        Extends the version-tuple check to sha256 content hashes of every
        upstream `.claude/agents/*.md`, `.claude/commands/*.md`, and
        `.claude/skills/**/SKILL.md` file, so drift is visible the day it
        happens rather than only when a maintainer notices by inspection.
        BUILT (C-4, TASK-104, codex-copilot commit 740aa10, 2026-07-12) —
        `check-upstream-parity.py --content [--json]`, compared against
        the committed manifest `codex-copilot/parity/upstream-content-hashes.json`
        ({generated_at, upstream_commit, files: {relpath: sha256},
        versions}). `--update-baseline` is the explicit "we caught up"
        action (never implicit); see codex-copilot
        docs/05-reference/03-parity-contract.md "Sweep Workflow". This
        register's earlier note ("PROPOSED, NOT YET BUILT") is corrected
        here, not silently — see claim t6-two-harnesses-one-behavior for
        the current content-check result.
    source: "codex-copilot/parity/claude-baseline.json (version_tuple); codex-copilot/parity/upstream-content-hashes.json (content_hash); codex-copilot scripts/check-upstream-parity.py (both levels)"

  live_integration:
    description: >-
      A service is counted as a live integration if `copilot health --json`
      (CLI Copilot's top-level `health` command, cli-copilot/copilot_cli/main.py)
      reports healthy==true for it. Per-service check functions return a
      dict containing a "healthy" boolean (or, for legacy checks, a
      "status"=="healthy" string that main.py's health() coerces to the
      same boolean); a service that raises an exception during its check,
      or that main.py could not import at all, is treated as unhealthy
      (healthy=false), never as absent from the count.
    source: "cli-copilot/copilot_cli/main.py (health(); checks list; print_health)"
    caveat: >-
      As of this register's writing, 4 service groups exist in CLI Copilot
      but are NOT registered in the `health` command's checks list at all
      (phase-2-prd.md C-1: "registering the 4 service groups missing from
      copilot health"). Until that lands, a live_integration count is an
      undercount of true integration health, not a false one — services
      missing from the list are neither healthy nor unhealthy, they are
      simply not checked.

  outcome_bars:
    description: >-
      The nine ratified outcome measures (owner, 2026-07-13) that define
      ecosystem success: O-1 Time-to-First-Loveable-Solution (two timestamps,
      t_working and t_loveable, per solution); O-2 Solution Completeness
      (against a brief locked at start: sessions-to-done, tokens-to-done,
      post-ship fix-vs-feature ratio); O-3 Speed (prompt to finished solution
      vs bare-harness counterfactual); O-4 Token Efficiency (constraint not
      objective: aspiration 90 percent reduction, below 60 percent = work to
      do, never traded against O-1/O-3); O-5 Solution Survival (started ->
      shipped -> in-use at N weeks); O-6 Counterfactual Delta (4-config
      ladder); O-7 Voluntary Return Rate; O-8 Transfer Coefficient (>=2
      non-author users); O-9 Upkeep Tax. Unit of value = a Solution: a
      completed artifact resolving a real person's real problem. Full
      operational definitions: phases/phase-4-outcome-program-prd.md par.2.
    source: "docs/40-initiatives/01-cse-auditability/phases/phase-4-outcome-program-prd.md"
    note: >-
      Pre-registered before any outcome data exists (V-2). Gated on the
      Outcome Ledger (W-1), token joins (W-2), ladder harness (W-3), and
      external pilots (W-4). The removal rule rides on these: surface that
      moves no outcome bar within its review window is nominated for
      delete-or-defend.

  solution_token_accounting:
    description: >-
      W-2 (per-solution token accounting, phase-4-outcome-program-prd.md
      par.3 W-2): a solution's total tokens are computed TWO independent
      ways and the two are compared — never quoted alone.
    methods:
      ledger: >-
        The solutions.tokens_total column (claude-copilot tools/tc/,
        `tc solution log-usage --tokens N`) — a self-reported, cumulative
        figure a person types in. Per the W-1 (TASK-123) status gate,
        log_usage can only run once a solution has shipped
        (tc.services.solutions.log_usage), so today this column
        accumulates POST-ship usage entries, not a running total from
        first prompt — an inherited W-1 scope limit this task does not
        change; the ledger figure compared here is whatever a person has
        logged, however that entry was arrived at.
      transcripts_billed_volume: >-
        RENAMED + WINDOWED 2026-07-13 (QA WP-17 finding 1, correction
        commit -- was `transcripts`, unwindowed, in the original W-2
        landing; see join_keys.windowing below for the attribution bug
        this fixes). For every DISTINCT session_id recorded against a
        solution in solution_sessions, windowed to that solution's OWN
        touch span in that session, the sum of input_tokens +
        cache_creation_input_tokens + cache_read_input_tokens +
        output_tokens across every assistant message WHOSE OWN TIMESTAMP
        FALLS INSIDE THE WINDOW, in that session's main transcript file
        and its sibling subagent transcript files. This is billed API
        VOLUME (what the API metered for those messages), not marginal
        spend -- see transcripts_marginal_spend for the number comparable
        to a person's mental model of "tokens this took."
      transcripts_marginal_spend: >-
        NEW 2026-07-13 (QA WP-17 finding 2, correction commit, citing the
        F-8 precedent -- docs/40-initiatives/01-cse-auditability/phases/
        phase-1-findings.md: the falsified "~94% less context" figure was
        cache-read SHARE misattributed as the framework's own context
        reduction, a property of Claude Code's prompt caching, not the
        framework. Cumulative-summing transcripts_billed_volume across a
        long session has the same error class: cache_read_input_tokens
        reflects a turn's full re-read of the growing cached context, so
        summing it turn-over-turn conflates billed API volume with
        tokens actually spent doing NEW work). Same windowed message set
        as transcripts_billed_volume, but summing ONLY input_tokens +
        cache_creation_input_tokens + output_tokens -- i.e. EXCLUDING
        cache_read_input_tokens entirely (tokens re-read from an existing
        cache, not newly spent). This is the figure the tolerance below
        is primarily checked against, since a person typing a
        `tc solution log-usage --tokens` estimate is estimating marginal
        spend, not cumulative billed cache-read volume.
      event_ledger_c3: >-
        NOT YET BUILT. phase-4-outcome-program-prd.md par.3 W-2 names a
        third source — "the PostToolUse event ledger from C-3" — as what
        "makes this exact in every wired repo." C-3 (phase-2-prd.md
        C-series, "Observability hook rollout ... beyond the single wired
        repo") has not landed anywhere as of this register's writing (see
        phase-4-outcome-program-prd.md par.1, "Pending queues: R-series
        (phase 3), C-3/C-5, B-13/B-14/B-17") — no PostToolUse hook that
        logs a token/tool-call event ledger exists in any repo today. The
        two methods this register compares are therefore `ledger` vs
        `transcripts`, NOT `ledger` vs the C-3 event ledger — do not
        mistake `transcripts` for C-3's output.
    join_keys: >-
      repo_path (cwd at the time of the `tc solution` call), session_id
      (the CLAUDE_CODE_SESSION_ID environment variable Claude Code exports
      to every tool invocation inside a live session), and logged_at
      (datetime('now') at the call) — captured automatically by every
      solution-mutating `tc solution` subcommand (create, lock-brief,
      mark-working, mark-loveable, log-usage, close) into the additive
      solution_sessions table whenever that env var is present; absent
      outside a live session (e.g. scripts, tests), which is the honest
      no-signal state, not an error.
    windowing: >-
      CORRECTION 2026-07-13 (QA WP-17 finding 1, correction commit). The
      original W-2 landing attributed a session's ENTIRE transcript total
      to EVERY solution that shared its session_id — a real attribution
      bug (caught before any O-4 number was ever quoted, not after). Fix:
      per (solution, session_id) pair, window := [min(logged_at),
      max(logged_at)] across that solution's OWN solution_sessions
      touches in that session — first touch to last touch. Chosen over
      the other honest option (cross-solution per-touch bracketing, where
      each touch's window extends to the NEXT touch to ANY solution in
      that session) because a single solution's own touch rows are
      sufficient to bound its own window without reading every OTHER
      solution's touches in the same session — simpler, and still fixes
      the attribution bug, at the cost of the limitation below. Only
      assistant messages whose OWN timestamp falls inside the window
      count toward that solution's transcripts_billed_volume /
      transcripts_marginal_spend / waste_decomposition_heuristics
      figures for that session.
      KNOWN LIMITATION (disclosed, not padded away): a solution touched
      only once, or whose touches all land in the same instant (e.g. a
      scripted/synthetic sequence rather than real elapsed work), yields
      a near-zero-width window and near-zero attributed tokens. This
      join is only informative for solutions whose real `tc solution`
      commands are genuinely spread across the session's real elapsed
      time — the expected shape of real usage, not of a test script.
    tolerance: >-
      agrees_within_tolerance := abs(ledger_tokens - transcripts_marginal_spend)
      / transcripts_marginal_spend <= 0.20 (20 percent relative
      difference); undefined (null) when transcripts_marginal_spend is 0
      or either side has no data. Registered BEFORE any real two-method
      comparison was computed or looked at (V-2); value UNCHANGED by the
      2026-07-13 correction commit (QA WP-17) — only the windowing and
      the marginal-spend/billed-volume split were added, never the
      threshold. Rationale: `ledger` is a human-typed, rounded estimate,
      not a second machine-exact computation, so this tolerance measures
      "same order of magnitude, roughly right," not agreement between
      two machines that should be identical to the token. 20 percent is
      a deliberately loose first bar for that reason; expected to
      tighten once event_ledger_c3 lands and the comparison becomes
      machine-vs-machine. Applies PRIMARILY to ledger vs
      transcripts_marginal_spend (the figure comparable to a human
      estimate); ledger vs transcripts_billed_volume is reported
      alongside for transparency only — it is NOT expected to agree at
      the same order of magnitude (billed_volume includes cumulative
      cache-read re-reads, not new spend), and a billed_volume/ledger
      mismatch must never be quoted as if it were the same finding as a
      marginal_spend/ledger mismatch (the F-8 conflation this correction
      exists to avoid).
    source: >-
      claude-copilot tools/tc/src/tc/db/schema.py (solution_sessions
      table); tools/tc/src/tc/services/solutions.py
      (_record_session_touch); tools/cse-bench/collectors/economy.py (the
      join + comparison).
    verified_finding_initial_superseded: >-
      SUPERSEDED by verified_finding_corrected below (QA WP-17 caught the
      unwindowed attribution bug and the billed-volume/marginal-spend
      conflation this initial run exhibits). Kept verbatim as the honest
      record of what the FIRST run actually found, not deleted: TASK-124,
      2026-07-13, added AFTER the tolerance above was registered
      and the mechanism built (not a threshold change): a scratch-DB
      verification joined to this task's own real, live session
      transcript found transcript_tokens two-to-three orders of magnitude
      larger than a plausible human-typed ledger guess (~190M vs a 50,000
      test estimate). This is NOT a bug -- `cache_read_input_tokens` is
      billed PER ASSISTANT MESSAGE and reflects that turn's full re-read
      context, so summing it across every turn of a long, many-turn
      session (here: 1 main + 7 subagent files, 1,373 assistant messages
      total) is mathematically a cumulative sum, not a snapshot -- it
      legitimately grows to the tens/hundreds of millions for a
      real, heavily-delegated session. Two consequences worth recording:
      (1) a `tc solution log-usage --tokens` estimate typed against
      intuition (e.g. "current context size") will essentially never
      agree with this definition at real-world scale unless the person
      logging it is told explicitly to estimate the SAME cumulative-sum
      quantity; (2) a session_id shared across multiple unrelated tasks
      (Claude Code reuses one session_id across a long-lived orchestrator
      conversation) attributes that WHOLE session's cumulative usage to
      whichever solution happens to be joined to it -- the join is only
      a clean per-solution measure when the joined session is dedicated
      to that one solution. Neither point changes the registered 20%
      tolerance or the mechanism; both are open gaps for whoever next
      logs ledger tokens or interprets a comparison this collector
      produces.
    verified_finding_corrected: >-
      TASK-124, 2026-07-13, RE-RUN after the windowing + billed_volume/
      marginal_spend correction commit above (QA WP-17) -- same method
      (scratch DB, never written to a production tasks.db), but this time
      with the 5 touches given a REALISTIC simulated spread (18:40:00 ->
      19:00:30, ~20 minutes of this task's own real session, rather than
      the first run's 5 calls clustered in the same instant, which would
      have produced a near-zero window per the windowing's own disclosed
      limitation). Windowing worked as designed: transcripts_billed_volume
      dropped from the unwindowed run's ~190.2M to ~76.4M (the ~20-minute
      slice, not the whole ~83-minute session) -- confirms the attribution
      bug QA caught is fixed. transcripts_marginal_spend for that same
      slice: ~2.16M -- ~35x smaller than billed_volume, confirming
      cache_read dominated the old figure exactly as the F-8 precedent
      predicted. Comparison against a 20,000-token ledger guess: BOTH
      still fail the 20% tolerance (vs_marginal_spend delta_pct ~99.1%,
      vs_billed_volume ~99.97%) -- a plain negative finding, not hidden.
      Unlike the first run, this is not primarily a cache-read artifact:
      the ~20-minute window contains genuine heavy subagent delegation
      (multiple large exploration/read-heavy subagent conversations), so
      ~2.16M real marginal spend in ~20 minutes is itself a large but
      plausible number a hand-typed guess would not anticipate.
      mismatch_sessions is now non-empty for this window (was clean
      unwindowed) -- an honest, disclosed side effect of windowing: a
      delegation call and its subagent file's own activity can straddle
      a window boundary, so the windowed call-count and windowed
      file-count can legitimately disagree even when the unwindowed
      counts matched.
      NEW OPEN GAP surfaced by this re-run (not asked for by QA WP-17,
      not fixed here -- flagged, not silently patched, per V-2):
      failed_direction_tokens was 96.7% of billed_volume in this window,
      because failed_direction's check is literally the string
      "<promise>COMPLETE</promise>" (waste_decomposition_heuristics.
      categories.failed_direction) -- but only "me"/engineer-class agents
      in this CSE use that exact marker; other delegated roles (qa, ta,
      doc, cw, ...) signal completion in their OWN vocabulary (e.g. qa's
      "VERDICT: APPROVED" + "ARTIFACT:" markers), never the literal
      promise string, even when they succeeded. As registered, this
      heuristic therefore likely systematically OVER-counts
      failed_direction for any session with a normal multi-agent-role
      delegation mix. Not corrected in this commit (out of the two
      findings QA WP-17 asked for); left for a future correction commit
      once an owner/QA decision is made on how to recognize per-role
      completion vocabulary.

  waste_decomposition_heuristics:
    description: >-
      phase-4-outcome-program-prd.md par.3 W-2: "waste decomposition
      (re-explaining, rework, failed-direction tokens — classify by
      session structure; document the heuristics in the register first)."
      Registered BEFORE any waste-decomposition number was computed or
      looked at (V-2). Computed per session, then summed across a
      solution's joined sessions (definitions.solution_token_accounting
      join_keys). A given subagent (sidechain) transcript file is
      attributed to AT MOST ONE of failed_direction/rework (priority order
      below); re_explaining is scoped to main-session tokens only, a
      disjoint pool, so it never double-counts against the other two.
      WINDOWED 2026-07-13 (QA WP-17 finding 1, correction commit): every
      category below now uses the SAME per-(solution,session) touch
      window as definitions.solution_token_accounting.windowing — only
      messages (and, for rework's subagent-file matching, only subagent
      files with at least one message) inside the window are considered;
      the token totals below use the billed_volume formula (see
      solution_token_accounting.methods.transcripts_billed_volume).
    categories:
      failed_direction: >-
        Full windowed token total (billed_volume formula: input+
        cache_creation+cache_read+output, messages inside the solution's
        touch window only) of any subagent (sidechain) transcript file,
        WITHIN THE WINDOW, whose LAST real assistant-role message in the
        FULL file does not contain the literal string
        "<promise>COMPLETE</promise>" — i.e. the delegated direction ended
        BLOCKED, CONFUSED, or with no completion marker at all. The
        completion-marker check itself reads the whole file (a
        structural/qualitative signal, not token-windowed); only the
        token total attributed is windowed. Highest attribution priority
        (checked first).
      rework: >-
        Full windowed token total (billed_volume formula) of a subagent
        (sidechain) transcript file, WITHIN THE WINDOW, for a
        subagent_type already delegated to EARLIER in the same session's
        windowed delegation order (2nd-or-later occurrence of that
        subagent_type; the 1st occurrence is assumed the primary attempt,
        never rework), and not already counted under failed_direction.
        Subagent files are matched to the session's windowed, ordered
        list of Agent/Task tool_use calls by chronological order (each
        file's own first record timestamp) — an approximation valid
        where call count and file count agree 1:1; a per-session
        mismatch count is reported alongside so silent misattribution
        stays visible, never hidden.
      re_explaining: >-
        Windowed token total (billed_volume formula) of every
        main-session assistant message, WITHIN THE WINDOW, that is the
        first reply to a real user turn (jsonl_utils.is_real_user_turn)
        whose own text, normalized (stripped, lowercased,
        whitespace-collapsed), exactly matches an earlier real user
        turn's normalized text in the SAME session — a proxy for "the
        person had to repeat themselves." A deliberately conservative
        floor: paraphrased (non-exact) repeats are not caught, so this
        undercounts, never overcounts.
    caveat: >-
      All three are approximations of a genuinely fuzzy concept (why was a
      token spent); they exist to make waste VISIBLE and comparable
      release over release, not to be an exact ledger. waste_total is a
      lower bound given re_explaining's exact-match floor, the
      chronological-order assumption behind rework/failed_direction
      attribution, AND the windowing limitation
      (solution_token_accounting.windowing's known-limitation note
      applies here identically — a narrow or single-instant touch window
      undercounts waste the same way it undercounts total tokens).
    source: "tools/cse-bench/collectors/economy.py"

  ladder_config_materialization:
    description: >-
      TASK-125 (W-3): the 4-config ladder's config isolation, verbatim from
      phase-4-outcome-program-prd.md par.3 W-3 ("bare = fresh dir, no
      framework files; +framework = claude-copilot install; +knowledge =
      CC_KNOWLEDGE_REPO populated vs empty tree; +integrations =
      cli-copilot on PATH with .env"). Registered BEFORE any config was
      run against a real job (V-2). Three isolation levers (CORRECTED
      same-day per QA WP-23 finding 5 — see corrections below for what
      changed and why), all verified against this machine's
      actually-installed `claude` CLI before this register entry was
      written (`claude --help`, 2026-07-13): (1) a per-run HOME override
      to a fresh, empty temp directory — the PRIMARY isolator of
      user-level CLAUDE.md/agents/skills, since Claude Code auto-discovers
      those relative to $HOME/.claude/ and this machine already has
      claude-copilot installed at the user level (`~/.claude/agents/`,
      `~/.local/bin/{tc,cc}`); (2) `--setting-sources project` on every
      rung including bare — a SECONDARY restriction on which
      settings.json-style permission/hook sources merge, kept as
      defense-in-depth, not the CLAUDE.md/agent isolator; (3) a per-rung
      PATH override (bare/+framework exclude cli-copilot's venv bin; bare
      additionally excludes tc/cc's ~/.local/bin). `+framework`
      materializes `.claude/{agents,commands,skills}/` + CLAUDE.md by
      copying directly from claude-copilot's own VERSION.json roster (the
      same files `/setup-project`'s FULL-mode flow would copy), so the
      ladder measures a job's MARGINAL cost with the framework already
      installed, not the one-time setup cost. `+framework`'s
      CC_KNOWLEDGE_REPO points at an EMPTY directory (not unset),
      isolating "knowledge content present" from "knowledge mechanism
      present."
    source: "tools/cse-bench/benches/ladder/configs.py"
    corrections: >-
      TASK-125, same-day, QA WP-23 findings 4/5 (fixed, not a rewrite of
      the design — the mechanisms above were already correct; these are
      bugs in how they were described/keyed): finding 5 — an earlier
      version of this entry (and of configs.py's own docstring) credited
      `--setting-sources project` with isolating user-level CLAUDE.md/
      agents; QA verified the actual isolator is the per-run HOME
      override (lever 1 above), corrected here so the isolation doesn't
      silently break if a future edit removes the HOME override thinking
      `--setting-sources` alone covers it. Finding 4 — workdir/HOME
      materialization was keyed on (job_id, config_name) only; `--reps >
      1` silently reused the same directory across reps with no cleanup.
      Fixed: every workdir/HOME is now keyed on (job_id, config_name, rep)
      — verified via `python3 run.py --dry-run --reps 3 --job
      job-1-bugfix --config bare` producing three fully distinct
      rep1/rep2/rep3 directory trees.
    caveat: >-
      Environment gap found while building this (recorded, not hidden):
      `/opt/homebrew/bin/copilot` (the path this repo's README and
      benches/mcp_twin/run.py name) does not exist on this machine;
      `copilot` is also a shell alias for `cd` interactively. The real,
      working entry point used for +integrations is cli-copilot's own venv
      (`cli-copilot/.venv313/bin/copilot`, verified live via --help).
      Whether an isolated HOME still resolves Anthropic auth (OAuth/
      keychain) the same way the real HOME does is UNTESTED, not asserted
      as fact; verify on one throwaway cell before a full live run.

  ladder_job_pack_v1:
    description: >-
      TASK-125 (W-3): the ladder's job pack v1, per
      phase-4-outcome-program-prd.md par.3 W-3 ("3 real software jobs of
      graduated size ... candidates: an R-series fix implemented 4 ways;
      a small web utility; a data-pull-and-report task that exercises
      integrations. Pack format is pluggable"). Registered BEFORE any job
      was scored (V-2). Three jobs: job-1-bugfix (small — an R-series-
      style off-by-one fix, mechanical t_working via stdlib `unittest`,
      not pytest, since this machine's default python3 has no pytest
      importable, verified live); job-2-web-utility (medium — build a
      word-frequency CLI utility from scratch, mechanical t_working via
      an external checker diffing real output against a precomputed
      expected_output.txt); job-3-integration-report (large, exercises
      integrations — pull a service-health report via the copilot CLI,
      with an explicit, mechanically-checked honest-fallback path
      ("integrations unavailable") when the CLI is not reachable, so this
      job is reachable and t_working-passable from EVERY rung, not gated
      to +integrations alone — the ladder's O-1..O-4 deltas are what
      reveal whether +integrations actually helps, not a rigged pass/fail
      gate). Pack format is a plain, directly-importable Python module
      (job_pack.py), not a bank.yaml/rules.yaml-style file like this
      program's other benches: this machine's default python3 has no
      importable PyYAML (verified live), and a job pack's nested shape
      (per-job acceptance-check specs, fixture dirs) would need a bigger
      vendored parser than knowledge_qa/voice_lint's flat-row parsers
      already carry — "pack format is pluggable" is satisfied by
      run.py's --job/--config selectors reading this module, not by a
      file-format promise.
    source: "tools/cse-bench/benches/ladder/job_pack.py"

  ladder_job_pack_v2:
    description: >-
      TASK-142 (W-3b), superseding ladder_job_pack_v1: QA WP-76
      (adversarial verification of the first live ladder run,
      20260714T135253Z) proved v1 was an instrument that could not detect
      a framework advantage even if one existed — job-3's own check.py
      docstring admitted it "never asks did it reach real service data,"
      and job-1/job-2 required zero org knowledge or live integration
      data to solve correctly from ANY rung. v1's 12/12 t_working tie
      across all rungs was therefore not evidence of "no advantage"; it
      was evidence of an instrument with no discriminating power on that
      axis, by construction — see ladder-job-pack-v1-could-not-discriminate
      below. Registered BEFORE the v2 scored run's aggregate numbers were
      examined (V-2) — 6 throwaway pipeline-verification cells (job-2 x
      {bare, knowledge}, job-3 x {bare, integrations}, job-4 x {bare,
      framework}), written to a scratch out-dir never under output/, were
      run first to confirm the mechanism itself works, matching this
      bench's own established precedent (ladder_config_materialization's
      "verify on one throwaway cell before a full live run" caveat) — not
      a violation of pre-registration, since only the DESIGN is being
      registered here, and the scored 4x4x3 matrix's own aggregate had not
      been looked at when this text was written.

      Four jobs, one CONTROL + three DISCRIMINATING (a discriminating
      job's correct answer must be UNREACHABLE from a lower rung):

      - job-1-bugfix (CONTROL, unchanged from v1, small): any rung can
        solve it — a rung's failure to beat bare here is itself a
        meaningful null result, not noise, since it needs no knowledge/
        integration/decomposition advantage.
      - job-2-house-voice (KNOWLEDGE-discriminating, NEW, medium):
        correct output requires knowledge-copilot's real
        01-company/02-voice/06-glossary.md (Anti-Glossary: consultant/
        advisor/vendor/stakeholder/leverage/synergy/"best practice"/
        "touch base"/"circle back"/"deep dive" banned; Core Terms:
        "Copilot"/tension/breakthrough/co-creation/"struggling moment" as
        private substitutes) — verified present in the real repo and
        verified NOT inferable: this project's own CLAUDE.md (materialized
        for +framework too, with an EMPTY knowledge tree) only POINTS at
        01-company/02-voice/, it never states the banned/required terms
        itself, so +framework cannot pass by reading CLAUDE.md alone.
        fixtures/job-2-house-voice/check.py mechanically requires BOTH
        zero hits on the banned-term denylist AND >=1 hit on the private
        substitute-term set — avoiding jargon alone (satisfiable by luck)
        is not sufficient; positive evidence of the private vocabulary is
        required, closing the fabrication gap the same way (b) below
        does. Live-verified during pipeline smoke-testing: bare produced
        an honest "no house guidance found, using my own judgment"
        paragraph (t_working=False, zero private terms, as expected);
        knowledge produced a paragraph using the real glossary's terms
        (t_working=True).
      - job-3-integration-report (INTEGRATION-discriminating, brief
        mostly unchanged from v1, check.py REWRITTEN): now independently
        re-fetches real service health via `copilot --json health` by
        ABSOLUTE path + cli-copilot's own .env, NEVER through the
        config-under-test's PATH, and mechanically asserts the
        deliverable's per-service claims match ground truth exactly
        (name-by-name, status-by-status), not merely that a
        well-formed-looking line exists (v1's exact gap). A rung whose own
        env lacks `copilot` on PATH (bare/framework/knowledge) must use
        the honest "integrations unavailable" fallback — a populated
        "Services: N healthy" claim from such a rung now FAILS as
        fabrication by construction, regardless of whether N happens to
        match. A rung with `copilot` reachable (+integrations) must
        report real, exactly-matching data — falling back to "unavailable"
        despite having the capability now also FAILS. Live-verified
        during smoke-testing against this machine's real, currently-live
        `copilot --json health` (fireflies/reddit/metabase already cut
        per DEC-5/TASK-122; 20 services registered, 15 healthy / 4
        unhealthy (n8n, brevo, uspto, infisical -- auth/config gaps) / 1
        skipped (discord) at smoke-test time): +integrations produced a
        report whose 15-healthy claim and full 20-service breakdown
        matched live ground truth exactly (PASS); bare produced the
        honest unavailable fallback (PASS); a synthetic fabricated
        "Services: 99 healthy" and a synthetic single-wrong-status report
        were both mechanically caught (FAIL) in dry unit testing of
        check.py itself before any model call.
      - job-4-toolkit (FRAMEWORK-discriminating, NEW, large): toolkit.py
        must implement 6 independent, equally-weighted pure functions
        (is_prime, reverse_words, flatten, run_length_encode,
        most_common_word, caesar_cipher); fixtures/job-4-toolkit/
        test_toolkit.py (protected, 17 assertions across the 6) requires
        ALL 6 to pass at once — a completeness-across-parts bar sized so
        decomposition/systematic checklisting should matter, not a task a
        single free-write reliably nails end to end. t_working is
        strictly all-or-nothing (unittest's own exit code); per-function
        pass/fail is visible in the audit trail's acceptance stdout for
        richer post-hoc completeness analysis even when t_working is
        False.

      Fabrication resistance, closing the exact gap QA WP-76 named as the
      only prior guard (the rubric's error_help floor, human-judged only):
      every non-control job's check.py now MECHANICALLY verifies its
      discriminating property (job-2: banned/required term sets; job-3:
      service-by-service ground-truth match; job-4: full 17-assertion
      pass), not merely a well-formed shape.
    source: "tools/cse-bench/benches/ladder/job_pack.py"

  ladder-job-pack-v1-could-not-discriminate:
    description: >-
      Finding, not a defect fix: v1's null t_working result (12/12 tie
      across all 4 rungs, first live run 20260714T135253Z) cannot be read
      as "no framework advantage exists" because the instrument had no
      power to detect one on that axis — job-1/job-2 required zero org
      knowledge or live integration data to solve from ANY rung, and
      job-3's own check.py docstring stated outright it "never asks did
      it reach real service data." A null result from an instrument that
      cannot detect a positive is not evidence of absence. Recorded here,
      per this program's own "null and negative results are findings"
      convention (phase-4-outcome-program-prd.md par.5.4), as a finding
      about the FIRST INSTRUMENT, separate from ladder_job_pack_v2 (the
      fix) and from whatever v2's own scored run finds (a separate,
      later fact).
    source: "QA WP-76 (tc wp get 76), .copilot/wp/76.md Q4"

  ladder_mlp_rubric:
    description: >-
      TASK-125 (W-3): the written MLP expectation rubric that scores
      t_loveable, per phase-4-outcome-program-prd.md par.3 W-3
      ("guided experience, sensible defaults, error help, polish — judged
      blind, exemplar-anchored, deterministic checks where possible").
      Registered BEFORE any deliverable was scored (V-2). Each of the 4
      dimensions is scored 0-3 against a written exemplar anchor per
      level (not a bare good/bad scale); t_loveable = the first cell
      scoring >=2 on ALL 4 dimensions, not an averaged total (mirrors "MLP
      not MVP" literally). One hard floor set mechanically rather than
      left to judgment: job-3's error-help dimension scores 0 outright if
      the deliverable fabricates service data instead of honestly
      reporting integrations-unavailable. Blind judging protocol: the
      judge (human or, once ratified, a model) sees the brief, the
      deliverable, and the rubric — never the config name, ladder rung,
      elapsed time, or token count; enforced by construction
      (build_blind_judge_prompt() has no config-name parameter at all,
      not merely an instruction not to reveal one).
    source: "tools/cse-bench/benches/ladder/rubric.md"
    note: >-
      STATUS: DRAFT, NOT RATIFIED. Per phase-4-handoff.md §6 row 6 ("the
      MLP expectation rubric needs owner sign-off before first scoring —
      it operationalizes his bar"), this rubric requires owner ratification
      before ANY live t_loveable scoring — see
      docs/40-initiatives/01-cse-auditability/decisions/DEC-6-mlp-rubric-signoff.md.
      This is enforced MECHANICALLY, not just by convention: run.py's
      check_signoff() requires DEC-6's header to contain EXACTLY ONE
      "Status:" field whose value starts with "**ratified**", and refuses
      any non-dry-run invocation otherwise.
    corrections: >-
      TASK-125, same-day, QA WP-23 finding 2 (fixed): an earlier version
      of check_signoff() substring-searched DEC-6's whole header block for
      "Status: **ratified**" and could false-positive two ways — (a) an
      unrelated second "Status:" line in the header, (b) the marker hidden
      inside an HTML comment. Both
      closed by construction: HTML comments are stripped before matching,
      matching is per single physical line (never spanning a line break —
      the original regex's `\s*` could match a literal newline), and the
      gate fails CLOSED (not ratified) whenever it finds zero or more than
      one "Status:" field rather than accepting any match anywhere in the
      block. Regression tests for both bypasses (plus the original
      not-ratified/ratified cases) added:
      tools/cse-bench/benches/ladder/test_signoff_gate.py, 7/7 passing.

  value_density:
    description: >-
      TASK-128 (W-6): operationalizes the removal rule (phase-4-outcome-
      program-prd.md §0.5 / §2, "surface that does not move an outcome
      bar within its review window is a removal candidate... nominations
      are mechanical; deletions are the owner's"). Per CSE surface
      (agent, cli_service_group, knowledge_area, skill), density is
      NOT a continuous ratio for this first review -- several evidence
      channels are genuinely unavailable this early (no CLI usage
      ledger enabled anywhere; no skill-invocation event ledger; C-3's
      hook rollout still staged to claude-copilot only), so a real
      per-unit-time rate is not honestly computable yet. Instead this
      first review computes, per surface, two binary facts: whether
      usage_evidence is a MEASURED value (and what it is) or genuinely
      unmeasured (null), and whether outcome_trace is a MEASURED value
      or unmeasured (null). Registered BEFORE the collector that
      computes these numbers was written (V-2) -- collectors/value_density.py
      implements exactly this definition, joining already-computed
      collector output rather than re-deriving raw evidence a second
      way. A future review, once the CLI usage ledger and a broader
      hook rollout are both live, should upgrade this to a real
      per-unit-time density and register that change here rather than
      silently reinterpreting this entry.
    review_window: >-
      The full corpus available on this machine as of the first review
      (2026-07-13): the entire archive-UNION-live session transcript
      corpus (collectors/transcripts.py's DEFAULT_ARCHIVE_ROOT/
      DEFAULT_LIVE_ROOT merge) for agent usage signals, and the full
      knowledge-copilot corpus for the knowledge cross-reference check.
      No periodic (e.g. rolling-90-day) cadence has been ratified yet --
      there is one author and one corpus to date, so "the whole history"
      and "the current review window" are the same thing. The NEXT
      review (once W-4's external pilots and/or a second review cycle
      exist) should register a real rolling window here instead of
      reusing "all time" by default.
    usage_evidence_by_surface:
      agent: >-
        Count of subagent invocations attributed to that agent
        (attributionAgent field) across the review-window transcript
        corpus, exactly as collectors/framework_soul.py's
        agent_frugality.by_agent_type already computes (median_iqr's `n`
        field per agent) -- reused via output/framework_soul-latest.json,
        never recomputed a second way.
      cli_service_group: >-
        NULL for all 22 registered CLI service groups. CLI Copilot ships
        an opt-in usage ledger (COPILOT_USAGE_LOG,
        copilot_cli/shared/usage_ledger.py) that has never been enabled
        on this machine (~/.copilot-cli/usage.jsonl does not exist --
        verified directly, matching DEC-5's own finding). The only
        corpus-wide signal (collectors/transcripts.py's cli_invocation
        metric) is not broken out per service. This is an honest null,
        not a computed zero -- absence of instrumentation, not evidence
        of non-use.
      knowledge_area: >-
        % of a top-level knowledge-copilot/ directory's files that are
        referenced (by full relative path or, failing that, bare
        basename) anywhere else in the knowledge corpus or its manifest
        -- collectors/knowledge_soul.py's orphan_rate metric (metric 5),
        re-grouped by top-level directory rather than recomputed. This
        is DELIBERATELY the "referenced by other knowledge content"
        signal, not "read by an agent in a live session" -- the latter
        (knowledge_never_read_denominator, above) is only computed as an
        ecosystem-wide aggregate today, not sliceable per top-level
        directory without re-deriving the corpus scan a second way (the
        exact drift risk tools/cse-bench/README.md's "no metric ... may
        be reported without a claims.yaml entry" rule warns against). Per
        knowledge_soul.py's own orphan_rate docstring this is a FLOOR on
        true orphan rate (a permissive basename fallback), not a point
        measurement -- carried through unchanged here.
      skill: >-
        NULL for all 37 .claude/skills/**/SKILL.md leaf skills. Skill
        invocation is not tracked by any event ledger anywhere in the
        ecosystem yet (the same C-3 hook rollout that would carry this
        signal is staged to claude-copilot only, per phase-4-handoff.md
        TASK-103). Honest null, not a computed zero.
    outcome_trace_by_surface:
      agent: >-
        Whether the agent has a golden-set eval AND that eval currently
        passes (output/evals-latest.json's per_agent[<agent>].overall_pass).
        An agent with NO eval at all (15 of 16 today, per claim
        agent-eval-coverage) is trace=null (unmeasured), never
        trace=false (measured-and-failing) -- never scored is a
        different finding than scored-and-found-wanting.
      cli_service_group: >-
        Whether the service passes cli-copilot's own SOUL §6 conformance
        scorecard (collectors/cli_soul.py) with zero gaps. A
        conformance/quality-bar proxy, not a literal outcome-bar (O-1..
        O-9) trace -- no outcome bar is wired to any individual CLI
        service yet; labeled as a proxy everywhere it is reported.
      knowledge_area: >-
        NULL for every area -- no outcome bar is wired to any specific
        knowledge area yet (the knowledge_qa bench's +98pp accuracy
        result is corpus-wide across 2 product dossiers, not a per-area
        outcome trace).
      skill: >-
        NULL for every skill -- no outcome bar or eval is wired to any
        individual skill yet.
    nomination_rule: >-
      A surface is a MECHANICAL removal nomination this review iff its
      usage_evidence is a MEASURED zero (not null/unavailable) AND its
      outcome_trace is anything other than a measured true (i.e. a
      measured false, or unmeasured/null). A surface whose usage_evidence
      is null is NEVER nominated on that basis alone: absence of
      instrumentation is not evidence of non-use, and nominating on
      missing data would violate this program's own honesty rule
      (phase-4-handoff.md §5.4). This is deliberately a HIGHER bar than
      "no evidence of use" -- it is "measured evidence of no use."
    threshold: >-
      Binary (measured-zero-usage AND not-measured-true-outcome) for
      agent and knowledge_area, the only two surfaces with a real
      measured usage_evidence channel today -- see the description field
      above for why a continuous per-unit-time cutoff is not yet honestly
      computable. knowledge_area additionally requires >=10 scope files
      (MIN_KNOWLEDGE_AREA_FILES; the orphan-rate proxy is too noisy on a
      handful of files) and an orphan rate >=50% (a literal majority of
      the area's files unreferenced anywhere else) -- a natural,
      non-arbitrary bright line chosen independently of any specific
      area's number, not fitted post-hoc to produce a particular result.
      cli_service_group and skill usage_evidence is null ecosystem-wide
      (see usage_evidence_by_surface above), so NO cli_service_group or
      skill surface is mechanically nominated this review on usage
      grounds -- the CLI services with an actual STRUCTURAL-ABSENCE
      finding (fireflies, reddit, metabase, method) are already covered
      by DEC-5 and this review defers to that memo rather than
      re-deriving the same finding.
    source: "tools/cse-bench/collectors/value_density.py"

  outcome_transfer_tolerance:
    description: >-
      O-8 (Transfer Coefficient)'s pre-registered pass/fail tolerance: what
      "within a stated tolerance of the owner's numbers" (outcome_bars'
      O-8 definition) means precisely, for at least 2 non-author pilots
      each attempting O-1..O-5 unassisted. Registered 2026-07-14 BEFORE any
      pilot has been recruited and before any pilot data exists (V-2) —
      this is the only reason picking a number is defensible right now:
      nobody has seen a result this number could be bent toward. Full
      spec drafted in
      docs/40-initiatives/01-cse-auditability/phases/phase-4-w4-external-pilot-kit.md
      §2 ("O-8 tolerance — PROPOSED, owner ratification pending").
    status: "proposed — owner ratification pending"
    ratio_bars: >-
      O-1 (TTFLS), O-3 (Speed), O-4 (Token efficiency), and O-2's
      sessions-to-done all use the SAME single ratio: pilot's figure <=
      2.0x the author's size-matched comparable solution (lower/faster/
      fewer-tokens always clears). Rationale for 2.0x: the loosest
      widely-defensible "same order of magnitude" bound — tight enough to
      fail a visibly-struggling pilot (3x+), loose enough not to fail
      ordinary first-time-tool ramp-up variance trivially at a 1.0x
      (exact-match) bar, which would tell us nothing. One consistent
      multiplier across all four ratio bars, deliberately, rather than
      four separately-tuned numbers with no data to justify the
      differences.
    flat_band_bars: >-
      O-2's post-ship fix-vs-feature ratio uses a flat 20-percentage-point
      band (pilot's fix-fraction must not exceed the author's fix-fraction
      by more than 20pp, absolute) instead of a ratio — a multiplicative
      bound is degenerate near a fix-fraction close to zero (e.g. an
      author fix-fraction of 0.05 would force a pilot ceiling of 0.10
      under a 2.0x rule, far tighter than intended).
    binary_bars: >-
      O-5 (Survival) is binary, not a ratio: small-N pilot data (2-3
      people) makes a percentage meaningless. O-5 clears for a pilot iff
      their solution both shipped AND was confirmed still in active use
      at the SAME N-weeks checkpoint used for the author's own O-5
      measurement. No partial credit.
    matching_rule: >-
      "Author's own most comparable solution" is matched by tokens_total
      order of magnitude (same decade band, e.g. both in the 10k-100k
      token range) — the one sizing dimension every tc solution record is
      guaranteed to carry via the ledger. If no author solution in a
      comparable size band exists yet when a pilot's data is ready to
      score, that bar is not-yet-measurable for that pilot (mirrors O-7's
      own not-yet-measured convention) — never a fabricated ratio against
      a mismatched solution.
    aggregate_rule: >-
      O-8 requires ALL FIVE of O-1..O-5 to clear their own tolerance, for
      at least 2 of the >=2 required pilots. Per-pilot, per-bar pass/fail
      is reported in full in each pilot's own report regardless of the
      aggregate outcome — a pilot clearing 4 of 5 bars is a real,
      informative partial result, not noise to discard.
    caveat: >-
      V-2-legitimate ONLY because zero pilot data exists on this machine
      or anywhere in this program as of registration — the moment any
      pilot's numbers are looked at, this tolerance can no longer be
      chosen or revised without breaking pre-registration. Marked
      owner-overridable before the first pilot: the owner may accept this
      proposal as-is, edit it, or replace it outright before W-4's first
      pilot's data is scored; once scored, changing it becomes a
      post-hoc adjustment, not a pre-registration.
    source: "docs/40-initiatives/01-cse-auditability/phases/phase-4-w4-external-pilot-kit.md §2"

  claim_sweep:
    description: >-
      TASK-137 (t2-no-claim-outlives-its-check): the CSE-wide claim sweep
      the README's Phase-0 vision named and this register's own SCOPE NOTE
      said was "not yet" built. Registered BEFORE the sweep was run for a
      number (V-2) — this entry defines what the sweep counts, how it
      decides BACKED vs UNBACKED, and states its precision honestly, so the
      mechanism cannot be tuned after seeing the result.
    verifiable_assertion: >-
      A "verifiable assertion" is a quantified or mechanism-guarantee claim
      matched by one of seven regex categories over a curated set of each
      product's self-description docs (SCAN_TARGETS in claim_sweep.py —
      README.md, SOUL.md, PURPOSE.md/ECOSYSTEM.md, and a small number of
      F-18-cited architecture docs; NOT knowledge-copilot's ~900-file
      content corpus, and NOT CLAUDE.md files — see caveat below):
      PERCENT (`\d+%`), N_OF_M (`\d+ of \d+`), COUNT_UNIT (`\d+ agents/
      services/files/...`), FRACTION (`\d+/\d+`), VERSION (`v?\d+\.\d+`,
      excluding the SOUL.md template's own document-changelog lines —
      "Revised to vX.Y", "STATUS: RATIFIED", the Changelog table — which
      describe the DOCUMENT's version, not a product claim), RATIO_X
      (`\d+x`), and MECHANISM_CLAIM (automatically/ensures/guarantees).
      Text inside fenced code blocks and inline code spans is excluded
      (overwhelmingly commands/paths, not prose).
    backing_convention: >-
      An assertion is BACKED if an `<!-- claim-check: <id> -->` HTML
      comment appears on the same line, the line before, or the line
      after, AND <id> resolves to a real entry under this file's `claims:`
      list. An annotation whose id does NOT resolve is DANGLING (reported
      separately, never silently treated as backed). Chosen over trying to
      parse English more cleverly: the annotation makes backing explicit
      and cheap for a doc author to add, and cheap for the sweep to check,
      rather than the tool guessing which registered claim a sentence
      means. This is the same shape as knowledge-copilot's
      scripts/check-crosslinks.py baseline/regression convention, applied
      to prose assertions instead of markdown links.
    baseline: >-
      tools/cse-bench/claim-sweep-baseline.json — a regression gate, not a
      zero-unbacked gate (mirrors crosslinks-baseline.json exactly): every
      UNBACKED/DANGLING assertion found at baseline-authoring time is
      recorded (repo, file, category, matched text — a sha256 key, no line
      number, so the baseline survives unrelated line-shift edits, same
      tradeoff crosslinks-baseline.json makes). `claim_sweep.py --check`
      fails ONLY on a NEW unbacked assertion not already in the baseline —
      the ratchet only tightens. `--update-baseline` is the explicit,
      reviewed "accept this backlog" action, never implicit.
    precision_caveat: >-
      Stated plainly, not buried: this is a regex/heuristic scanner over a
      CURATED doc list, not a general claim-extraction engine. KNOWN FALSE
      POSITIVES: a genuinely non-claim decimal or count noun in ordinary
      prose (e.g. a Python version requirement, a section number) matches
      a category and is reported UNBACKED even though it isn't really a
      product claim needing a mechanism — these get baselined, not
      specially suppressed, so the baseline is an honest (if slightly
      noisy) upper bound. KNOWN FALSE NEGATIVES: any assertion outside
      SCAN_TARGETS (the ~900 knowledge-copilot content files, CLAUDE.md
      files, and every doc not explicitly listed) is invisible to this
      tool; any assertion phrased without a matched pattern (e.g. spelled-
      out numbers, comparative claims with no digit) is invisible; text
      inside inline code spans is never scanned. First real run
      (2026-07-14, see claim t2-no-claim-outlives-its-check for the full
      before/after numbers): 92 verifiable assertions
      across 4 of 5 scanned repos (codex-copilot's curated docs had zero
      after the changelog exclusion), 0 backed (the annotation convention
      is new — nothing in any product doc used it before this task), 92
      unbacked, 0 dangling. This is the honest starting backlog this
      register's V-2 discipline requires being stated, not hidden.
    source: "tools/cse-bench/claim_sweep.py"

  upkeep_tax:
    description: >-
      O-9 (Upkeep Tax, outcome_bars): the share of ecosystem effort spent
      MAINTAINING the CSE itself -- registry, links, freshness, parity
      sweeps, claim upkeep, framework/knowledge/CLI hygiene -- rather
      than producing solutions, netted against the value delivered.
      "What counts as maintenance" is an editorial judgment call, made
      explicitly per task or per PRD via `tc upkeep tag-task` / `tc
      upkeep tag-prd` (claude-copilot tools/tc/, tc.services.upkeep) --
      NEVER inferred automatically from task text, title keywords, or
      any heuristic this register does not name. Registered BEFORE any
      `tc upkeep tag-*` command had been run anywhere (V-2) -- this
      methodology was written from the SHAPE of tc's existing task/PRD
      schema and collectors/economy.py's existing token formulas, not
      from looking at what any repo's number would be. Two independent
      methods are registered, NEVER conflated into one number:
    methods:
      task_count_heuristic: >-
        RETROSPECTIVE. upkeep_tasks / total_tasks in a repo's Task
        Copilot store (`tasks` table), where upkeep_tasks is every task
        with a row in the additive `upkeep_tags` table (claude-copilot
        tools/tc/src/tc/db/schema.py UPKEEP_SCHEMA_SQL). Populated two
        ways: `tc upkeep tag-task <id>` (one task, explicit,
        method='explicit') or `tc upkeep tag-prd <id>` (bulk-tags EVERY
        task already filed under that PRD in one call,
        method='prd-heuristic', and never overwrites an explicit tag --
        tc.services.upkeep.tag_prd). The PRD-level flag is what makes
        this method retrospective: flagging a PRD today reclassifies
        tasks that were created -- and completed -- before this feature
        existed, so the tax is computable from task/PRD data that
        ALREADY EXISTS, not only from sessions tagged from now on.
        Honest limitation, stated plainly: this is a share of TASK
        COUNT, not of time or tokens (the PRD's literal unit) -- a
        proxy, not an exact accounting, because tasks predating this
        feature carry no session-id or token linkage a collector could
        reconstruct retroactively. Denominator is ALL tasks regardless
        of status (pending/in_progress/completed/blocked/cancelled):
        completed-only would undercount genuine upkeep effort spent on
        work that was blocked, superseded, or abandoned -- still real
        effort.
      session_token_exact: >-
        PROSPECTIVE (from the moment this feature exists onward) and
        token-exact. `tc upkeep tag-task`, when run inside a live Claude
        Code session (CLAUDE_CODE_SESSION_ID set), appends a row to the
        additive `upkeep_sessions` table -- the same session-touch
        mechanism solution_token_accounting's `ledger` method uses
        (tc.services.solutions._record_session_touch), mirrored verbatim
        for upkeep by tc.services.upkeep._record_upkeep_session_touch.
        cse-bench's upkeep collector (tools/cse-bench/collectors/
        upkeep.py) joins every DISTINCT tagged session_id to that
        session's own transcript (main + subagent files) and sums
        tokens using the EXACT SAME formulas solution_token_accounting
        registers (methods.transcripts_marginal_spend /
        .transcripts_billed_volume, reused directly from
        collectors/economy.py's _billed_volume/_marginal_spend, not
        re-implemented): marginal_spend (input + cache_creation +
        output, cache_read EXCLUDED) is PRIMARY, billed_volume is
        secondary/transparency-only -- the same F-8-class conflation
        correction this register already applied to W-2 (cumulative-
        summing cache_read misattributes a turn's full cache re-read as
        new work). UNWINDOWED, unlike W-2's per-solution join: one `tc
        upkeep tag-task` call is a single whole-session judgment ("this
        session was upkeep"), not a shared/intermittent touch across
        several ledger entries, so there is no shared-session ambiguity
        to window away -- the collector sums each tagged session's FULL
        transcript.
    netting_rule: >-
      task_count_heuristic nets against ALL tracked task effort in the
      same store (upkeep_tasks / total_tasks) -- it does not reference
      solutions at all, since most stores have none yet (see
      solution_token_accounting; W-1 adoption is early). session_token_exact
      nets against the Outcome Ledger directly: upkeep_tax_ratio_marginal_spend
      = upkeep_tokens_marginal_spend / (upkeep_tokens_marginal_spend +
      outcome_tokens_marginal_spend), where outcome_tokens_marginal_spend
      is the SAME whole-session marginal-spend sum, computed identically,
      over solution_sessions' own distinct session ids (the Outcome
      Ledger's W-2 session touches) -- symmetric, apples-to-apples, never
      comparing a windowed figure (W-2's economy collector) against an
      unwindowed one (this collector) on the same side of a ratio. A
      session_id present in BOTH upkeep_sessions and solution_sessions
      (tagged upkeep AND touched a solution) is ambiguous and is
      EXCLUDED from both sides, reported separately as
      overlap_sessions_excluded -- never silently assigned to either.
    threshold: >-
      None registered. Unlike O-4 (Token Efficiency), which the PRD
      gives an explicit 60/90% band, the PRD's O-9 text names no target
      number -- O-9 is a measurement bar, not a pass/fail bound. A high
      upkeep tax is a finding to act on (via the removal rule, W-6), not
      a defined failure of this claim.
    caveat: >-
      Editorial judgment calls this register does NOT make on anyone's
      behalf: which PRDs/tasks in any given repo count as "maintenance"
      is left to whoever runs `tc upkeep tag-task` / `tag-prd` in that
      repo, and is disclosed per-repo in metrics.per_repo[].by_kind /
      .prds_flagged / .by_method, never hidden inside a single
      ecosystem-wide number. A repo where `tc upkeep` has never run
      reports task_count_share: null (upkeep_instrumentation_present:
      false) -- the honest pre-adoption state, not a fabricated 0%.
    source: >-
      tools/cse-bench/collectors/upkeep.py; claude-copilot
      tools/tc/src/tc/services/upkeep.py; claude-copilot
      tools/tc/src/tc/db/schema.py (UPKEEP_SCHEMA_SQL)

claims:

  - id: outcome-ttfls
    statement: "A new user reaches a first loveable solution (t_loveable) within their first working session; the t_loveable minus t_working gap quantifies the design chain's value."
    definition_refs: [outcome_bars]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only solutions && python3 -c \"import json; print(json.load(open('output/solutions-latest.json'))['metrics']['totals']['ttfls_seconds'])\""
    status: unchecked
    evidence: "W-1 Outcome Ledger landed (claude-copilot tools/tc/ `tc solution` verbs + tools/cse-bench/collectors/solutions.py, TASK-123): the ledger and collector are live and run clean (errors: []), but zero solutions have both t_working and t_loveable set yet -- an honestly empty ledger, not a measured result. Re-run the check above once the owner's next real solution reaches mark-loveable."
    last_checked: "2026-07-13"

  - id: outcome-completeness
    statement: "Shipped solutions match their locked brief, and post-ship activity is dominated by new features, not fixes, over the review window."
    definition_refs: [outcome_bars]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only solutions && python3 -c \"import json; print(json.load(open('output/solutions-latest.json'))['metrics']['totals']['completeness'])\""
    status: unchecked
    evidence: "W-1 Outcome Ledger landed (claude-copilot tools/tc/ `tc solution` verbs + tools/cse-bench/collectors/solutions.py, TASK-123): sessions-to-done/tokens-to-done/post-ship fix-vs-feature-share are computed and run clean (errors: []), but zero solutions have shipped against a locked brief yet -- an honestly empty ledger, not a measured result."
    last_checked: "2026-07-13"

  - id: outcome-speed-observed
    statement: "Observed elapsed time (first prompt -> working -> loveable -> shipped) is captured per solution from ledger timestamps alone. This is O-3's OBSERVED half only -- it carries no counterfactual bare-harness comparison; that comparison is O-6's job (outcome-counterfactual-delta, the W-3 ladder harness) and is a separate, not-yet-built measurement."
    definition_refs: [outcome_bars]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only solutions && python3 -c \"import json; print(json.load(open('output/solutions-latest.json'))['metrics']['totals']['speed_observed_seconds'])\""
    status: unchecked
    evidence: "W-1 Outcome Ledger landed (claude-copilot tools/tc/ `tc solution` verbs + tools/cse-bench/collectors/solutions.py, TASK-123): started_at/t_working/t_loveable/closed_at are captured and the collector computes started->working/started->loveable/started->shipped elapsed seconds, run clean (errors: []), but zero solutions exist yet -- an honestly empty ledger, not a measured result. Pre-registered here (V-2) before any data exists, per phase-4-outcome-program-prd.md par.3 W-1 ('a cse-bench collector solutions.py emitting O-1/O-2/O-3(observed)/O-5')."
    last_checked: "2026-07-13"

  - id: outcome-token-efficiency
    statement: "Creating a finished solution through the CSE uses at least 60 percent fewer tokens than the bare-harness counterfactual (aspiration: 90 percent), with O-1/O-3 non-degraded. Constraint, not objective."
    definition_refs: [outcome_bars, solution_token_accounting]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only economy && python3 -c \"import json; print(json.load(open('output/economy-latest.json'))['metrics']['totals'])\""
    status: unchecked
    evidence: "W-2 landed (TASK-124, claude-copilot tools/tc/ solution_sessions join table + tools/cse-bench/collectors/economy.py), then CORRECTED same-day per QA WP-17 (windowing + billed_volume/marginal_spend split -- see definitions.solution_token_accounting.windowing and .methods): the CSE-side half of O-4 (a solution's tokens, computed two independent ways -- ledger vs transcripts, windowed to each solution's own touch span, joined via repo+time+session-id -- and cross-checked against the tolerance) is now real and runnable, replacing the prior no-instrumentation gate. Flipped gated->unchecked because the check command is now real, not because the full statement has been evaluated: this claim's 60/90 percent figure also needs a bare-harness counterfactual denominator, which is W-3's ladder harness (harness now built and dry-run validated, TASK-125 -- but still unchecked pending owner rubric sign-off, see outcome-counterfactual-delta and ladder-harness-dry-run-validated), so no percentage can be reported yet. The production ledger (tools/cse-bench's own repo glob) has zero real solutions as of this run, same honest-empty state as outcome-ttfls/outcome-completeness/outcome-survival -- see token-accounting-dual-method-agreement for the real-transcript verification this task ran instead (a scratch DB, not a production solution record)."
    last_checked: "2026-07-13"

  - id: token-accounting-dual-method-agreement
    statement: "For at least one completed solution with a joined session, tokens computed via the ledger and via transcripts (transcripts_marginal_spend, primarily) agree within the registered tolerance (definitions.solution_token_accounting)."
    definition_refs: [solution_token_accounting, waste_decomposition_heuristics]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only economy && python3 -c \"import json; d=json.load(open('output/economy-latest.json')); print(d['metrics']['totals']); print(d['errors'])\""
    status: unchecked
    evidence: "TASK-124 (W-2), CORRECTED same-day per QA WP-17: the join table, both windowed accounting methods (transcripts_billed_volume, transcripts_marginal_spend), and the waste-decomposition heuristics are implemented, run clean (errors: []), and were RE-verified against REAL transcript data after the correction -- a scratch-DB solution (deliberately NOT written into any production tasks.db) joined to this task's own real, live session, with a realistic ~20-minute simulated touch spread (not the first run's same-instant cluster). Result: windowing works as designed (billed_volume dropped from the pre-correction ~190.2M, the whole ~83-minute session, to ~76.4M, the actual ~20-minute window); marginal_spend (~2.16M) is ~35x smaller than billed_volume, confirming cache_read dominated the old figure per the F-8 precedent. BOTH still fail the 20% tolerance against a 20,000-token ledger guess (vs_marginal_spend delta_pct ~99.1%, vs_billed_volume ~99.97%) -- an honest negative finding, not hidden, and for a genuinely different reason than the first run's cache-read artifact (this window's real marginal spend is itself large due to heavy subagent delegation). See definitions.solution_token_accounting.verified_finding_corrected for full detail, including a newly-surfaced, NOT-yet-fixed open gap: the failed_direction heuristic's literal '<promise>COMPLETE</promise>' check does not recognize other agent roles' own completion vocabulary (e.g. qa's VERDICT/ARTIFACT markers), so it likely over-counts failed_direction in a normal multi-role session -- flagged, not silently patched. Zero real (non-scratch) solutions with a joined session exist in any production ledger as of this run -- the full acceptance criterion (a real completed solution, not a verification scratch-DB) remains open."
    last_checked: "2026-07-13"

  - id: outcome-survival
    statement: "Of solutions started, a stated majority ship; of shipped, a stated majority remain in active use at the N-week check. Sustained use is the primary loveability evidence."
    definition_refs: [outcome_bars]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only solutions && python3 -c \"import json; print(json.load(open('output/solutions-latest.json'))['metrics']['totals']['survival'])\""
    status: unchecked
    evidence: "W-1 Outcome Ledger landed (claude-copilot tools/tc/ `tc solution` verbs + tools/cse-bench/collectors/solutions.py, TASK-123): started/shipped/in-use counts and ratios are computed and run clean (errors: []), but zero solutions have been started yet -- an honestly empty ledger, not a measured result."
    last_checked: "2026-07-13"

  - id: outcome-counterfactual-delta
    statement: "The 4-config ladder (bare -> +framework -> +knowledge -> +integrations) shows each component contributing positively to O-1..O-4; a component with no positive contribution enters the removal queue."
    definition_refs: [outcome_bars, ladder_config_materialization, ladder_job_pack_v1, ladder_mlp_rubric]
    check: "cd tools/cse-bench/benches/ladder && python3 run.py --dry-run"
    status: unchecked
    evidence: "TASK-125 (W-3): the ladder harness is built (tools/cse-bench/benches/ladder/{run.py,configs.py,job_pack.py,rubric.md}) and fully dry-run validated -- 4 configs x 3 jobs = 12 cells materialize cleanly (0 wiring problems, 0 claude invocations, nothing written to output/, per the command above). Flipped gated->unchecked because the check command is now real (the dry-run), not because the full statement has been evaluated: NO live scored ladder run has executed. This is deliberate, not an oversight -- the PRD's own MLP rubric requires owner sign-off BEFORE first scoring (phase-4-handoff.md §6 row 6), and run.py's check_signoff() mechanically refuses any non-dry-run invocation until docs/40-initiatives/01-cse-auditability/decisions/DEC-6-mlp-rubric-signoff.md's header reads 'Status: **ratified**' (AS OF 2026-07-13, the date this evidence was written: not yet ratified, verified by the dry-run's own printed gate line). CORRECTED 2026-07-14 (QA WP-47): DEC-6 was ratified by the owner 2026-07-14 (in-conversation, Option A, no edits to rubric.md) -- the sign-off gate this paragraph describes as blocking is therefore now open. This claim's own `status` is left exactly as this task found it (`unchecked`) and NOT flipped here: whether a live scored ladder run has since happened, and what it found, is the ladder lane's own result to report, not restated or guessed at by this correction. See ladder-harness-dry-run-validated for the mechanism-level check, and DEC-6 for the ratification record. No O-1..O-4 numbers exist for any config or job as of THIS evidence's own last_checked date. CORRECTED same-day per QA WP-23 (see ladder_config_materialization.corrections and ladder-harness-dry-run-validated's own evidence for the four capture-method fixes: O-4 now primarily marginal_spend not billed_volume alone, an F-8-class conflation fix; the sign-off gate closes two false-positive bypasses; per-call cost ceilings default to real values instead of None/unbounded; per-rep workdir/HOME isolation replaces prior config-only keying). Nothing about the ladder's counterfactual delta or token-efficiency percentages is known yet -- these are capture-method corrections to instrumentation that still has zero real data, not new findings."
    last_checked: "2026-07-13"

  - id: ladder-v2-o6-discriminating-verdict
    statement: "The job-pack-v2 discriminating ladder run (TASK-142) shows whether ANY rung beats bare on a job whose correct answer is genuinely unreachable from a lower rung -- the first honest test of this, since v1 could not discriminate (see ladder-job-pack-v1-could-not-discriminate)."
    definition_refs: [outcome_bars, ladder_config_materialization, ladder_job_pack_v2, ladder_mlp_rubric]
    check: "cat tools/cse-bench/output/bench_ladder-runs/20260714T144205Z/job-2-house-voice__*__rep1.json | python3 -c \"import json,sys; [print(json.loads(l)['config'], json.loads(l)['acceptance']['passed']) for l in sys.stdin]\" 2>/dev/null; ls tools/cse-bench/output/bench_ladder-runs/20260714T144205Z/"
    status: passing
    evidence: >-
      TASK-142, 2026-07-14, real scored run (4 configs x 4 jobs x 3 reps =
      48 cells, model=sonnet, --max-budget-usd 4.0, total cost $8.70 for
      this batch): YES, a rung beats bare on a discriminating job, twice,
      mechanically verified, not judged. job-2-house-voice (KNOWLEDGE):
      bare 0/3, framework 0/3, knowledge 3/3, integrations 3/3 -- a clean,
      complete separation; bare/framework cells honestly report no house
      guidance found (0 fabricated attempts across all 6), knowledge/
      integrations cells use the real knowledge-copilot glossary's private
      terms (tension/co-creation/struggling moment/copilot) while avoiding
      every Anti-Glossary term, mechanically verified by check.py, not
      merely judged. job-3-integration-report (INTEGRATIONS): all 4 rungs
      pass t_working (12/12, by design -- an honest-fallback job), but
      check.py now independently re-fetches live ground truth and verifies
      per-service correctness (closing QA WP-76's exact gap): ONLY
      +integrations (3/3 reps) ever produced real, exactly-matching
      service data (20/20 services correct, live-verified against a fresh
      `copilot --json health` call at check time); bare/framework/
      knowledge (9/9 cells) all used the honest "integrations unavailable"
      fallback -- zero fabrication attempts anywhere in 12 cells.
      job-4-toolkit (FRAMEWORK/decomposition): a genuine, robust null --
      all 4 rungs pass 17/17 assertions on all 3 reps (12/12); Sonnet
      solves this 6-part completeness bar reliably even bare, so this job
      shows no decomposition advantage in this run (see
      ladder-o4-scaffold-attribution-wp79-closed for the same null
      extended to the 2 new ablation rungs). job-1-bugfix (CONTROL): 12/12
      as expected. t_loveable (blind-scored, same disclosed
      not-independent-judge limitation as the first run --
      see t_loveable_blind_scoring/README.md): job-2 is a uniform null
      (0/12) on t_loveable specifically -- NOT because no rung did better,
      but because rubric.md's dimensions 1.1/1.3 were written for
      software-artifact deliverables and structurally cannot score a
      prose-only about.md; job-4 is also a uniform t_loveable null (0/12,
      no docstrings/error-handling on any rung); job-3 shows a
      counter-intuitive split -- the 9 honest-fallback cells all reach
      t_loveable=true (their prose explanation satisfies all 4 dimensions)
      while the 3 real-data +integrations cells do NOT (a correct flat
      data dump with no remediation guidance for the 4 unhealthy services
      it correctly found) -- the rubric rewards explanatory prose over
      verified-correct data on this job, a rubric-fit finding recorded in
      t_loveable_blind_scoring/README.md, not smoothed over.
    last_checked: "2026-07-14"

  - id: ladder-o4-scaffold-attribution-wp79-closed
    statement: "QA WP-79's gate (no O-4 number may be published until a same-model, same-task-type matched ablation closes CLAUDE.md/agent-frontmatter's residual attribution) is closed: turn-1 cache_creation is isolated per rung, in situ, at the ladder's own real model (sonnet) and real jobs, not a synthetic haiku/no-tool probe."
    definition_refs: [ladder_config_materialization, ladder_job_pack_v2]
    check: "cd tools/cse-bench/benches/ladder && python3 -c \"import configs; assert 'framework_minus_claudemd' in configs.CONFIG_NAMES and 'framework_minus_agents' in configs.CONFIG_NAMES; print('OK', configs.CONFIG_NAMES)\""
    status: passing
    evidence: >-
      TASK-142, 2026-07-14: two new ablation rungs (framework_minus_claudemd,
      framework_minus_agents -- configs.py) ran at the SAME model (sonnet,
      now recorded per-cell via `record["model"]`, not only in the
      top-level aggregate, per WP-79's own "this confound can never recur
      silently" requirement) against all 4 job-pack-v2 jobs x 3 reps (24
      cells, $1.78 + $1.90 = $3.68); turn-1 cache_creation isolated from
      later turns for every cell in the run (48 original + 24 ablation =
      72 cells total) via run.py's new extract_turn_breakdown(), reading
      the real Claude Code session transcript (message.id-deduped), not
      reconstructed after the fact. Result, averaged across the 4 jobs:
      mean turn-1 premium (+framework vs bare) = 3003 tokens; CLAUDE.md's
      OWN in-situ contribution (+framework vs framework_minus_claudemd) =
      1228 tokens (40.9%); the agent roster's OWN in-situ contribution
      (+framework vs framework_minus_agents) = 1071 tokens (35.7%);
      COMBINED ATTRIBUTED = 2298 tokens (76.5% of the premium) -- UP from
      WP-79's own ~62% cross-model estimate, confirming same-model/
      same-task-type measurement closes much of the gap, but a REAL
      RESIDUAL SURVIVES: 704 tokens (23.5%) per job on average (range
      14.5%-29.9% across the 4 jobs), not attributed to either named
      component. NOT rounded away: closing it fully would need the same
      ablation extended to the remaining untested scaffold pieces
      (.claude/commands/, templates/skills/, .mcp.json) as their own
      isolated rungs -- not done here (named as the explicit next step,
      not silently assumed negligible). Separately, verified directly
      (file diff against the live template, not inferred from commit
      timestamps): every cell in this entire run materialized
      claude-copilot's POST-trim CLAUDE.template.md (commit 0c73f65,
      TASK-127/W-5, landed before this run) -- the trim's effect is
      already baked into the "1228 token" CLAUDE.md figure above, not a
      separate before/after delta (no reliable pre-trim same-model
      baseline exists: the one pre-trim live run, TASK-125's first run,
      never captured turn-level data and its ephemeral run_root cannot be
      unambiguously re-identified among several later runs' leftover temp
      directories on this machine -- marked, not guessed, same as the
      contaminated-artifact finding above). A capability-control result,
      found while building this ablation, not asked for but too material
      to omit: on ALL 3 discriminating jobs, t_working was IDENTICAL with
      CLAUDE.md removed and IDENTICAL with the entire 13-agent roster
      removed (job-2 0/3=0/3=0/3=0/3, job-3 3/3 all four, job-4 3/3 all
      four) -- neither ablation caused a single regression on this job
      pack, at this task difficulty, with Sonnet, while both cost real,
      measured average tokens (1228 and 1071 respectively). Scoped
      finding, not a general claim that these components are worthless --
      stated plainly per this program's own honesty convention.
    last_checked: "2026-07-14"

  - id: ladder-harness-dry-run-validated
    statement: "The W-3 ladder harness's full pipeline (config materialization x job-pack wiring x acceptance-check wiring x rubric structural consistency x the DEC-6 sign-off gate) runs end-to-end without error in --dry-run mode, with zero live model calls."
    definition_refs: [ladder_config_materialization, ladder_job_pack_v1, ladder_mlp_rubric]
    check: "cd tools/cse-bench/benches/ladder && python3 run.py --dry-run"
    status: passing
    evidence: "TASK-125, 2026-07-13: `python3 run.py --dry-run` materializes all 4 configs x 3 jobs = 12 cells (real, side-effect-scoped directory operations under a fresh tempfile.mkdtemp() -- .claude/{agents,commands,skills}/ + CLAUDE.md correctly present for +framework/+knowledge/+integrations and correctly ABSENT for bare; CC_KNOWLEDGE_REPO correctly None for bare, an empty tree for +framework, the real knowledge-copilot repo for +knowledge/+integrations; PATH correctly excludes tc/cc/copilot for bare, restores tc/cc for +framework/+knowledge, adds cli-copilot's venv bin only for +integrations), reports 0 acceptance-check wiring problems across all 3 jobs, validates job_pack.py (0 structural problems) and rubric.md (all 4 dimensions present) at startup, and correctly reported the DEC-6 sign-off gate as ratified=False AS OF 2026-07-13 (the date this evidence was written and DEC-6 had not yet been ratified). A live (non-dry-run) invocation was also verified to exit 1 with an explicit refusal message rather than proceed. CORRECTED 2026-07-14 (QA WP-47): DEC-6 was ratified by the owner 2026-07-14 -- the gate no longer reads ratified=False today. This claim's `status` (passing) and `check` describe the --dry-run MECHANISM working correctly (gate enforcement, materialization, wiring) and are unaffected either way; this correction only fixes the stale present-tense "ratified=False" assertion in this evidence text. It does not report on any live (non-dry-run) ladder run that may have happened since ratification -- that result belongs to the ladder lane, not to this mechanism-level claim. Exit code 0; nothing written to output/, per this bench's own convention (see README.md 'Dry-run output'). RE-VERIFIED same-day after QA WP-23's fixes (four correction items: O-4 capture method, sign-off gate bypasses, cost ceiling defaults, rep-keyed materialization -- see ladder_config_materialization.corrections and ladder_mlp_rubric.corrections): `python3 run.py --dry-run` still exits 0 with 12/12 cells OK; the live-run refusal still exits 1; 7/7 new regression tests in test_signoff_gate.py pass (covering both QA-found bypasses); `python3 run.py --dry-run --reps 3 --job job-1-bugfix --config bare` produces 3 fully distinct rep1/rep2/rep3 directory trees, confirming the rep-independence fix."
    last_checked: "2026-07-13"

  - id: outcome-return-rate
    statement: "After a first solution, users choose the CSE for their next problem unprompted."
    definition_refs: [outcome_bars]
    check: "gated on W-4 external pilot"
    status: gated
    evidence: "phases/phase-4-outcome-program-prd.md par.2 (O-7)"
    last_checked: "2026-07-13"

  - id: outcome-transfer
    statement: "Non-author users achieve O-1..O-5 unassisted within a stated tolerance of the owner's numbers (>=2 external users)."
    definition_refs: [outcome_bars, outcome_transfer_tolerance]
    check: "gated on W-4 external pilot; tolerance pre-registered 2026-07-14 (see definitions.outcome_transfer_tolerance) pending owner ratification — see phase-4-w4-external-pilot-kit.md §2"
    status: gated
    evidence: "phases/phase-4-outcome-program-prd.md par.2 (O-8). V-2-legitimate pre-registration ONLY because zero pilot data exists on this machine or anywhere in this program as of 2026-07-14 — the tolerance (definitions.outcome_transfer_tolerance) was drafted and registered before any pilot was recruited or any pilot number was looked at, and is marked owner-overridable up until the first pilot's data is scored. Status STAYS gated: pre-registering the yardstick does not manufacture the >=2 external users this claim's own truth condition requires; see outcome-return-rate and t8-value-transfers-beyond-author for the same Act-B dependency."
    last_checked: "2026-07-14"

  - id: outcome-upkeep-tax
    statement: "The time and tokens spent maintaining the CSE itself are measured and netted against outcome value in every value statement."
    definition_refs: [outcome_bars, upkeep_tax]
    check: >-
      CORRECTED 2026-07-14 (the original check tested only clause 1; see
      evidence). `cd tools/cse-bench && python3 cse_bench.py collect
      --only upkeep && python3 -c "import json; d=json.load(open(
      'output/upkeep-latest.json'))['metrics']['totals']
      ['session_token_exact']; up=d['upkeep_tokens_marginal_spend'];
      out=d['outcome_tokens_marginal_spend']; print(d); measured = up is
      not None; netted = out is not None and out > 0; print('MEASURED:',
      measured, ' NETTED:', netted)"` -- the statement is a CONJUNCTION,
      both clauses must hold for `passing`: (a) MEASURED -- upkeep token/
      task spend is actually being measured (upkeep_tokens_marginal_spend
      non-null, OR task_count_heuristic.upkeep_tasks > 0 -- either
      registered method suffices for this clause alone); (b) NETTED --
      outcome_tokens_marginal_spend > 0 for the SAME run, i.e. real
      solution-attributed tokens exist to net upkeep against, not merely
      a zero produced by there being no outcome-side data at all
      (task_count_heuristic cannot satisfy this clause by itself -- it
      does not net against outcome value, only against total task count;
      see definitions.upkeep_tax.netting_rule). errors: [] required
      either way. `unchecked` (not `failing`) when (a) holds and (b) does
      not -- the instrument is honestly awaiting the Outcome Ledger's
      first real solution, the same state outcome-ttfls/outcome-
      completeness/outcome-survival are already registered `unchecked`
      for. Re-run once a real solution ships with a joined, transcript-
      matched session (`tc solution` W-1/W-2); this check then decides
      the claim honestly in whichever direction the number lands.
    status: unchecked
    evidence: >-
      CORRECTION 2026-07-14: this claim was flipped to `passing` earlier
      this session on a check that tested only the statement's first
      clause ("measured") and never tested the second ("netted against
      outcome value") -- a check weaker than its own statement, the exact
      t2-no-claim-outlives-its-check disease this register exists to make
      impossible, caught in review and fixed here (never a silent
      rewrite). The check above now tests both clauses; this row records
      the honest result of running it.

      Clause 1 (MEASURED) is genuinely true and stays true: instrumentation
      landed (claude-copilot tools/tc/ `tc upkeep` verbs, TASK-138) and
      ran for real. task_count_heuristic (retrospective): `tc upkeep
      tag-prd 9` bulk-tagged all pre-existing tasks under PRD-9 ("CSE
      Verification & Benchmark Program + SOUL Remediation" -- this
      initiative's own hygiene/registry/measurement work, not a customer
      solution; editorial call, disclosed, not hidden) plus TASK-138
      itself = 23 of this repo's tracked tasks are upkeep, ~68% of this
      repo (23/34 at last re-run; this repo is the measurement/ops repo
      for the initiative, not a product repo, so a high share here is
      expected, not a finding) and ~11.5% ecosystem-wide (23/200; 10
      repos scanned, only this one has run `tc upkeep` so far -- most
      repos show upkeep_instrumentation_present: false, pre-adoption, not
      zero). These task-count figures move slightly run to run as the
      shared tasks.db gains tasks from concurrent work; re-run for the
      exact current count.

      Clause 2 (NETTED) is where the honest headline finding lives, and
      it is stated here PLAINLY, not softened into a footnote:
      session_token_exact (tagging TASK-138 captured this live session's
      real id, joined to its transcript) computes
      upkeep_tax_ratio_marginal_spend = 1.0, because
      upkeep_tokens_marginal_spend is real and positive (~21M at last
      re-run) while outcome_tokens_marginal_spend = 0. Read plainly:
      **100% of the ecosystem's measured token effort, right now, is
      upkeep; 0% is solution production.** That is the single most
      important number this register has produced so far, and it answers
      the owner's founding question -- "how do I prove any of this is
      useful?" -- in a direction that is not flattering. Its caveats do
      not soften that number, they scope it: n=1 tagged session (this
      method has never run before this task -- there is nothing else to
      compare it against yet); the tagged session is unwindowed by design
      (definitions.upkeep_tax.methods.session_token_exact), so it also
      carries this session's other work, not only TASK-138's; and the 0%
      solution-production side is zero not because no solutions are being
      built, but because the Outcome Ledger (W-1/W-2) has zero solutions
      with joined, transcript-matched session tokens ANYWHERE in the
      ecosystem yet (RULING-AGENDA.md §6) -- there is no outcome value on
      record to net against. This number WILL move the moment one real
      solution is tracked end-to-end with a joined session, and should be
      re-run then, not assumed static.

      Because clause 2 has no positive outcome-side figure to net
      against, the claim's own truth condition is not met today --
      status is `unchecked`, matching outcome-ttfls/outcome-completeness/
      outcome-survival's identical honest state (an empty Outcome Ledger,
      not a measured result), not `failing` (the instrument is not broken
      and the statement is not disproven -- it correctly reports it has
      nothing to net against yet). Both methods run clean (errors: []);
      re-run: `cd tools/cse-bench && python3 cse_bench.py collect --only
      upkeep` -> output/upkeep-latest.json.metrics.totals / .per_repo.
    last_checked: "2026-07-14"

  # --- Program truth conditions (phase-2-prd.md §2, T1-T8) ---------------

  - id: t1-ecosystem-sees-itself
    statement: "Every CSE component leaves durable, queryable usage evidence as a side effect of normal work: transcripts are retained (not pruned), tool events are logged at source, and CLI invocations are recorded (opt-in)."
    definition_refs: []
    check: "manual — inspect ~/.claude/transcript-archive/runs.log (retention) and cli-copilot for an invocation ledger (C-1, not yet built)"
    status: failing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-2-prd.md (T1 row); tools/cse-bench/retention/retain_transcripts.sh (B-1, now running — partial evidence transcript retention is fixed); CLI invocation logging (C-1) does not exist yet"
    last_checked: "2026-07-12"

  - id: t2-no-claim-outlives-its-check
    statement: "Every public CSE claim maps to a pre-registered, runnable check; unverifiable claims are deleted; the F-18 artifact-without-mechanism count is zero, enforced on commit."
    definition_refs: [claim_sweep]
    check: "tools/cse-bench/check_claims.py (register structure); tools/cse-bench/claim_sweep.py (the CSE-wide sweep, TASK-137 — BUILT this task); tools/cse-bench/claim_sweep.py --check (pre-commit enforcement, installed this task in all 5 repos)"
    status: failing
    evidence: >-
      TASK-137 (2026-07-14): the mechanism this claim was blocked on ("needs
      the full CSE-wide ~40-claim sweep... plus F-18's 7 instances driven to
      zero, enforced on commit" — decisions/RULING-AGENDA.md §6) is BUILT and
      RUN, not just proposed. F-18's SEVEN NAMED INSTANCES are individually
      resolved (0 of 7 remain artifact-without-mechanism): #1 PreToolUse
      matcher — BACKED (hook-deadlock-fix-verified: TASK-106/C-6 fixed and
      tested, 48/49 replay assertions pass, live matcher = Bash|Read|Edit|
      Agent). #2 Restructure doc's "After" column — CORRECTED (the false
      "Sonnet ~94%" model-tier row, still uncorrected in-doc until this
      task, now carries a correction note + claim-check annotation to
      model-tier-opus-dominant-main-session, which proves it false: 94.5%
      Opus, not 94% Sonnet). #3 Extension loader — BACKED
      (knowledge-extension-install-mechanism: install-extensions.py is
      real, tested, wired into sync-knowledge.sh; PURPOSE.md's prose was
      already accurate, now annotated). #4 Glossary — BACKED
      (knowledge-glossary-manifest-resolves: the manifest's `.glossary` key
      now resolves to an existing file; PURPOSE.md annotated). #5
      task-copilot MCP dist/index.js — BACKED
      (task-copilot-mcp-dist-exists: the file now exists, 66,120 bytes,
      verified built; no doc annotation needed, the original artifact was
      a config file outside SCAN_TARGETS). #6 "--json versioned contract" —
      CORRECTED (cli-json-contract-not-versioned: cli-copilot's own doc no
      longer says "versioned" — found already fixed pre-task; schema_version
      itself remains genuinely absent, an open gap, honestly registered as
      such, not claimed fixed). #7 Codex parity scorecard — BACKED-AS-HONEST
      (codex-specialist-chain-scorecard-not-run, status unchecked: the
      template really is unrun, the product's own docs never claimed
      otherwise, so there was no false claim to correct — the artifact now
      has a mechanism tracking its true state instead of silence). See each
      claim's own evidence for full detail; phase-1-findings.md's F-18
      table is otherwise unchanged (a historical record, not rewritten per
      V-2).


      BUT the claim's OWN TRUTH CONDITION IS BROADER than F-18's 7 named
      instances, and does NOT pass: claim_sweep.py's first real run against
      the ecosystem's curated self-description docs (README/SOUL/PURPOSE/
      ECOSYSTEM.md + the F-18-cited architecture docs — see the claim_sweep
      definition's SCAN_TARGETS and precision_caveat) found 94 verifiable
      assertions across 4 of 5 scanned repos (codex-copilot's curated docs
      had zero after the SOUL-changelog exclusion): 12 BACKED (all via this
      task's own annotations — the <!-- claim-check: --> convention did not
      exist in any product doc before this task), 82 UNBACKED, 0 DANGLING.
      Per repo: claude-copilot 54 scanned / 11 backed / 43 unbacked;
      cli-copilot 5 scanned / 0 backed / 5 unbacked; copilot-control-tower
      32 scanned / 0 backed / 32 unbacked (all in SOUL.md's own body prose —
      version numbers already excluded via the changelog-line rule, so
      these are FRACTION/MECHANISM_CLAIM/VERSION hits in the document's
      substantive content); knowledge-copilot 3 scanned / 1 backed / 2
      unbacked. "Every public CSE claim maps to a pre-registered check" is
      false while 82 assertions sit outside the register — the sweep makes
      this VISIBLE and MEASURABLE for the first time, which is this task's
      job, but visibility is not resolution. The 82 are baselined
      (tools/cse-bench/claim-sweep-baseline.json) as the honest, disclosed
      starting backlog — NOT silently accepted: `claim_sweep.py --check`
      (installed on commit in all 5 repos this task) blocks any NEW
      unbacked assertion from this point forward, so the backlog can only
      shrink, never silently grow. What remains for this claim to ever
      pass: someone triages the 82 baselined assertions one at a time
      (annotate with a real registered claim + check, correct the prose if
      false, or delete it if unverifiable) the same way this task closed
      F-18's 7 — that is real, un-shortcuttable editorial work this task
      does not do wholesale, both because most of the 82 are pre-existing
      SOUL.md/README.md claims unrelated to F-18 and because forcing that
      triage in one pass, under time pressure, is exactly the "produced by
      choosing operational definitions after seeing the data" failure mode
      V-2 exists to prevent. Not forcing a pass.


      UPDATED 2026-07-14 (t2 triage pass, same day, following QA WP-47's
      audit of the sweep mechanism itself): TWO layers of work landed.
      (1) THE GATE WAS LEAKY -- QA found and this task closed 3 real
      bypasses in claim_sweep.py: a TOCTOU gap (--check scanned the working
      tree, not the git index, so a staged-then-reverted-in-place bad
      assertion could land in a commit while the hook reported clean --
      proven both ways via an isolated scratch-repo reproduction, now fixed
      by scanning `git show :<path>` / `git ls-files`); a coverage gap
      (SCAN_TARGETS was 19 hand-picked files, silently excluding every
      CLAUDE.md -- control-tower's own CLAUDE.md had a live unbacked
      "automatically"/"guarantee" pair as a result; scope is now a
      documented formula -- root self-description files + docs/ depth-1 --
      not an implicit allowlist); an over-backing gap (the old +/-1
      either-direction annotation rule let one annotation nominally back an
      unrelated adjacent line by proximity alone -- found live in
      claude-copilot's restructure doc; binding tightened to same-line or a
      standalone-preceding line only; all 12 previously-backed assertions
      re-audited, 2 nominal ones correctly demoted and re-fixed). A gate
      that can be walked around is decoration, not a rule -- these are
      fixed, not just documented, and re-verified via `--check` on staged
      content in every touched repo before each commit.


      (2) THE BACKLOG TRIAGE, scoped by the now-WIDER sweep: after the
      coverage fix, the sweep finds 133 verifiable assertions across 39
      files in 4 repos (codex-copilot: still 0, its curated docs make no
      quantified claims) -- 51 BACKED (10 pre-existing + 41 from this
      pass's annotations/reused-claim-backing/corrections), 82 UNBACKED, 0
      DANGLING. The 82 remaining are NOT a random remainder -- every one
      falls into one of three named, disclosed categories: (a) SOUL.md
      files (42 of 82) -- explicitly out of scope per this task's own
      guardrail, another lane owns them; (b) CLAUDE.md governance/
      instruction files (15 of 82, across all 4 repos) -- deliberately
      deferred rather than rushed, since these are high-blast-radius
      operating-instruction docs (one is this very session's own CLAUDE.md)
      that deserve a dedicated review, not a same-session grep-and-edit
      (one exception WAS fixed directly: control-tower-agent-count, a
      clean, low-risk, mechanically-verified count); (c) newly-in-scope
      docs the coverage fix surfaced for the first time and this session's
      time budget did not reach (25 of 82: session HANDOFF/AUDIT/RESUME
      logs, docs/README.md index files, cli-copilot's docs/01-setup.md,
      claude-copilot's docs/00-overview.md and docs/README.md, knowledge-
      copilot's docs/knowledge-copilot-integration-plan.md). All 82 are
      baselined (claim-sweep-baseline.json, regenerated this session) and
      the pre-commit hook (verified live and re-proven this session: a
      real test commit with a fresh unbacked assertion was blocked, exit 1,
      then reverted) still blocks any NEW unbacked assertion.


      STATUS STAYS FAILING. Per the statement's own three clauses: clause 1
      ("every public CSE claim maps to a pre-registered, runnable check")
      is false while 82 assertions sit unbacked, even though the true
      denominator grew (94->133) because the gate itself got harder to
      satisfy, not easier -- that is the intended direction of a ratchet,
      not evidence of regression. Clause 2 ("unverifiable claims are
      deleted") saw real, if modest, exercise this session: no CSE-doc
      prose was deleted as unverifiable-in-principle, but three ALREADY-
      RULED register claims that were unverifiable/superseded/deleted-
      obligation were retired (turn-definition-incompatible-with-april ->
      retired-by-unverifiability per DEC-10; framework-externalization-
      94pct -> retired-by-ratification per DEC-3, EXECUTED in claude-
      copilot@e1e1501; protocol-declaration-rate-baseline -> retired-by-
      deletion per DEC-2, EXECUTED in claude-copilot@1b67851) -- these were
      blocked on check_claims.py's closed 4-value status enum, extended
      this session to unblock all three. Clause 3 (F-18 zero, enforced on
      commit) remains true and independently re-verified: 0 of 7 named
      instances regressed, and the hook enforcement is now demonstrably
      harder to bypass than when F-18 was first closed. What remains, exact
      and unchanged in kind from the prior update: someone triages the
      (now-recategorized) 82 baselined assertions -- the SOUL.md/CLAUDE.md
      categories need their owning lanes' sign-off before this task's kind
      of session should touch them at all, and the 25 newly-surfaced items
      need the same annotate-or-correct-or-delete pass this session gave
      the other 51. Not forcing a pass.
    last_checked: "2026-07-14"

  - id: t3-instruction-layer-changes-behavior
    statement: "Enforcement fires where the docs say it does (or the docs stop saying it); delegation/protocol adoption is measured at source; every specialist agent has a passing golden-set eval."
    definition_refs: [delegation_rate_tool_share, delegation_rate_event_share, protocol_declaration]
    check: "manual — python3 tools/cse-audit/run_probe.py (in claude-copilot) plus a headcount of cc eval golden sets"
    status: failing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-1, F-2, F-4); phase-2-prd.md (T3 row: 'evals exist for 1 of 16 agents')"
    last_checked: "2026-07-12"

  - id: t4-knowledge-layer-changes-output
    statement: "With-knowledge output beats without-knowledge output on private-fact accuracy; voice conformance shows a delta attributable to the knowledge repo rather than to prompting alone."
    definition_refs: [knowledge_read]
    check: "manual — checked 2026-07-14 against the already-registered B-9/B-10 claims (knowledge-factual-accuracy-delta, voice-conformance-deltas), both passing, last_checked 2026-07-12; no fresh live-model run needed, existing dated evidence answers both clauses"
    status: failing
    evidence: "Clause A (private-fact accuracy) PASSES: knowledge-factual-accuracy-delta, with=98.0% vs without=0.0%, delta +98.0pp. Clause B (voice delta attributable to the repo rather than prompting alone) FALSIFIED: voice-conformance-deltas shows knowledge(0.48)<bare(2.77) but rules-in-prompt(0.10)<knowledge(0.48) — compiled-rules prompting alone outperforms the raw repo-as-context, so the repo's marginal value over prompting is not demonstrated (in fact negative). T4 statement is a conjunction; one falsified clause fails the whole claim. See tools/cse-bench/benches/voice_lint/README.md (\"Does the REPO add value over just pasting the compiled rules into the prompt?\" — the sharp question this run answers no to) and benches/knowledge_qa/README.md's Results section. This is a finding about how the knowledge layer actually delivers value — via distillation into prompts, not via raw repo context — not a failure of the knowledge layer to help at all (clause A still passes decisively)."
    last_checked: "2026-07-14"

  - id: t5-integration-layer-pays-its-way
    statement: "Live integrations authenticate; retained CLI commands show usage; dead surface is pruned or revived; the CLI-vs-MCP advantage is measured and honestly bounded."
    definition_refs: [cli_liveness_classes, live_integration]
    check: "manual — see docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html liveness breakdown; copilot health --json for live_integration"
    status: failing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-2-prd.md (T5 row: '~23% of 439 commands plausibly alive')"
    last_checked: "2026-07-12"

  - id: t6-two-harnesses-one-behavior
    statement: "Claude/Codex parity is checked at content and behavior level; drift is bounded and alarmed — 'same solution, different harness' is a tested property, not a slogan."
    definition_refs: [parity]
    check: "cd /Users/pabs/Sites/COPILOT/codex-copilot && python3 scripts/check-upstream-parity.py --content --json (both parity levels, C-4 now built); cross-checked via cd tools/cse-bench && python3 cse_bench.py collect --only parity"
    status: failing
    evidence: >-
      Was failing under "content_hash level is C-4, not yet built"
      (phase-2-prd.md T6 row: "Parity check covers 3 version strings
      only") — that gap has since closed: C-4 landed (codex-copilot commit
      740aa10, 2026-07-12; see definitions.parity.levels.content_hash,
      corrected in the same commit as this claim). RE-RUN 2026-07-14 (this
      task): version_tuple level PASSES (`mismatches: {}` — framework/cc/tc
      version strings agree). content_hash level shows DRIFT: 2 of 70
      tracked upstream files changed since the last baseline update
      (baseline @ claude-copilot 70de3d3, current upstream @ 4c976f4) --
      `.claude/agents/kc.md` and `.claude/commands/knowledge-copilot.md`,
      both from an in-progress, not-yet-committed claude-copilot edit
      (frontmatter `last_updated`/`status` fields added to the `kc` agent
      and its bootstrapper command). Diagnosis: this is NOT mechanical sync
      drift eligible for a routine catch-up — both files are exactly the
      `kc` surface DEC-8 has nominated for removal (agent-eval-coverage /
      removal-review-first-pass; decisions/DEC-8-first-removal-review.md
      §6: "kc — cut anyway: ... rm claude-copilot/.claude/agents/kc.md
      and .claude/commands/knowledge-copilot.md's dependency on it").
      Syncing (or baselining) kc content now would pre-judge that ruling
      by design ("respect the relevance rule" — copilot-control-tower
      CLAUDE.md), so it is deliberately NOT synced and the content
      baseline is deliberately NOT updated (`--update-baseline` is the
      explicit "we caught up" action; not run, since we haven't). No other
      file in the 70-file tracked surface (`.claude/agents`,
      `.claude/commands`, `.claude/skills/**/SKILL.md`) drifted between
      the baseline and today, including everything landed this session
      (tc `solution` entity, 5 new golden-set eval dirs, the hook-path
      fix) — none of those touch the tracked glob patterns (evals live
      under `.claude/evals/`, the hook fix under `.claude/settings.json`,
      the Solution entity under `tools/tc/`), so there was nothing else to
      sync or diagnose. Before/after divergence: 2 files / 2 files
      (unchanged — nothing mechanical was found to close). Also fixed in
      this task, as a precondition for trusting the above: the cse-bench
      `parity` collector's `_detect_content_flag` had a latent bug (picked
      "--json" off the shared usage-summary line instead of "--content",
      because "--update-baseline"'s own help text also contains the word
      "content") that made `cse_bench.py collect --only parity` silently
      report content_check as unavailable/pass-only; fixed
      (tools/cse-bench/collectors/parity.py, regression test
      tools/cse-bench/collectors/test_parity.py) and re-verified it now
      reproduces the direct script's real drift result. Status stays
      failing: 2 items awaiting DEC-8's ruling on `kc`, not a mechanical
      gap this task can close.
    last_checked: "2026-07-14"

  - id: t7-inheritance-ladder-in-practice
    statement: "At least one real Foundation->Org->Personal chain syncs through Control Tower, entitlement by repo access, secrets outside git."
    definition_refs: []
    check: "manual — depends on Copilot Control Tower's own build program (docs/02-prd/prd.md); this program's trust panel only verifies it, does not deliver it"
    status: gated
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-2-prd.md (T7 row: 'Delivered by the Control Tower build program; tracked here as a dependency')"
    last_checked: "2026-07-12"

  - id: t8-value-transfers-beyond-author
    statement: "A person who is not the owner, using a persona task pack, completes representative tasks measurably better with the CSE than without."
    definition_refs: []
    check: "manual — pending B-14 (external pilot protocol), phase-2-prd.md P3"
    status: unchecked
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-2-prd.md (T8 row: 'Every number in existence is single-author')"
    last_checked: "2026-07-12"

  # --- Still-quotable Phase-1 / re-audit corrected numbers ----------------

  - id: delegation-rate-baseline
    statement: "Post-hook delegation rate, reported under both registered definitions: tool-share median ~40.5-40.9%; event-share median ~3.0-3.1%. The two disagree by an order of magnitude and neither is authoritative alone."
    definition_refs: [delegation_rate_tool_share, delegation_rate_event_share]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only transcripts (reads metrics.global.delegation_rate_tool_share / delegation_rate_event_share .median from output/transcripts-latest.json; supersedes the tools/cse-audit/run_probe.py invocation as the runnable check now that B-5 exists, and additionally unions the archive with live ~/.claude/projects)"
    status: passing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-5); docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html (F-5 row: 'Reproduces (40.9% tool-share vs 3.0% event-share)'); B-5 re-run 2026-07-12 via tools/cse-bench/collectors/transcripts.py, n=108 sessions (archive UNION live): tool-share median 40.9%, event-share median 3.2% — reproduces within the registered range"
    last_checked: "2026-07-12"

  - id: protocol-declaration-rate-baseline
    statement: "Post-hook protocol-declaration rate never recovered from the April 2026 pre-hook baseline (3.5%): median ~0.8-0.9% under the all-messages definition, 0.0% under the strict first-of-turn definition. CORRECTED 2026-07-14: the all-messages ('loose') median no longer reproduces at ~0.8-0.9% -- fresh collect reads 0.0% under BOTH definitions."
    definition_refs: [protocol_declaration]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only transcripts (reads metrics.global.protocol_declaration_rate_loose / protocol_declaration_rate_strict .median from output/transcripts-latest.json; supersedes the tools/cse-audit/run_probe.py invocation as the runnable check now that B-5 exists)"
    status: retired-by-deletion
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-4); docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html (F-4 row: 'Reproduces: 0.9% median, 0.0% strict (n=76)'); B-5 re-run 2026-07-12 via tools/cse-bench/collectors/transcripts.py, n=108 sessions (archive UNION live): loose median 0.86%, strict median 0.0% — reproduced within the registered range at that time. This figure was already flagged stale by RULING-AGENDA.md's DEC-2 section ('the register still cites median ~0.8-0.9%; the fresh collect says 0.0%. The registered figure no longer reproduces. A correction commit is owed either way') ahead of this correction landing. RE-RUN 2026-07-14 (this task, fresh cse_bench.py collect --only transcripts): protocol_declaration_rate_loose median 0.0 (n=25), protocol_declaration_rate_strict median 0.0 (n=17) -- the strict conjunct still reproduces exactly, but the loose conjunct does NOT: the registered ~0.8-0.9% figure no longer matches a live re-run, a materially larger deviation (complete disappearance, not a small drift) than the ~21% relative deviation that already set this register's own precedent for marking a quoted, non-reproducing figure failing (see knowledge-never-read-rate). Marked failing on reproducibility grounds at the time, consistent with that precedent -- NOT because the claim's directional thrust ('adoption never recovered from 3.5%') is any less true; if anything the fresh number is a MORE extreme instance of the same finding (0.0%, not ~0.86%, further from 3.5% than previously registered), so this is a correction to a stale precise figure, not a reversal of the underlying finding. Disclosed, not hidden: this run's transcript corpus differs in composition from the B-5 baseline run (corpus block: archive_jsonl_files 0, live_jsonl_files 294, merged 294 -- live-only on this machine right now, vs the B-5 run's archive-UNION-live n=108), so part of the n=108->n=25/17 sample-size drop reflects a smaller/different corpus snapshot, not necessarily behavior change alone; the 0.0% loose figure is reported as measured, not adjusted for this, and the corpus-composition difference is flagged here for whoever next reconciles it. RETIRED 2026-07-14 per DEC-2 (owner RULED, Option C) -- EXECUTED in claude-copilot@1b67851: the mandatory '[PROTOCOL: ...]' declaration-PREFIX obligation itself was struck from .claude/commands/protocol.md, .claude/commands/continue.md, and docs/30-operations/01-working-protocol.md (verified 2026-07-14: no 'MUST start with' prefix language remains in protocol.md) -- there is no longer a live obligation for this claim to measure adherence to. The underlying delegation/routing discipline this obligation was a proxy for is RETAINED and separately measured, passing (delegation-rate-baseline, tool-share median ~40.5-40.9%) -- DEC-2's own header is explicit that retiring the prefix obligation is not retiring routing discipline. Applied as the next serialized register-patch pass DEC-2 itself asked for."
    last_checked: "2026-07-14"

  - id: turn-definition-incompatible-with-april
    statement: "April 2026's reported '671 turns/session' figure is directly comparable to (reproducible under) this register's `turn` definition."
    definition_refs: [turn]
    check: "manual — April's original counting script does not exist; nothing to re-run"
    status: retired-by-unverifiability
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-6: probe measures median 6.0, a ~100x gap); docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html (F-6 row: 'PARTIALLY REOPENED... Turn-gap corroborated as a definitional artifact (~90-112x)'). Retired per DEC-10 and t2's own rule ('unverifiable claims are deleted') -- check field confirms April's counting script does not exist and cannot be recovered; the underlying finding (F-6, definitional turn-gap) remains on record unchanged. Applied 2026-07-14 as the next serialized register-patch pass DEC-10 itself asked for (owner RULED 2026-07-14, Option A, per DEC-10's header banner) -- this claim's id and evidence stay in the register per DEC-10 §4 ('retiring the claim does not retire the finding'), so <!-- claim-check: turn-definition-incompatible-with-april --> annotations in doc prose (backing the disclosed-irreproducible '671 turns/session' figure) remain valid: they point to a real, evidence-backed register entry regardless of its active/retired status."
    last_checked: "2026-07-14"

  - id: model-tier-opus-dominant-main-session
    statement: "Main-session tool-call/message share is Opus-dominant (92.5% of main-session assistant messages; 94.5% of main-session tool calls per the re-audit), contradicting the framework claim of 'Sonnet for ~94% of work' under every denominator tested (main-session messages, all messages, token share, and the re-audit's additional readings)."
    definition_refs: []
    check: "python3 tools/cse-audit/run_probe.py (reads the literal `model` field per assistant message — an unambiguous field, not a contested operational definition, hence no definitions: entry)"
    status: passing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-3); docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html (F-3 row: 'STRENGTHENED... Today's main-session Opus tool-call share: 94.5%')"
    last_checked: "2026-07-12"

  - id: knowledge-never-read-rate
    statement: "CORRECTED 2026-07-14: Share of Knowledge Copilot files never read, reported under both registered denominators, per this register's own deterministic (non-guessing) rename-robust matching rule: 80.8% under knowledge_md (915 files, 2026-07-12) and 88.7% under all_files (1724 files, 2026-07-12) -- these are now the reproducible baseline, superseding the reaudit's one-off manually-reconciled ~67% knowledge_md figure (see reconciliation below). The original Phase 1 headline of 95.6% remains superseded (buggy path-join + vendored-file-polluted denominator) and must not be requoted; the 67.1% figure is likewise superseded and must not be requoted as an ongoing target -- both remain on record as historical, non-reproducible data points, directionally consistent (a cold library, not a dead one)."
    definition_refs: [knowledge_read, knowledge_never_read_denominator]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only transcripts (reads metrics.global.knowledge_read.never_read.{knowledge_md,all_files}.never_read_pct from output/transcripts-latest.json; this is the automatable re-run B-5 was scoped to build)"
    status: passing
    evidence: >-
      docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-11);
      docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html
      (F-11 row: 'CORRECTED... Corrected: 67% (knowledge-md) to 88% (all files)').
      B-5 re-run 2026-07-12 via tools/cse-bench/collectors/transcripts.py (n=108
      sessions, archive UNION live, rename-robust matching per
      knowledge_never_read_denominator): all_files never-read 88.7% (denominator
      1724) — reproduces the registered ~88% closely (0.8% relative). knowledge_md
      never-read 80.8% (denominator 915, vs the registered 906 — file count has
      grown since this claim was registered) did NOT reproduce the registered
      ~67% (~21% relative deviation) and was marked failing on that basis
      (last_checked 2026-07-12), flagged as needing reconciliation, out of scope
      for that task. RECONCILED 2026-07-14 (this task; full mechanism recorded in
      definitions.knowledge_never_read_denominator.reconciliation_2026_07_14):
      the 67.1% figure was a ONE-OFF MANUAL reconciliation of the same
      stale-path-join problem that inflated Phase 1's 95.6% headline -- the
      reaudit report's own "Probe blind spots found but not fixed" section
      states the underlying join mechanism was never durably fixed ("has no
      rename tracking and will silently re-break on the next repo reorg"), only
      patched by hand for that one measurement. Proven, not asserted: direct
      inspection of the live knowledge-copilot tree found two files this
      register's own matcher correctly reports unmatched because they underwent
      one-to-many directory splits since the original count -- 01-company/
      10-org-and-team.md is now the directory 01-company/10-org-and-team/
      {00-roster.md, people/}, and 01-company/08-case-studies/beacon.md is now
      01-company/08-case-studies/beacon/{00-case-study.md, 00-index.md,
      reference/} -- plus a basename-changing rename in git history
      (01-company/08-case-studies/uber-freight/00-case-study.md ->
      01-company/12-pursuits/uber-freight/00-pursuit.md, commit history since
      2026-05-01) that no basename-key match, current or numeric-prefix-stripped
      fallback, can ever land. This is the register's own original hypothesis
      (matching conservatism, concentrated in the narrower denominator because a
      1-to-many split changes the *.md inventory's shape while barely moving the
      much larger all_files pool) CONFIRMED with direct filesystem and git
      evidence, not a collector bug (refusing to guess at ambiguous/
      basename-changing renames is the correct, deliberate behavior; guessing
      would be the actual bug) and not an ill-specified original definition
      (906 knowledge_md files, both read channels, is precise) -- it is a
      figure that is not reproducible by any mechanical, non-guessing re-run,
      the same class of problem as definitions.turn's unrecoverable April
      figure. Per that precedent, re-baselined rather than chased: 80.8%/88.7%
      (both 2026-07-12) are the new reproducible baseline this claim's
      statement now quotes, and the check reproduces them exactly, hence
      PASSING per this claim's own truth condition (the statement is a
      description of what the register's deterministic check measures, not a
      bar the corpus must clear -- a high never-read rate is itself a finding
      about the knowledge corpus being under-consumed, not a bug being
      papered over; that finding is unchanged and stated plainly above).
      Also confirms definitions.knowledge_never_read_denominator.variants.
      all_files's prior OPEN GAP (whether the exclusion set matches the
      reaudit's) as closed: 0.8% relative deviation is a clean
      machine-vs-machine reproduction.
    last_checked: "2026-07-14"

  - id: cli-command-liveness-breakdown
    statement: "CLI Copilot's real surface is 439 leaf commands across 22 service groups (Phase 1's 208 was a ~2x undercount), classified: Alive 103 (23%), Occasional-signal 142 (32%), Never-invoked 133 (30%), Local-utility 61 (14%)."
    definition_refs: [cli_liveness_classes, cli_invocation]
    check: "manual — see docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html CLI Copilot panel; automatable re-run pending C-1 (usage ledger) landing"
    status: passing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-16); docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html (CLI Copilot panel, liveness stack chart)"
    last_checked: "2026-07-12"

  - id: cli-copilot-test-suite-verified
    statement: "CLI Copilot's test suite passes: 1,588 tests pass, 16 deselected, 53% coverage, CI green (ruff + format-check + pytest, py3.11/3.12) on push-to-main and PRs. UPDATED 2026-07-14: 1,808 tests pass, 16 deselected, 3 xfailed, 55% coverage, CI green on push-to-main (run 29333615840, both matrix jobs, all steps incl. Ruff format check)."
    definition_refs: []
    check: "cd /Users/pabs/Sites/COPILOT/cli-copilot && CC=/usr/bin/cc PATH=/usr/bin:$PATH uv run --extra dev pytest tests/ -q --cov=copilot_cli --cov-report=term-missing -m \"not e2e\" (the CI-matching invocation, cli-copilot/.github/workflows/ci.yml; .venv is dead, uv run rebuilds a working env; /Volumes/Dev is not mounted on this machine, real path is /Users/pabs/Sites/COPILOT); cross-checked live via gh run list -R Everyone-Needs-A-Copilot/cli-copilot --branch main / gh run view <run-id> --job=<job-id>"
    status: passing
    evidence: >-
      Was failing (last_checked 2026-07-14, same day): HEAD's own live CI
      (run 29280957243, commit 1695731 "Merge branch
      'feat/r11-r12-soul-remediation'") had both matrix jobs (py3.11,
      py3.12) FAIL at the "Ruff format check" step -- pytest itself was
      healthy (1808 passed, 16 deselected, 3 xfailed, 55% coverage, past
      the pre-registered 1,588/53%) but the claim's own "CI green on
      push-to-main" conjunct was false. Root cause: this program's own
      R-11 commit (21e7c61, cli-soul-conformance remediation) merged
      tests/test_services/test_infisical.py unformatted. FIXED same day:
      cli-copilot commit be895d6 ("fix(ci): ruff-format
      tests/test_services/test_infisical.py"). RE-RUN 2026-07-14 (this
      task; cli-copilot main @ 78617b3, be895d6 included): local `uv run
      --extra dev pytest tests/ -q --cov=copilot_cli
      --cov-report=term-missing -m "not e2e"` -> 1808 passed, 16
      deselected, 3 xfailed, 55% coverage (TOTAL line), 119.44s; `uv run
      --extra dev ruff check .` -> "All checks passed!"; `uv run --extra
      dev ruff format --check .` -> "199 files already formatted" (clean,
      the prior violation is gone). Cross-checked against LIVE CI, not
      just local repro: `gh run view 29333615840` (be895d6's own
      push-to-main run) shows BOTH matrix jobs (py3.11, py3.12) green
      end-to-end -- Set up job / checkout / Set up Python / Install
      dependencies / Ruff check / Ruff format check / Run tests / Complete
      job all passed; job log confirms "1808 passed, 16 deselected, 3
      xfailed" and "TOTAL ... 55%". A later push (run 29334163178, commit
      78617b3) is also green, confirming this is stable, not a one-off.
      Per the claim's own conjunction, both conjuncts now hold: pytest
      passes (and exceeds the pre-registered baseline), and CI is green on
      push-to-main. Marked passing, plainly.
    last_checked: "2026-07-14"

  - id: enforcement-hook-wiring-ratio
    statement: "Exactly 1 of 27 repos carrying .claude/agents/ (26 real directories plus 1 symlink, research-copilot -> knowledge-copilot) registers a PreToolUse hook in .claude/settings.json."
    definition_refs: []
    check: "find /Volumes/Dev/Sites/COPILOT -maxdepth 3 -name settings.json -path '*/.claude/*' -exec grep -l PreToolUse {} \\; (external tree scan, informational)"
    status: passing
    evidence: "docs/40-initiatives/01-cse-auditability/phases/phase-1-findings.md (F-2); docs/40-initiatives/01-cse-auditability/phases/phase-1-reaudit-report.html (F-2 row: 'UPHELD... exactly one repo registers PreToolUse')"
    last_checked: "2026-07-12"

  - id: delegation-hook-matcher-scope
    statement: "claude-copilot's delegation-enforcement PreToolUse hook matcher is Bash-only (excludes Read/Edit/Agent), unchanged since the 2026-04-22 same-day deadlock-resolution commit (23c02c0) that narrowed it from 'Bash|Read|Edit|Agent' to 'Bash'."
    definition_refs: []
    check: "git -C /Volumes/Dev/Sites/COPILOT/claude-copilot show HEAD:.claude/settings.json (external repo, informational)"
    status: failing
    evidence: "C-6 (TASK-106) fixed the deadlock and re-widened the matcher to 'Bash|Read|Edit|Agent': claude-copilot commit 77f5cdb0 (subagent exemption via agent_type, CC_HOOK_ENFORCE kill switch, fail-open hardening; root cause in claude-copilot docs/10-architecture/06-hook-deadlock-root-cause-2026-07.md). This statement is now FALSE — the desired direction. Rollout beyond claude-copilot remains staged (C-3); see enforcement-hook-wiring-ratio, still true."
    last_checked: "2026-07-12"

  - id: knowledge-factual-accuracy-delta
    statement: "With dossier content in context, model accuracy on private product facts is >=90%; without it, accuracy is ~0% with a high UNKNOWN rate (contamination immunity). First run: with=98.0% (48/49), without=0.0% (49/49 UNKNOWN), delta=+98.0pp, model=sonnet."
    definition_refs: []
    check: "cd tools/cse-bench/benches/knowledge_qa && python3 run.py (49-question bank, 2 dossiers; output/bench_knowledge_qa-latest.json)"
    status: passing
    evidence: "tools/cse-bench/output/bench_knowledge_qa-latest.json (run 20260712T175029Z; per-call audit in output/bench_knowledge_qa-runs/)"
    last_checked: "2026-07-12"

  - id: voice-conformance-deltas
    statement: "Knowledge voice content reduces linter violations vs bare prompting (knowledge_vs_bare < 0), BUT compiled rules-in-prompt outperform raw repo-as-context on conformance (knowledge_vs_rules >= 0) — the effective payload is the distilled rubric, not the prose. First run: bare=2.77, rules=0.10, knowledge=0.48 violations/100w."
    definition_refs: []
    check: "cd tools/cse-bench/benches/voice_lint && python3 run.py (6 tasks x 3 arms; output/bench_voice_lint-latest.json)"
    status: passing
    evidence: "tools/cse-bench/output/bench_voice_lint-latest.json (clean 18/18 run 20260712T175434Z; direction replicated across 2 runs); rubric: benches/voice_lint/rules.yaml (44 rules, source-cited)"
    last_checked: "2026-07-12"

  - id: cli-mcp-net-token-advantage
    statement: "CLI Copilot's token advantage over its two MCP twins is positive. FALSIFIED as stated: net advantage is negative for both twins under probe-everything grammar cost (crm -780, db -1894 tokens) and positive only for crm (+1238) when the shipped prose docs are provided up front. Live tools/list handshakes, tiktoken o200k_base."
    definition_refs: []
    check: "cd tools/cse-bench && python3 benches/mcp_twin/run.py (output/bench_mcp_twin-latest.json)"
    status: failing
    evidence: "tools/cse-bench/output/bench_mcp_twin-latest.json (run 20260712T175500Z). Product implication: ship the per-service prose (docs/services/*.md) into agent context; F-17's 10% deferral threshold remains changelog-sourced/unverified."
    last_checked: "2026-07-12"

  - id: agent-eval-coverage
    statement: "Every framework agent has a golden-set eval with a passing score. UPDATED 2026-07-13: 6 of 16 agents covered (qa, me, ta, doc, sd, uxd -- each 10/10 pass, deterministic LocalPythonRunner, threshold 0.8); coverage_ratio 37.5% (was 1 of 16, 6.25%)."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only evals (runs cc eval --agent <agent> --json in claude-copilot for EVERY agent under <claude-copilot>/.claude/evals/ with a golden set -- the collector already discovers this list dynamically from the evals directory, not hardcoded to qa; this check-field description previously undersold that as qa-only and is corrected here to match the collector's actual behavior, collectors/evals.py)"
    status: failing
    evidence: "tools/cse-bench/output/evals-latest.json (re-run 2026-07-13: agents_with_evals [doc, me, qa, sd, ta, uxd], agents_total 16, coverage_ratio 0.375; all six 10/10 pass, overall_pass true) -- all six covered suites pass; the CLAIM still fails on coverage (10 of 16 agents remain unmeasured: the claim's own statement requires EVERY framework agent, not a majority). Expansion tracked as C-5 (TASK-105). Evidence note: narrowing this claim's statement (e.g. to 'every agent WITH a golden set passes it') is an owner option, not applied unilaterally here -- the statement as pre-registered says 'every framework agent,' and this program does not silently narrow a claim to match a partial result (V-2)."
    last_checked: "2026-07-13"

  # --- S-series: SOUL-aligned claims (2026-07-12 revision; phase-2-prd.md §2.5) ---

  - id: framework-resume-value
    statement: "The state Memory/Task Copilot persist makes cold resume accurate: with-state resume correctness 100% (task, completed step, next action) vs 0% without, at +3.9% token overhead (the ~654-token state block). The framework's SOUL §1 job, measured."
    definition_refs: []
    check: "cd tools/cse-bench/benches/resume_cost && ./build_state.sh && python3 run.py"
    status: passing
    evidence: "tools/cse-bench/output/bench_resume_cost-latest.json (run 20260713T001935Z; 3 reps/arm, contamination-controlled)"
    last_checked: "2026-07-12"

  - id: framework-externalization-94pct
    statement: "Externalized work products yield '~94% less context' vs inlining (claude-copilot SOUL §3 Principle 3, README panel). FALSIFIED AND INVERTED under population measurement. README claim struck 2026-07-12; SOUL §3 line flagged for owner-ratified correction per SOUL §10."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only framework_soul"
    status: retired-by-ratification
    evidence: "REFRESHED 2026-07-13 (QA WP-28): tools/cse-bench/output/framework_soul-latest.json, fresh collect this session -- wp_tokens median 353 (n=126 work products with inline content, of 137 total scanned across 10 repos' */.copilot/tasks.db), agent_return_tokens median 893 (n=124 subagent final returns); savings_ratio_median -1.53 (-153%) -- agent returns are ~2.5x LARGER than the work products they externalize, same falsified-and-inverted direction as the prior reading, still failing at the time. The prior evidence (n=1099 WPs, n=658-token median return) is SUPERSEDED, not because the direction changed but because the corpus it was measured against was wrong: both this collector's work-product glob and its transcript corpus root resolve through collectors/paths.py's resolve_copilot_root(), which (per this session's own §2 fix) previously fell back silently to a path that does not exist on this machine. Now that both known COPILOT root candidates are tried, the collector sees this machine's REAL, smaller tree (137 WPs, 124 returns) instead of whatever larger/stale corpus the prior number reflects. RETIRED 2026-07-14 per DEC-3 (owner RULED, Option B) -- EXECUTED in claude-copilot@e1e1501: SOUL.md's Changelog now shows v1.1 'Ratified (DEC-3, Option B). Struck the falsified 94% less context figure from §3/§5/§7 (three sites)... Rewrote all three to state the mechanism ... and defer the number to the claims register' -- verified directly 2026-07-14 (SOUL.md §3/§5/§7 no longer hard-code any externalization percentage; they cite this claim id by name instead and point to the register for the current number). Applied as the next serialized register-patch pass DEC-3 itself asked for. The underlying finding (agent returns are ~2.5x larger than the work-product content they externalize -- the mechanism's number, not its existence, was falsified) is unchanged and stays on record here; only this claim's bookkeeping status changes, per DEC-3 §4 Option A's own reasoning ('the claim can be marked retired-by-ratification instead of sitting failing indefinitely')."
    last_checked: "2026-07-14"

  - id: framework-agent-frugality
    statement: "Agents return ~100 tokens to the main session (claude-copilot SOUL §6 non-negotiable). UPDATED 2026-07-13 (QA WP-28 refresh): median return 893 tokens (was 658 -- WORSE, further from the ~100-token bar), p90 3,474 (was 2,749); 95.97% of returns exceed 300 tokens (was 86.2%). CORRECTION 2026-07-14: agent return-contracts tightened (see evidence) -- claim deliberately NOT flipped, still failing, until a post-change collect exists; see evidence for the pre-change baseline and why."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only framework_soul"
    status: failing
    evidence: >-
      REFRESHED 2026-07-13 (QA WP-28): tools/cse-bench/output/framework_soul-latest.json,
      fresh collect this session -- agent_frugality block: n=124 subagent returns
      (merged transcript corpus, now visible in full after the collectors/paths.py
      root-resolution fix, TASK-128/719cb6e, previously silently blind to this
      machine's real tree), median 893.0, p90 3473.8, pct_over_threshold 95.97%.
      This is a genuine regression vs the 658-token/86.2% figure this claim
      previously quoted, not a methodology change -- quoted plainly, not
      softened. The claim was already failing and remains failing; the gap
      from the SOUL §6 bar widened.

      CORRECTION 2026-07-14 (no owner ruling needed -- DEC-1's enforce-vs-amend
      question and the staged w5 patches remain untouched and ladder-gated;
      this is additive instruction work only). PRE-CHANGE BASELINE, from the
      fresh tools/cse-bench/output/framework_soul-latest.json (generated_at
      2026-07-14T13:14:54Z, NOT re-derived by this task) -- agent_frugality:
      n=145 (of 147 subagent files, 2 with no final text), median 932.0, p90
      3446.4, pct_over_threshold 96.55%; by_agent_type (roster that survives
      the removal review, cpa/cs/kc excluded per the relevance rule): me n=51
      median 853, ta n=13 median 1060, qa n=21 median 1132, doc n=12 median
      456.5, sd n=5 median 3786, uxd n=3 median 5089, sec n=5 median 3556, do
      n=14 median 603, cw n=2 median 1229.5, uid n=3 median 770, uids n=1
      median 4118, cco n=13 median 1054, ind n=1 median 1938 -- the
      "design-chain agents 3.5k-5k" pattern already on record is confirmed
      (sd/uxd/sec/uids cluster 3.5k-5.1k median). DIAGNOSIS, from reading
      real returns (subagents/agent-*.jsonl under ~/.claude/projects, the
      corpus this collector consumes): all 13 surviving agents' definitions
      (claude-copilot .claude/agents/*.md) already carried an explicit
      "Return ONLY (~100 tokens)" structured template before this
      correction -- the contract was not silently absent. The actual
      mechanism, proven by sampling: only 35 of 153 sampled subagent
      invocations (22.9%) even attempt `tc wp store`; a live sd sample
      (5568 tokens, the largest sd return) opens with "No task ID was
      provided and `tc` isn't on PATH, so I'll deliver the experience
      design directly" -- i.e. when no work product gets stored, the
      pre-existing contract had no fallback branch (its own template
      assumes a WP already exists: "WP: WP-xxx"), so the agent free-styled
      and inlined the full deliverable instead of the short summary. FIX
      APPLIED 2026-07-14 (claude-copilot commit 65f9bf7, branch
      docs/40-initiatives-migration, pushed): added the missing fallback
      branch to all 13 agents' Output Format sections ("if `tc wp store`
      fails, is unavailable, or no task ID exists: still return ONLY the
      block above with `WP: none (<reason>)` -- never substitute the full
      deliverable for it") plus a matching Core Behaviors "Never" bullet,
      for me/ta/qa/doc/sd/uxd/sec/do/cw/uid/uids/cco/ind. Explicitly
      excluded cpa/cs/kc (DEC-8 deletion nominees; a concurrent session was
      observed deleting cpa.md/cs.md in this same working tree while this
      correction was made). SOUL.md and .claude/hooks/ were NOT touched by
      this correction (a concurrent, unrelated session was separately
      amending SOUL.md to v1.1/v1.2 in the same working tree during this
      task; verified via `git diff SOUL.md` that none of those lines are
      this correction's). STATUS DELIBERATELY NOT FLIPPED: this is an
      untested contract change, not a re-measurement -- the 932-token/
      96.55% figures above are the PRE-change baseline, not a post-change
      result, and per this register's own check (a `framework_soul`
      collect) can only be verified by running new agent sessions under
      the amended contracts and re-collecting. Marked failing, honestly,
      until that re-run exists; a high pre-change baseline is the
      finding, not a bug being hidden by this note.
    last_checked: "2026-07-14"

  - id: framework-qa-gate-adherence
    statement: "No implementation ships without a qa pass carrying an ARTIFACT: marker (claude-copilot SOUL §6). UPDATED 2026-07-13 (QA WP-28 refresh): 66.67% of qa returns carry the marker (10/15, was 75.9%/41-of-54 -- WORSE), 3 sessions delegated to me with no qa activity at all (of 9 sessions with me-or-qa activity); the qa-gate hook state file now EXISTS at the primary path (claude-copilot/.claude/hooks/state/qa-gate.json) but is empty (`{}`, 0 sessions tracked, 0 history events) -- the hook has been installed but has not yet actually fired/gated anything on this machine, a materially different (and still concerning) state from 'has never existed.'"
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only framework_soul"
    status: failing
    evidence: "REFRESHED 2026-07-13 (QA WP-28): tools/cse-bench/output/framework_soul-latest.json, fresh collect this session -- qa_gate_adherence block: artifact_marker_rate 0.6667 (n_qa_returns 15, with marker 10), n_sessions_with_me_activity_but_no_qa_activity 3 (of n_sessions_with_me_or_qa_activity 9), hook_state.primary_exists true (was false), hook_state.primary_summary all-zero (installed, unfired). This is a genuine regression vs the 75.9%/41-of-54 figure this claim previously quoted, not a methodology change -- quoted plainly. The claim was already failing and remains failing."
    last_checked: "2026-07-13"

  - id: knowledge-crosslink-integrity
    statement: "Knowledge is cross-linked where decisions depend on other documents (knowledge-copilot SOUL quality bar). CORRECTED 2026-07-13: 70.99% of relative links resolve (186/262, 76 broken), up from 52.5% (137/261, 124 broken), after knowledge-copilot commit 5a07dcfe (R-6) repaired the remaining real breaks beyond the systematic wrong-depth batch. The 76 that remain are BASELINED (scripts/crosslinks-baseline.json) as non-decision-dependent: 68 pedagogical example syntax inside documentation-teaching SKILL.md content (never meant to resolve) and 8 framework-template files (CLAUDE.md, CLAUDE_REFERENCE.md, .claude/commands/reflect.md) synced verbatim from claude-copilot. A pre-commit regression gate (scripts/check-crosslinks.py) now fails any NEW broken link outside this baseline."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only knowledge_soul; or knowledge-copilot's own gate: cd knowledge-copilot && python3 scripts/check-crosslinks.py"
    status: passing
    evidence: "knowledge-copilot commit 5a07dcfe (R-6, scripts/check-crosslinks.py + scripts/crosslinks-baseline.json); tools/cse-bench/output/knowledge_soul-latest.json (crosslink_integrity block, re-run 2026-07-13: n_links_checked 262, n_broken 76, pct_resolving 70.99 -- reproduces the commit's own verified numbers). Flipped failing->passing per this claim's own truth condition: the SOUL bar concerns cross-links between DECISIONS, and every remaining broken link is now baselined as non-decision content (pedagogical example / synced template), not a real cross-reference gap; a mechanical gate prevents regression back into real breakage."
    last_checked: "2026-07-13"

  - id: knowledge-staleness-honesty
    statement: "The repo never becomes a stale content dump; documents carry honest freshness status (knowledge-copilot SOUL). CORRECTED 2026-07-13: last_updated + status frontmatter coverage is 100% (764/764 non-archive files), up from 0% literal last_updated / 1.3% any-freshness-key, after knowledge-copilot commit c6a6b492 (R-7) backfilled both keys on every active file missing them -- last_updated always derived from that file's own git history (git log -1, never invented), status defaulting to 'active' (matching the ~90% pre-existing convention) without overwriting any pre-existing value (2 'draft' + 1 'proposed' file kept as-is)."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only knowledge_soul && python3 check_freshness_accuracy.py --json   # 2nd command added QA WP-47: the collector only measures KEY PRESENCE (coverage), never DATE ACCURACY -- this spot-checks every last_updated value against that file's own git commit-date history"
    status: passing
    evidence: "knowledge-copilot commit c6a6b492 (R-7, scripts/backfill-freshness.py); tools/cse-bench/output/knowledge_soul-latest.json (staleness_archive_honesty.frontmatter_freshness block, re-run 2026-07-13: n_scope_files 764, any_frontmatter_pct 100.0, freshness_key_pct 100.0, literal_last_updated_key_pct 100.0). STRENGTHENED 2026-07-14 (QA WP-47): that coverage check only proves the KEY is present, not that the DATE is honest -- a fabricated date would score 100% under it alone. Built tools/cse-bench/check_freshness_accuracy.py, which for every one of the 761 files carrying last_updated asks git for every date that file was ever actually touched (`git log --follow`) and checks the frontmatter date against that set (membership, not 'matches the latest commit' -- the latest commit is now usually c6a6b492's own mechanical backfill touch, or a later bulk touch, which correctly postdates an honest last_updated without that being dishonest; see the script's own docstring). Result: 753/761 (98.95%) verified -- the recorded date is a real date drawn from that file's own history, not invented. 8/761 (1.05%) NOT found in git history (all under 03-ai-enabling/01-skills/17-software-factory/, all last_updated=2026-03-27 against a 2-commit history of 2026-02-25/2026-07-13 only): these 8 already carried a last_updated value BEFORE the R-7 backfill ran (R-7 only fills MISSING keys, confirmed by re-reading scripts/backfill-freshness.py's own description -- it never overwrites an existing value), so they were not derived by this claim's own mechanism at all; this repo's git history shows a batched 'vault backup' commit pattern (Obsidian-vault-style bulk commits), so a hand-set date from the authoring tool genuinely predating its own bulk commit is plausible and not proven fabricated -- flagged here as a disclosed, unverifiable residual, not swept under the 100%-coverage number. Status STAYS passing: the claim's truth condition ('documents carry honest freshness status') is now verified by a real mechanism, not just a presence count, and holds for 98.95% of the corpus with a small, named, disclosed exception rather than a blanket, uninspected 100%."
    last_checked: "2026-07-14"

  - id: knowledge-voice-self-conformance
    statement: "The company-voice reference content passes its own voice rules (anti-pattern: 'rewriting company voice into generic consultant language'). CORRECTED 2026-07-13: the tone-of-voice document ITSELF (01-company/01-brand/02-tone-of-voice.md) now lints at 0.0 violations/100w (was 2.74 -- nearly the unguided-output rate of 2.77), Flesch-Kincaid grade 6.5 (was 9.9, out of the 6-8 band; now in band), after knowledge-copilot commit b1f99719 (R-8) rewrote the doc and the cw extension into the distilled-rules form the voice_lint bench proved beats prose -- the doc now practices what it preaches instead of re-typing its own denylist in violation of it."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only knowledge_soul"
    status: passing
    evidence: "knowledge-copilot commit b1f99719 (R-8); tools/cse-bench/output/knowledge_soul-latest.json (voice_preservation.per_file block, re-run 2026-07-13: 01-company/01-brand/02-tone-of-voice.md total_violations 0, total_violations_per_100_words 0.0 -- verified by this claim's own voice_lint linter, benches/voice_lint/lint.py, not a self-report). Flipped failing->passing per this claim's own truth condition (the reference content now passes its own voice rules). Note: the broader 01-company/ aggregate (90 files) still lints at 1.861 violations/100w -- this claim is specifically about the tone-of-voice reference document, not every company-voice file; the aggregate figure is unchanged by R-8 and is not what this claim's statement scopes to."
    last_checked: "2026-07-13"

  - id: knowledge-registry-completeness
    statement: "The ecosystem registry tracks the ecosystem (anti-pattern: registry staleness). CORRECTED 2026-07-14: the two named 2026-07-13 defects are FIXED -- ECOSYSTEM.md now lists both copilot-control-tower and knowledge-copilot's own local path (knowledge-copilot commit 5f729946) -- but the registry still does not fully track the ecosystem: 10 top-level dirs remain unmentioned/uncovered (down from 12). Forward-resolving paths improved to 13/19 on this machine."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only knowledge_soul"
    status: failing
    evidence: "FIXED 2026-07-14: knowledge-copilot commit 5f729946 ('Fix ECOSYSTEM.md registry: add missing rows, fix hardcoded mount') added Local-path rows for copilot-control-tower and knowledge-copilot itself, and replaced the hardcoded /Volumes/Dev/Sites/COPILOT mount (verified NOT mounted on this machine -- test -d /Volumes/Dev fails) with the truthful /Users/pabs/Sites/COPILOT root throughout. RE-RUN 2026-07-14 (tools/cse-bench/output/knowledge_soul-latest.json, registry_integrity block, generated fresh this session): forward_check 13/19 resolving (up from 11/17 pre-fix; the 6 still-broken paths -- product-creation-copilot, n8n-copilot, the-collective, project-copilot, _archive/everyone-needs-knowledge-management, transformation -- were verified with `test -d` to be genuinely uncloned on THIS machine, a machine-inventory artifact, not a registry defect); reverse_check n_uncovered 10/23 (down from 12/23) -- copilot-control-tower and knowledge-copilot no longer appear in either uncovered list, confirming both named 2026-07-13 defects are closed. 10 top-level dirs remain genuinely uncovered: 5 unmentioned anywhere (THD, crm-automation-copilot, flow, prompt-copilot, sow-copilot) and 5 named-but-uncovered (admin-server, docs, drip-copilot, everyone-needs-knowledge-management, shared-docs). Status STAYS failing per the claim's own truth condition: 'the registry tracks the ecosystem' remains false while 10 of 23 top-level directories are still unmentioned or uncovered -- fixing the two specific defects this claim was CORRECTED to name on 2026-07-13 closes the substantive item this program was tracking, but does not make the broader statement true. This is a genuine partial fix, stated plainly as such, not silvered over."
    last_checked: "2026-07-14"

  - id: cli-soul-conformance
    statement: "Every CLI service meets the SOUL §6 quality bar (health, test file, CopilotError, lazy import, config documented, docs entry). UPDATED 2026-07-13: 135/138 scorecard cases pass (was 125/138); 3 remaining named gaps tracked as strict-xfail, all scoped to fireflies/reddit (fireflies: FIREFLIES_API_KEY undocumented in .env.example + no docs/services entry; reddit: REDDIT_CLIENT_ID/SECRET/USER_AGENT undocumented). The bar is mechanically enforced as a test."
    definition_refs: []
    check: "cd cli-copilot && CC=/usr/bin/cc PATH=/usr/bin:$PATH uv run --extra dev pytest tests/test_soul_conformance.py -q --tb=no -rxX; or: python3 cse_bench.py collect --only cli_soul (repo root resolved via collectors/paths.py resolve_copilot_root() -- /Volumes/Dev/Sites/COPILOT/cli-copilot on the owner's primary machine, /Users/pabs/Sites/COPILOT/cli-copilot on a secondary machine where /Volumes/Dev is unmounted)"
    status: failing
    evidence: "cli-copilot commits 21e7c61 (R-11, close kept-service conformance gaps) + 4f90d7b (R-12, fix residual service-count untruths in CLAUDE.md/overview docs), merged to cli-copilot main 2026-07-13; re-run post-merge on main: `pytest tests/test_soul_conformance.py` -> 135 passed, 3 xfailed; full suite `pytest -q` -> 1808 passed, 16 skipped, 3 xfailed, ruff clean. Status STAYS failing: the claim's own truth condition is EVERY CLI service meeting the bar, and 3 named cases still genuinely fail (xfail is a tracked, expected failure, not a pass) -- fireflies and reddit are both structurally-dead, credential-less services already flagged as removal candidates by DEC-5 (TASK-122, R-13); the gaps close either by configuring them for real or by the owner's R-13 cut ruling, whichever comes first. Quoting xfail as passing would misrepresent a tracked violation as resolved."
    last_checked: "2026-07-13"

  - id: removal-review-first-pass
    statement: "The first mechanical removal review (W-6) traces every claude-copilot agent, CLI Copilot service group, knowledge-copilot top-level area, and .claude/skills leaf skill to usage evidence + outcome-bar trace per the registered value_density definition, and produces a mechanical nomination list (surface + evidence) to the owner's delete-or-defend queue -- nominations only, no deletions, per the removal rule (phase-4-outcome-program-prd.md §0.5). First run: 4 nominations of 75 surfaces traced (16 agents, 22 CLI service groups, 10 knowledge areas, 37 skills) -- agents cpa/cs/kc (0 subagent invocations, no golden-set eval) and knowledge area 04-shared-systems (68.8% of its 16 files orphaned). 0 CLI-service and 0 skill nominations this review -- both surfaces' usage_evidence is null ecosystem-wide (no event ledger), and the nomination_rule never nominates on a null signal alone."
    definition_refs: [value_density]
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only value_density && python3 -c \"import json; d=json.load(open('output/value_density-latest.json')); print(d['metrics']['nominations'])\""
    status: passing
    evidence: "tools/cse-bench/output/value_density-latest.json (nominations block); docs/40-initiatives/01-cse-auditability/decisions/DEC-8-first-removal-review.md (the owner-facing memo)"
    last_checked: "2026-07-13"

  - id: hook-deadlock-fix-verified
    statement: "claude-copilot's PreToolUse force-delegate/QA-gate hooks fire correctly on Read/Edit/Agent post-fix (TASK-106/C-6): the widened matcher (Bash|Read|Edit|Agent) is live, the shared-session_id subagent-exemption fix is in place, and the hook's own replay test suite passes (48 of 49 assertions; the sole failure is a pre-existing, unrelated <50ms timing flake, not a regression from this fix)."
    definition_refs: []
    check: "cd claude-copilot && bash tests/hooks/test-pretool-check.sh; grep -A2 '\"PreToolUse\"' .claude/settings.json"
    status: passing
    evidence: "claude-copilot commit 77f5cdb0 (TASK-106/C-6: subagent exemption via agent_type, CC_HOOK_ENFORCE kill switch, fail-open hardening); docs/10-architecture/06-hook-deadlock-root-cause-2026-07.md (root cause, the fix, the 48/49 replay-suite result, and a live smoke test against a real throwaway claude -p session). Live .claude/settings.json verified 2026-07-14: PreToolUse matcher = 'Bash|Read|Edit|Agent'. Closes F-18 instance #1 (phase-1-findings.md F-18 table row 1, 'PreToolUse matcher | never fires on Read/Edit/Agent (F-1)') -- the artifact (the matcher plus the force-delegate/QA-gate hooks it feeds) now has a real, tested, live mechanism behind it, annotated in-doc via <!-- claim-check: hook-deadlock-fix-verified --> at the replay-suite result line. Rollout beyond claude-copilot itself remains staged (C-3, per the root-cause doc's own 'Rollout Readiness' section) -- see enforcement-hook-wiring-ratio, unaffected by this claim and still true (1 of 27 repos)."
    last_checked: "2026-07-14"

  - id: knowledge-extension-install-mechanism
    statement: "Knowledge Copilot's agent extensions (cw/do/ind/sd/uxd) install into a consuming project's .claude/agents/*.md files via a real, tested sync-time installer (claude-copilot scripts/knowledge-sync/install-extensions.py, invoked from sync-knowledge.sh) -- not by Claude Code 'loading them automatically' at runtime, which is not a real injection point Claude Code offers."
    definition_refs: []
    check: "cd claude-copilot && python3 scripts/knowledge-sync/install-extensions.py --help && bash scripts/knowledge-sync/test-knowledge-sync.sh"
    status: passing
    evidence: "claude-copilot scripts/knowledge-sync/install-extensions.py (fenced <!-- kc-extension:begin/:end --> marker installer; idempotent re-run via a sha256 hash check; --remove uninstall path; extension vs override manifest types). scripts/knowledge-sync/test-knowledge-sync.sh Tests 8-9 verify install-extensions.py exists/compiles/--help works AND that sync-knowledge.sh actually invokes it (grep -q wiring check) -- verified 2026-07-14 by direct inspection of both files. Closes F-18 instance #3 (phase-1-findings.md F-9: 'Extensions load automatically... Nothing loads them'). knowledge-copilot's PURPOSE.md line ('Reduced context building -- Extensions install into consuming agents at sync time...') already describes this real mechanism accurately (not a wording change made by this task -- found already corrected during investigation, presumably alongside the mechanism's own build); this task added the <!-- claim-check: knowledge-extension-install-mechanism --> annotation linking that sentence to this entry."
    last_checked: "2026-07-14"

  - id: knowledge-glossary-manifest-resolves
    statement: "knowledge-copilot's knowledge-manifest.json 'glossary' entry resolves to a file that exists on disk."
    definition_refs: []
    check: "cd knowledge-copilot && python3 -c \"import json, pathlib; d = json.load(open('knowledge-manifest.json')); p = pathlib.Path(d['glossary']); print(p, p.exists())\""
    status: passing
    evidence: "Verified 2026-07-14: knowledge-manifest.json's 'glossary' key reads '03-ai-enabling/03-operations/04-shared-glossary.md', which exists on disk. Closes F-18 instance #4 (phase-1-findings.md F-10: the original broken target was '...operations/12-shared-glossary.md', which did not exist -- the one dead reference of 228 the original finding named). PURPOSE.md's prose claim ('Proper terminology -- Glossary ensures correct language') is annotated to this claim (<!-- claim-check: knowledge-glossary-manifest-resolves -->) rather than reworded, since the manifest fix (not a text change) is what makes the sentence true today; a future re-break of the manifest path would make the annotation dangling-in-spirit even though the sweep only checks annotation PRESENCE, not the underlying check's live result -- re-running this claim's own check command is still required to catch that, same as every other registered claim."
    last_checked: "2026-07-14"

  - id: task-copilot-mcp-dist-exists
    statement: "The task-copilot MCP server's built entrypoint (~/.claude/copilot/mcp-servers/task-copilot/dist/index.js), referenced by knowledge-copilot's (and sibling repos') .mcp.json, exists on disk."
    definition_refs: []
    check: "test -f ~/.claude/copilot/mcp-servers/task-copilot/dist/index.js"
    status: passing
    evidence: "Verified 2026-07-14: /Users/pabs/.claude/copilot/mcp-servers/task-copilot/dist/index.js exists (66,120 bytes), alongside dist/database.js, dist/http-server.js, dist/types.js, and a populated node_modules/ -- the server is genuinely built, not a stub. Closes F-18 instance #5 (phase-1-findings.md F-18 table row 5, 'task-copilot MCP config (2 repos) | dist/index.js doesn't exist'). No product doc in claim_sweep.py's SCAN_TARGETS asserts this in prose -- the original artifact was a config file (.mcp.json), not a doc sentence -- so this claim is registered directly rather than doc-annotated; see the claim_sweep definition's precision_caveat for why config-file artifacts outside SCAN_TARGETS are invisible to the sweep itself even though they are still trackable in this register."
    last_checked: "2026-07-14"

  - id: cli-json-contract-not-versioned
    statement: "CLI Copilot's --json output carries no schema_version/$schema/contract_version field anywhere in the package (F-15's finding is still true and unchanged); the product's OWN docs no longer claim otherwise."
    definition_refs: []
    check: "cd cli-copilot && test $(grep -rn schema_version copilot_cli/ | wc -l) -eq 0 && test $(grep -c versioned docs/00-overview.md) -eq 0   # both clauses now mechanically asserted (exit nonzero on failure), not just clause 1 -- QA WP-47 found clause 2 was a bare grep for a human to read"
    status: passing
    evidence: "Verified 2026-07-14: `grep -rn schema_version copilot_cli/` returns 0 matches -- F-15's underlying gap (no schema_version anywhere) is real and unchanged; Control Tower's own docs/01-architecture/cli-contract.md still names schema_version as a WS-A prerequisite explicitly 'not yet in upstream scope,' i.e. correctly framed as a design target, not a shipped fact. RE-VERIFIED 2026-07-14 (QA WP-47, strengthened check): `grep -c versioned docs/00-overview.md` returns 0 -- the word 'versioned' does not appear anywhere in the doc at all (not just absent from the one sentence F-15 originally flagged), a stronger and now-automated confirmation of the same fact the original evidence only read by eye. cli-copilot/docs/00-overview.md:11 reads '...one grammar, one `--json` contract, one portable `.env`...' -- the word 'versioned' is gone (present when F-15 was originally filed); this correction predates this task (found already fixed during investigation, consistent with cli-soul-conformance's R-12 'fix residual service-count untruths in CLAUDE.md/overview docs' commit touching this same doc). Closes F-18 instance #6 by CORRECTING the claim to match reality (the doc no longer overclaims) rather than building the still-missing feature -- this claim's own truth condition is narrower than 'the CLI is schema-versioned' (still false, a real open product gap, not asserted fixed here) -- it is 'the CLI's docs do not claim it is' (true, now mechanically checked), which is the specific F-18 concern (an artifact with no mechanism backing an assertion about it)."
    last_checked: "2026-07-14"

  - id: codex-specialist-chain-scorecard-not-run
    statement: "Codex Copilot's specialist-chain-evaluation scorecard (evals/specialist-chain/scorecard-template.json) is an unrun template (every criterion scored 0.0, 'Replace with...' placeholder evidence strings); the product's own docs describe it honestly as a template to copy and complete, and never claim it has been run."
    definition_refs: []
    check: "cd codex-copilot && python3 -c \"import json; d = json.load(open('evals/specialist-chain/scorecard-template.json')); scores = [c['score'] for case in d['cases'] for c in case['criteria'].values()]; assert all(s == 0.0 for s in scores), scores; print(scores)\" && test $(grep -Ec '[0-9]+(\\.[0-9]+)?%|[Ss]cored [0-9]|[Rr]esults?:' docs/03-developer-guides/03-specialist-chain-evaluation.md) -eq 0   # 2nd clause now mechanically asserted (no reported-outcome-shaped text in the doc), not just read by eye -- QA WP-47 found it was the same partial-conjunction pattern as cli-json-contract-not-versioned"
    status: unchecked
    evidence: "Verified 2026-07-14: scorecard-template.json's sole case has all 4 criteria (correctness, requirement_coverage, maintainability, context_efficiency) scored 0.0 with literal 'Replace with...' placeholder evidence -- genuinely never run, confirmed by direct file inspection, not by trusting the filename. RE-VERIFIED 2026-07-14 (QA WP-47, strengthened check): docs/03-developer-guides/03-specialist-chain-evaluation.md contains zero instances of an outcome-shaped pattern (a percentage, a 'scored N' phrase, or a 'results:'/'result:' line) anywhere in the file -- mechanically confirms the doc never states a completed result, not just that step 4's sentence reads as an instruction when a human looks at it. The doc is a 5-step INSTRUCTION list (run paired generalist/protocol tasks, score both, copy the template per variant, compare) with a comparator script invocation shown as a command, not a reported number. Closes F-18 instance #7 (phase-1-findings.md F-18 table row 7, 'Codex parity scorecard | a template of zeros, never run') by giving the artifact's true, incomplete state a registered, re-checkable mechanism for the first time, rather than leaving it an unbacked implication that someone might one day misquote as a real result. Status unchecked, not failing: no claim is being falsified here, only unfinished work made visible and trackable."
    last_checked: "2026-07-14"

  # --- t2 claim-sweep triage (TASK-137 follow-on): claims backing formerly
  # unbacked assertions found by claim_sweep.py's baseline. Each check below
  # was run BEFORE this entry was written (V-2 discipline) -- see evidence.

  - id: cli-copilot-service-count
    statement: "CLI Copilot fronts exactly 20 service integrations under the `copilot` command. All but one (Coolify) are configured purely via the single shared, .env-driven Pydantic Settings class (copilot_cli/config/settings.py); Coolify alone layers its own three-part config (CLI flags -> environment variables -> a TOML file)."
    definition_refs: []
    check: "cd tools/cse-bench && python3 cse_bench.py collect --only cli_soul && python3 -c \"import json; d=json.load(open('output/cli_soul-latest.json')); print(len(d['metrics']['scorecard']))\"   # expect 20; cd ../../cli-copilot && find copilot_cli/services -maxdepth 3 -iname config.py ! -path '*__pycache__*' | xargs grep -l tomllib   # expect exactly copilot_cli/services/coolify/core/config.py"
    status: passing
    evidence: "Verified 2026-07-14: fresh `cse_bench.py collect --only cli_soul` (generated_at 2026-07-14T14:08:35Z) reports exactly 20 services in the scorecard (bi, brevo, conversations, coolify, discord, docker, document, filesystem, git, infisical, insights, monitoring, n8n, nocodb, postgresql, project_copilot, shell, skill, system_services, uspto), 0 gaps. This is CURRENT, not stale: cli-copilot commit ba99edb ('cut fireflies/reddit, finish metabase cut, drop orphan creds', DEC-5/TASK-122, 2026-07-14 10:06 -0400, same day as this task, a concurrent session) is the owner's R-13 ruling landing and executing in one commit -- it removed fireflies/reddit/metabase/notion/method's dead surface and corrected the service count 22->20 in README/CLAUDE.md/docs/00-overview.md itself. R-13 (tc TASK-122) is therefore RULED, not still awaiting a decision -- the relevance-rule DEFER gate this task's own instructions named for 'products/services awaiting owner rulings' no longer applies to the service COUNT (though cli-soul-conformance's own xfail-gap tracking is a separate, already-registered claim, unaffected). Second half verified directly: exactly one service-level config.py (copilot_cli/services/coolify/core/config.py) imports tomllib/tomli_w; the other bespoke config module (infisical/core/config.py) is pydantic-only, no TOML -- confirming '19 of 20 use Pydantic Settings (.env); Coolify alone layers TOML' is accurate as worded, not just 'coolify differs somehow.'"
    last_checked: "2026-07-14"

  - id: cli-copilot-python-version
    statement: "CLI Copilot requires Python 3.11+."
    definition_refs: []
    check: "cd cli-copilot && grep -n requires-python pyproject.toml"
    status: passing
    evidence: "Verified 2026-07-14: cli-copilot/pyproject.toml line 10 reads `requires-python = \">=3.11\"`, matching docs/00-overview.md's stated requirement exactly."
    last_checked: "2026-07-14"

  - id: cli-copilot-env-file-autodiscovery
    statement: "CLI Copilot automatically walks up from the current working directory to find the nearest .env file when COPILOT_ENV_FILE is not set."
    definition_refs: []
    check: "cd cli-copilot && .venv313/bin/python3 -c \"import os, tempfile, pathlib; os.environ.pop('COPILOT_ENV_FILE', None); tmp = pathlib.Path(tempfile.mkdtemp()); (tmp / '.env').write_text('X=1'); deep = tmp / 'a' / 'b' / 'c'; deep.mkdir(parents=True); os.chdir(deep); from copilot_cli.config.settings import _find_env_file; found = _find_env_file(); assert found.resolve() == (tmp / '.env').resolve(), found; print('OK', found)\""
    status: passing
    evidence: "Verified 2026-07-14, functional (not just source-reading) test: created a throwaway .env three directories above a fresh CWD with no COPILOT_ENV_FILE set and no other project markers in between; copilot_cli.config.settings._find_env_file() correctly resolved to that exact .env path. Confirms README.md's 'the CLI will walk up from CWD to find the nearest .env file automatically' as a real, working mechanism, not just a docstring claim."
    last_checked: "2026-07-14"

  - id: claude-copilot-python-version
    statement: "claude-copilot's tooling requires Python 3.10+."
    definition_refs: []
    check: "cd claude-copilot && grep -n requires-python pyproject.toml"
    status: passing
    evidence: "Verified 2026-07-14: claude-copilot/pyproject.toml line 13 reads `requires-python = \">=3.10\"`, matching README.md's Requirements table exactly."
    last_checked: "2026-07-14"

  - id: claude-copilot-skill-description-coverage
    statement: "Every SKILL.md in claude-copilot's .claude/skills/ tree carries a non-trivial `description` frontmatter field -- the structural, CSE-controllable prerequisite for skills to 'surface automatically from trigger-rich descriptions' (whether Claude Code's own runtime actually auto-invokes a given skill on a given turn is a platform behavior outside this claim's scope; this claim covers only whether the framework holds up its half of the contract)."
    definition_refs: []
    check: "cd claude-copilot && python3 -c \"import sys, glob; sys.path.insert(0, '../copilot-control-tower/tools/cse-bench'); import check_claims; files = sorted(glob.glob('.claude/skills/**/SKILL.md', recursive=True)); bad = [f for f in files if len((check_claims.load_yaml(open(f).read().split('---', 2)[1]) or {}).get('description', '') or '') < 20]; print(len(files), 'skills,', len(bad), 'with a missing/short description'); assert not bad\""
    status: passing
    evidence: "Verified 2026-07-14: all 37 SKILL.md files under claude-copilot/.claude/skills/ parse (via check_claims.py's own YAML loader, reused rather than a second parser) to a `description` field of at least 20 characters -- 0 missing, 0 short. (An earlier hand-rolled single-line regex check falsely flagged 28/37 as 'short' because it matched YAML's `description: >-` block-scalar marker itself, not the folded text on the following lines -- corrected by reusing check_claims.load_yaml instead of re-deriving a second, worse parser; reported here per this task's own instruction to report scanner/check false positives.)"
    last_checked: "2026-07-14"

  - id: framework-session-cap-thresholds
    statement: "claude-copilot's session-length advisory hook fires a soft warning at 500 turns and a stronger warning at 750 turns; the force-delegate hook denies after 5 consecutive same-tool calls."
    definition_refs: []
    check: "cd claude-copilot && grep -n 'THRESHOLD_SOFT=\\|THRESHOLD_STRONG=' .claude/hooks/user-prompt-submit.sh && grep -n '5 consecutive' .claude/hooks/pretool-check.sh"
    status: passing
    evidence: "Verified 2026-07-14: .claude/hooks/user-prompt-submit.sh sets THRESHOLD_SOFT=500 and THRESHOLD_STRONG=750 (live config, not doc-only); .claude/hooks/pretool-check.sh's rule_force_delegate denies at 5+ consecutive same-tool calls (line 371's deny message: 'Main session has issued 5+ consecutive ... calls'). Matches docs/10-architecture/04-framework-restructure-2026-04.md's 'Advisory at 500 and 750 turns' / 'Hook blocks after 5 consecutive same-tool calls' cells exactly -- these are CURRENT config values (the hook fix, TASK-106/C-6, landed 2026-07-12; see hook-deadlock-fix-verified), not historical April-diagnostic figures."
    last_checked: "2026-07-14"

  - id: framework-april-2026-diagnostic-unrecoverable
    statement: "The April 2026 pre-restructure diagnostic quoted in claude-copilot's README.md and docs/10-architecture/04-framework-restructure-2026-04.md (15 sessions Apr 17-22; 94% main-session work / 6% delegation; 3.5% protocol-declaration rate; 671 turns/session; the interim 14->8 agent roster; the 57-bash-call/26-loop staging saga) is a one-time historical measurement that cannot be independently reproduced today: April's own counting script no longer exists (see turn-definition-incompatible-with-april), and separately, zero session transcripts predating the 2026-04-22 hook-enforcement cutover survive in either transcript corpus root -- the raw data itself, not just the script, is gone."
    definition_refs: []
    check: "python3 tools/cse-bench/check_pre_restructure_transcripts.py   # zero_pre_cutover_survives must be true"
    status: passing
    evidence: "Verified 2026-07-14 (tools/cse-bench/check_pre_restructure_transcripts.py, built this task): 310 .jsonl files scanned across the archive root (/Users/pabs/.claude/transcript-archive/claude-projects, empty on this machine) and the live root (/Users/pabs/.claude/projects); earliest surviving transcript timestamp is 2026-06-14T13:22:29Z -- almost two months AFTER the 2026-04-22 cutover this diagnostic predates. sessions_with_first_record_before_cutover: 0. This confirms, mechanically, that these specific figures are irrecoverable historical record (consistent with, and extending, F-6's 'the before/after comparison is permanently closed' finding and the already-registered turn-definition-incompatible-with-april claim, which covers the '671 turns/session' figure specifically) -- not that the figures are false, only that they can never again be independently checked. The doc's own framing already discloses this (past tense, 'a cross-session diagnostic covered...', 'Status: Superseded... historical context'); this claim makes the disclosure mechanically checkable rather than merely worded that way."
    last_checked: "2026-07-14"

  - id: control-tower-agent-count
    statement: "copilot-control-tower's own .claude/agents/ carries exactly 15 specialist agent definitions."
    definition_refs: []
    check: "find .claude/agents -maxdepth 1 -name '*.md' | grep -v manifest | wc -l"
    status: passing
    evidence: "Verified 2026-07-14: exactly 15 .md files under copilot-control-tower/.claude/agents/ (cco, cpa, cs, cw, do, doc, ind, me, qa, sd, sec, ta, uid, uids, uxd), matching CLAUDE.md's 'agents (.claude/agents/, 15 specialists)'."
    last_checked: "2026-07-14"
