---
id: 2d725c0b-19f5-4f7a-b59f-068b2b82a6ff
type: decision
tags: []
created: 2026-07-07T23:13:45Z
updated: 2026-07-07T23:13:45Z
scope: project
---

Copilot Control Tower M4 (Distribution/Signing/Self-Update, PRD-4, WS-D, phase P1): self-update is TRANSPORT not ecosystem computation, so it lives app-side without violating invariant #1 (parse-never-compute) — M1 already parses doctor's update-available/updating-app; M4 owns download/verify/stage/promote/rollback only. Ratified ADRs: (1) crash-only launchd watchdog KeepAlive={SuccessfulExit:false}, RunAtLoad=false, ThrottleInterval+circuit-breaker — never KeepAlive=true (resurrects a bad build); (2) rollback lives in the stable never-self-updated watchdog, not the new bundle — stages, launches --self-test, waits for a liveness heartbeat file, no heartbeat->discard+keep-last-known-good+poison-version+notify, offline-staple-verify before promote; (3) compiled-in trust root (minisign pubkey + default feed are consts), UpdateFeedURL/AllowSelfUpdate/UpdateChannel honored ONLY from forced/managed MDM domain (user-domain=tamper, reading feed from user domain=supply-chain RCE=OUT); (4) update state machine parses the doctor/compat signal, bidirectional min/max schema gate, newer CLI pulls newer app; (5) one owner of vendored cc via COPILOT_MANAGED_BY=controltower, verify-not-resign at pinned SHA, block release if cc<compat-floor, vendored via tauri.conf.json bundle.resources into Contents/Resources/cc (not build.rs, keeps build.rs Stream-A-owned). NO --force/--skip-verify anywhere (fail-closed verify). 7 fitness functions FF-M4-1..7. Streams: A(Rust me/sec) D(devops do) B(uid) Z(qa). Owner-gated: real codesign/notarize/staple (Developer ID cert+notarytool), minisign private key + two-of-N custody (blocks stable), cross-repo cc push+release build (D-3, real cc; M4 uses placeholder), update-feed endpoint URL (D-4-M4, undefined owner infra). Autonomous: all code/config/scripts/verification/tests via cargo test + headless.
