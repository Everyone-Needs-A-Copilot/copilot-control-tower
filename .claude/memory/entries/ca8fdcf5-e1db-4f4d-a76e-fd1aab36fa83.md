---
id: ca8fdcf5-e1db-4f4d-a76e-fd1aab36fa83
type: decision
tags: []
created: 2026-08-01T13:16:36Z
updated: 2026-08-01T13:16:36Z
scope: project
---

Task 216 RELEASE PUBLISHED: control-tower 0.3.0 (tag v0.3.0 @ 193c4ba, GitHub release https://github.com/Everyone-Needs-A-Copilot/copilot-control-tower/releases/tag/v0.3.0) shipping cc 2.0.1 (claude-copilot foundation tag v5.13.21 @ dea6a1d, the classifier-fixed build -- annotated-tag pins now peel FETCH_HEAD^{commit} instead of comparing the tag object SHA). Full pipeline: cut 3 claude-copilot foundation tags this session (v5.13.19 over 8a5cfcc superseded, v5.13.20 over the uv.lock fix superseded, v5.13.21 over the classifier fix = final); built/signed/notarized cc 2.0.1 via package-cc-macos-release.sh (Apple Accepted bf2ce5af); pinned into packaging/cc/ (sha256 ff2ac13f...); bumped app to 0.3.0 across User/Admin Info.plist, package.json, package-lock.json, src-tauri/tauri.conf.json, controltower.compat.json app_version (cc range 2.0.0-<3.0.0 already covered 2.0.1, no compat edit needed); found+fixed a REAL pre-existing regression blocking the gate suite -- CT_ONBOARD_QUESTION_SELFTEST's two inline ecosystem-report fixtures in native/control-tower-tray.swift predated the schema-2.0 DTO work (2057020) and were missing the now-required layers_state/completed_actions fields, silently failing decode; fixed by adding layers_state:not-computed + completed_actions:[] to both fixtures. All scripts/tests/*.sh green (16/16 files) including the two packaged-cc-topology-contract modes and both verify-vendored-cc.sh modes against the real 2.0.1 pin. Built+signed+notarized+stapled via package-user-release.sh (app notarization 274ec1af, DMG notarization 998ba493, DMG sha256 1692b2c6...); codesign --verify --deep --strict, spctl --assess --type execute (app) and --type open --context context:primary-signature (DMG, NOT plain -a -vv -- a locally-built DMG has no quarantine xattr so the explicit primary-signature context is required and is what package-user-release.sh itself uses), and offline stapler validate all pass, confirmed again on a gh-release-downloaded copy with checksum match. Published: release/control-tower-0.3.0-193c4ba/ committed (chore(release): add macOS release 0.3.0), branch+tag pushed, GitHub release created with the 5-asset convention (cc-notarization.json, controltower.compat.json, dmg, dmg.sha256, release-metadata.json). BONUS: also cut+published codex-copilot foundation tag v0.6.2 (coordinator-authorized optional task) via copilot-control-tower/scripts/foundation-snapshot-release.py --publish, using the SAME ~/.ssh/enac_foundation_release key -- confirmed its fingerprint SHA256:FIfppOkzwXZUAamELQzYoSUQXiEAmTYiVewHe1ACMZo is ALREADY both GitHub-registered (gh ssh-key list) AND compiled into claude-copilot/tools/cc/src/cc/core/ecosystem/policy.py's FOUNDATION_SSH_SIGNING_KEYS, so docs/06-deployment/foundation-release-signing.md's 'Current status: remain blocked' line is now stale -- someone should refresh it. Post-publish live read-only plan (packaged cc 2.0.1 against the real machine): cli-copilot and cli-copilot-internal now resolve current/reuse (both fixes -- the concurrent resolution agent's branch checkout AND the classifier fix -- combined); codex-copilot moved from sync_state=diverged to diverged-identical after v0.6.2 but STILL shows action=review -- this is architecturally permanent, not a residual bug: a foundation-role repo pinned via a parentless orphan-snapshot tag can never have its ordinary dev branch's HEAD literally equal the snapshot commit SHA, so diverged-identical is the best a content-matching foundation repo can ever reach on this classifier, by design (matches the phase-7 stage-A dossier's own prediction). Final review-row count is 4, not the 3 dirty-tree rows (knowledge-copilot, claude-copilot, claude-copilot-private) the coordinator anticipated -- codex-copilot is a legitimate 4th, structurally expected, not fixable by this classifier's design.
