---
id: 0b14d0c8-25ce-4964-ae53-254f2c2113e6
type: decision
tags: []
created: 2026-07-08T02:25:21Z
updated: 2026-07-08T02:25:21Z
scope: project
---

Copilot Control Tower M5-S3/S4 (do): (1) Login-item (src-tauri/src/loginitem/{mod,smappservice}.rs) uses objc2-service-management (0.3, macOS-only dep) for the real SMAppService.mainAppService()/registerAndReturnError/unregisterAndReturnError/status FFI, behind a LoginItemService trait seam (mirrors updater::launch::StagedBundleLauncher) — cargo test injects a fake, never calls real SMAppService. Enablement reads forced LoginItemManaged via managed::forced::forced_bool -> LoginItemEnablement{ManagedEnabled,ManagedDisabled,UnmanagedDefault}; ManagedDisabled is the only case that unregisters instead of registers. ADR-M5-004 delineation documented in loginitem/mod.rs doc comment: SMAppService=login-at-launch, launchd KeepAlive=crash-only-relaunch, two mechanisms one binary. FF-M5-4 fitness test scans src/loginitem/ for absence of KeepAlive/ThrottleInterval/SuccessfulExit vocabulary and absence of Command::new/std::process::Command (never spawns a process). (2) .mobileconfig generator (src-tauri/src/mobileconfig/{mod,generator}.rs) is PURE Rust (no plist crate, hand-rolled XML with escaping, like fitness_watchdog_plist.rs's precedent) emitting com.apple.ManagedClient.preferences (keyed by managed::keys::APPLICATION_ID, iterating MANAGED_KEYS so a new key is never forgotten) + com.apple.servicemanagement (login-item force-approve) + com.apple.notificationsettings payloads. generator_domain() re-exports keys::APPLICATION_ID (FF-M5-6 asserted equal in tests). generate() is fail-closed: looks_like_a_secret() heuristic (token prefixes, URL userinfo, JWT-shape) scans every string value BEFORE any XML is written and refuses generation (FF-M5-7) rather than passively scanning output after the fact. Golden fixture src-tauri/fixtures/mobileconfig/sample-acme-corp.mobileconfig generated via examples/gen_mobileconfig_fixture.rs, byte-identical copy at packaging/mobileconfig/ (the IT-facing deliverable); both plutil -lint clean. cargo build/clippy -D warnings/fmt --check/test all green, 463 lib+integration tests total (grew from concurrent sec/me M5 streams landing routing/deprovision/secretstore alongside). Owner-gated: real SMAppService register/status transitions need a signed bundle + real Mac; real MDM push/enrollment of the generated profile.
