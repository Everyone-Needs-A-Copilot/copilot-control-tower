---
id: f1841c0d-0901-434b-a206-93bc76145fbb
type: decision
tags: []
created: 2026-07-07T19:40:12Z
updated: 2026-07-07T19:40:12Z
scope: project
---

Control Tower M2 S2 (writer.rs): never-destroy merge is WHOLE-LAYER upsert by id, not field-level merge — write_manifest(path, incoming) reads existing manifest (or empty if absent), for each incoming.layers entry replaces the on-disk layer with that same id in place (or appends if new), leaves every other on-disk layer + its  fields byte-identical, then validates the MERGED result via S1's validate_layers before writing (refuses wholesale, no partial write). Top-level version/extra only overridden if incoming actually sets them. Atomic write = temp file in same dir + fsync + rename, cleaned up on any failure; parent dirs created via create_dir_all. Default path (D-3-M2) is ~/.copilot/copilot.layers.yml via HOME env var.
