---
id: 368abdf9-fb74-439d-a8c1-339674e2ae2f
type: lesson
tags: []
created: 2026-07-14T14:10:03Z
updated: 2026-07-14T14:10:03Z
scope: project
---

claim_sweep.py --check pre-commit hook bypasses found in TASK-137/WP-41 QA: (1) SCAN_TARGETS is a hardcoded curated file list (19 files total across 5 repos) -- any assertion added to ANY other markdown file (including a brand-new doc) is 100% invisible to --check, disclosed but the single biggest walk-around; (2) collectors/paths.py resolve_copilot_root() + scan_file() read the file straight off disk at a hardcoded absolute path, not from the git index/staged blob -- a classic pre-commit TOCTOU: stage violating content, then overwrite the working-tree file with clean content without re-staging, and the hook (which reads disk, not ) sees only the clean version and passes, while the commit still records the staged violation; (3) the annotation convention only validates that <id> resolves to SOME real claims.yaml entry, never that the entry is topically relevant -- annotating a false claim with any unrelated-but-real id (e.g. citing 'cli-command-liveness-breakdown' next to a fabricated percentage about something else entirely) marks it BACKED; only a non-resolving id is caught (DANGLING). (4) CLAUDE.md files are deliberately excluded from every repo's SCAN_TARGETS -- confirmed copilot-control-tower's own CLAUDE.md already contains a MECHANISM_CLAIM trigger word ('automatically') that would never be caught. (5) --no-verify skips the hook like any git hook, expected and undocumented as a caveat.
