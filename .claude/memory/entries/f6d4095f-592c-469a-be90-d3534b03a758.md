---
id: f6d4095f-592c-469a-be90-d3534b03a758
type: decision
tags: []
created: 2026-07-08T03:41:53Z
updated: 2026-07-08T03:41:53Z
scope: project
---

Copilot Control Tower M6-S3+S4 (tasks 54/55): ItSignal emission seam + local sink, and the un-dismissable security-banner DTO. S3 (routing/emit.rs): ItSignalSink trait (mirrors updater::launch::StagedBundleLauncher/loginitem::smappservice::LoginItemService's seam shape) + LocalSink (in-process non-secret audit buffer; AdminContact=None records deliverable:false, NEVER a Bob fallback) + emit(events, sink) which routes via policy::route and dispatches BOTH a bare EscalateIt AND an AutoAct's optional it_signal companion (the security-shadow auto-suspend's 'IT told in parallel' case) to the sink — AskBob/plain-AutoAct outcomes are only collected in EmitOutcome.routed for the caller, never sink-dispatched. Sink failures are eprintln!-audited AND retained in EmitOutcome.sink_errors, never silently dropped. Real IT transport is owner-gated (G-M6-1) — the seam is the deliverable, M7/M8 plug a real sink behind the same trait later. S4 (render/security_banner.rs): SecurityBanner{message, reaffirm_label} — exactly 2 &'static str fields, NO dismiss/clear/approve/resolved field or method (fitness-tested via source scan). build_security_banner(changed: &[ChangedItem]) keys ONLY on shadowed_by.is_some() (never requires severity_trailer present too) — model::update's own module doc names this exact renderer for the 'absent trailer is the WORST case' fail-closed rule, so an absent severity_trailer on a shadowed entry still renders the banner. Deliberately DISTINCT from routing::BobNotice::kept_you_safe (the Bob-lane's quiet mention, action-free by the closed BobPromptKind-set rule uid already flagged in copy.ts) — SecurityBanner is the one surface allowed the 're-affirm your version' affordance, wired into popover.ts between the Bob lane and the update section, TS mirror in types.ts + render/security_banner.ts + dev-fixtures/security-banner/. Two new fitness tests: fitness_m6_itsignal_sink_content_free.rs (trait signature + LocalSinkEntry field pin), fitness_m6_security_banner_reaffirm_only.rs (struct field pin + forbidden-vocabulary scan). 531 lib tests green, cargo clippy -D warnings clean debug+release, npm run build clean, headless CDP sweep (vite dev + headless Chrome, no Playwright installed so used raw CDP over native Node WebSocket) confirmed: empty fixture silent, kept-you-safe fixture renders exactly 1 button ('Re-affirm your version', no dismiss/clear/approve text anywhere in the banner markup), 0 console errors, Bob-lane prompt still outranks the banner in DOM order. routing/mod.rs and render/mod.rs each got one added 'pub mod' line (emit, security_banner) — S2's own doc claimed these would be pre-declared but weren't, so this stream added them, never touching any core type. Routes to S6 (sec) for live wiring: real doctor/update verdicts -> RoutableEvent -> route()/emit() -> LocalSink + SecurityBanner + Bob lane, plus S7's fitness suite.
