---
id: aed26458-9718-42c0-88ca-7e0306cb3231
type: decision
tags: []
created: 2026-08-02T12:29:27Z
updated: 2026-08-02T12:29:27Z
scope: project
---

Commit-sweep + manifest repair 2026-08-02 (WP-390 blocker): committed the intended framework footprint (.agents/, .claude/, .codex-copilot.json, AGENTS.md, CLAUDE.md, SOUL.md, copilot.lock.json, docs/, plugins/, scripts/) across all 9 pending layer repos (claude/cli/codex/knowledge-copilot-{accounting,internal,private} minus the 3 already-clean ones), gitignoring .mcp.json per claude-copilot's own convention (all 9 .mcp.json were empty {"mcpServers":{}} -- no leak, still gitignored per policy); leak-scanned every staged file (key/token/secret regex + full reads of matches) -- all clean, only prose word-matches (VERDICT token, design tokens, security-review guidance). Pushed all 9 as ordinary fast-forwards, verified local==remote HEAD and gh repo view identity for each. cc onboard --apply then hit a REAL PRE-EXISTING defect (not caused by the sweep): cc's lockfile.py has a documented (WP-372 P5.2, task 215) naming collision -- claude-copilot's OWN repo root has a project-shape copilot.lock.json ({components:[...]}), and running cc onboard --apply from inside that repo (the task's instructed 'neutral cwd') makes default_lockfile_path() resolve to it, crashing materialize.py's previous_lock.items() loop ('list' object has no attribute items). Rollback was clean (byte-identical restore, verified via diff). Fix: re-ran --apply from a genuinely neutral cwd (outside any git repo) via 'uv run --project .../claude-copilot/tools/cc' (same in-dev cc 2.2.0 codebase, not a version downgrade) -- succeeded, manifest now pins cli-foundation at v0.3.2, 16 layers with paths intact. Final read-only plan: result=ready, all 16 layers reuse/current, zero review, claude-foundation shows no divergence as of this check (concurrent cc 2.2.0 work not yet visible here).
