---
id: 10d55b53-627a-4313-9cc1-959425c9f209
type: decision
tags: []
created: 2026-07-08T05:42:36Z
updated: 2026-07-08T05:42:36Z
scope: project
---

Copilot Control Tower M7-S3/S9 (tasks 62/68): built telemetry/emitter.rs (TelemetryTransport trait + MockTransport/CaptureTransport/PanicIfCalledTransport, TelemetrySink gated by telemetry_optin(), TelemetryState managed wrapper) rather than tc's originally-described 'real batched/retry ItSignalSink with SAFETY-durable/ANALYTICS-best-effort split + dedup' shape — per this session's explicit brief, which simplified to: one unified FleetEvent-consuming sink, off-means-off (zero transport calls when Disabled, proven via PanicIfCalledTransport), fail-closed (transport errors retained/audited, never panic), retry/backoff left as named-const placeholders (DEFAULT_RETRY_ATTEMPTS/DEFAULT_RETRY_BACKOFF, G-M7-6 flagged not implemented as a loop). Added routing::wire::doctor_it_signals (pure, sink-free re-derivation of ItSignal from an already-trusted DoctorVerdict via existing doctor_routable_events+policy::route) so telemetry never re-computes trust or duplicates the LocalSink dispatch path — LocalSink stays completely unchanged; telemetry is a parallel opt-in remote sink. ItSignalKind->FleetEventKind mapping is partial by design (schema's own narrower closed set) — DeprovisionAmbiguous/BobItemTimedOut/PruneNeedsReview/RepairNeedsReview/UnrecognizedEvent map to None, silently skipped for analytics (LocalSink IT-audit unaffected). Wired timer::poll_once to call TelemetryState::emit_for_doctor_verdict on every Trusted doctor poll (previous_status captured via DoctorState::snapshot() before replace, so status_change only fires on genuine transitions). get_fleet command landed in render::fleet.rs (NOT commands.rs) because commands.rs/tray.rs/lib.rs's generate_handler! list is scanned by tests/fitness_m5_deprovision_is_it_routed.rs (FF-M5-3) for a blanket 'routing::' substring needle — get_fleet needs routing::emit::LocalSink directly (no render-owned wrapper existed), so the command itself lives in render/fleet.rs (which the scan doesn't cover) and lib.rs registers it as render::fleet::get_fleet. render::fleet::FleetView/FleetHostView/FleetActionItem mirror src/types.ts exactly (hosts-only, FF-M7-NOSCORE pinned by a value-level test); build_fleet_view returns the LOCAL host's real status+real LocalSink-derived actionable items as one row plus 2 fixed mock-prefixed fleet-mate rows (G-M7-3 collector undefined). CRITICAL GOTCHA hit: mock_fleet_mates() originally used CliStatus::Healthy for a 'clean' mock row and tripped tests/fitness_no_fabricated_healthy.rs (T3) crate-wide invariant that Healthy may only ever be constructed at model::state::parse_cli_status — fixed by using CliStatus::Syncing instead; this invariant applies even to obviously-labeled mock/dev data, no exception. Added capabilities/fleet.json + a new invisible 'fleet' window in tauri.conf.json (no open-command wired yet, matches admin module's own 'unscheduled stream' precedent) + get_fleet in build.rs AppManifest. Added tests/fitness_m7_telemetry_transport_content_free.rs (source-scan pin that TelemetryTransport::send's signature is exactly (&self, endpoint: &str, events: &[FleetEvent]) -> Result<(), TransportError>, mutation-tested live by widening the signature — cascaded into compile errors across every impl, reverted byte-identical). Full suite green: cargo build/build --release/clippy --all-targets (debug+release)/fmt --check/test (787 total passed across all binaries, 0 failed, stable across repeated runs)/npm run build all clean. Batched for owner: real HTTP transport to a collector endpoint + the collector's own ingest API (G-M7-3), real hardware-UUID+keychain-salt machine_id derivation (DEV_LOCAL_MACHINE_ID/render::fleet::LOCAL_MACHINE_ID are both honest fixed placeholders), retry/backoff params (G-M7-6).
