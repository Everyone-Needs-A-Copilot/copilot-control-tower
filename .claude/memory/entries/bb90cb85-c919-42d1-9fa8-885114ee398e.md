---
id: bb90cb85-c919-42d1-9fa8-885114ee398e
type: context
tags: []
created: 2026-07-08T04:51:51Z
updated: 2026-07-08T04:51:51Z
scope: project
---

Copilot Control Tower M7-S8 (task 67, doc agent): wrote the Admin/IT standup runbook set. Files: docs/06-deployment/README.md (rewrote stub into a real index — shipped-vs-designed table, managed-key reference transcribed from managed::keys::MANAGED_KEYS, consolidated owner-gated trail folding m5-owner-gated-batch.md + M7 task gaps), docs/06-deployment/standup-runbook.md (10-step seed->preflight->mobileconfig->rollout->telemetry->two-of-N->deprovision walkthrough, each step marked shipped/partial/designed), docs/06-deployment/two-of-n-custody-runbook.md, docs/08-observability/operator-guide.md (fleet dashboard, no-fleet-score rule). HYPOTHESIS stamp (Founding Decision #9, SOUL.md §9 item 9) placed at the top of all 4 docs plus the deployment README index.

KEY FINDING (moving-target accuracy): M7 code was landing IN PARALLEL, live, while this doc session ran — tc task tracker showed every M7 stream 'pending' throughout, but git status/filesystem showed real progress: telemetry::schema (S1, FleetEvent type + machine_id derivation) landed; updater::multisig (S5, two-of-N verifier, TRUST_ROOTS_B64 3 dev keys + THRESHOLD_K=2, verify_update_multisig as a SIBLING to the still-live single-key verify_update, not a replacement) landed; fleet dashboard FRONTEND (S4: src/render/fleet.ts, src/fleet.html, src/dev-fixtures/fleet/*.json, FleetHostView/FleetActionItem/FleetView in types.ts, GET_FLEET_CMD reserved) landed against fixtures only, no Rust backend/get_fleet command. Seed generator (S6) and preflight (S7) had NOT landed — docs for those two steps describe design-only with explicit hand-YAML-still-required interim guidance.

DISCREPANCY FLAGGED (not fixed, per instructions): observability.md §7.1 specifies a 4-named-state (Healthy/Stuck/Behind/Needs-re-auth) fleet-state model, but the landed FleetHostView type instead carries the CLI's own CliStatus + BadgeState directly (no distinct fleet-state enum) — narrower than the spec, flagged in operator-guide.md for whoever wires S9's live command, not resolved.

Lesson for future doc sessions on this repo: when the task brief says a sibling milestone is 'being built in parallel,' verify via git status/find at the START and re-verify near the END before finalizing claims — the tc task tracker status lags real filesystem state considerably in this repo's workflow (tasks stay 'pending' while agents commit untracked files), so 'pending' in tc task list is not evidence a stream hasn't started.
