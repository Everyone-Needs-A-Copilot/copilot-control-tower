---
id: e8a5f9b6-757e-4ef8-aa6a-b6416847d56c
type: decision
tags: []
created: 2026-07-08T01:00:50Z
updated: 2026-07-08T01:00:50Z
scope: project
---

M4 gap-closure (S11): wired the self-test + staged-bundle rollback machinery into the live path via 4 new updater/ modules (glue only, no rewrite of heartbeat.rs/watchdog.rs/check.rs's existing verify pipeline): launch.rs (StagedBundleLauncher trait + RealBundleLauncher, injectable seam for tests), rollback_marker.rs (atomic 'last update outcome' marker, shown-once), selftest.rs (--self-test argv entrypoint calling heartbeat::run_self_test, called from main.rs BEFORE copilot_control_tower_lib::run() so a self-test launch never starts the tray), startup.rs (crash-only reconciliation of an interrupted staged update via watchdog::run_self_test with an always-Timeout NeverAliveSource, called from lib.rs's .setup()). check.rs::apply_update_at now completes promote/rollback SYNCHRONOUSLY (not deferred to a later launchd relaunch): after apply_update_with stages+staples, confirm_staged_bundle_boots launches the staged bundle with --self-test via the launcher seam then calls watchdog::run_self_test (never reimplemented) with a real heartbeat::FileHeartbeatSource, surfacing UpdateStatus::Ready on promote / RolledBack ('Kept your working version.') + rollback_marker::record_rollback on rollback. check_for_update_at checks rollback_marker::take_rollback_outcome FIRST (before the AllowSelfUpdate gate) so a subsequent normal launch surfaces the toast once then clears it. Added tests/fitness_self_update_machinery_is_wired.rs (anti-orphan regression guard, text-scan of production-only source for watchdog::run_self_test/UpdateStatus::RolledBack/selftest::requested/startup::reconcile_interrupted_update references in main.rs/lib.rs/check.rs/startup.rs/selftest.rs). All green: cargo build/clippy -D warnings/fmt --check/test, debug AND release (373/374 individual tests — 1 pre-existing debug-vs-release dev-seam test-count delta, unrelated). Fresh  + strings scan: CT_CLI_PATH/CT_UPDATE_FEED/DEV_OVERRIDE_ENV/DEV_FEED_OVERRIDE_ENV all 0 occurrences (note: running cargo test --release/clippy --release AFTER a plain cargo build --release can transiently overwrite target/release/<bin> with a dev-seam-enabled build sharing the same output path — always re-verify with a clean rm -rf target/release rebuild as the LAST step, not mid-sequence). Owner-gated (not exercised): the real staged-bundle launch + promote on a signed, notarized .app bundle (RealBundleLauncher's positive path — mirrors verify::verify_staple's identical 'fail-closed path tested for real, positive path needs a real cert' caveat).
