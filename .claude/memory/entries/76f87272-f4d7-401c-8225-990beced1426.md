---
id: 76f87272-f4d7-401c-8225-990beced1426
type: decision
tags: []
created: 2026-07-08T06:16:26Z
updated: 2026-07-08T06:16:26Z
scope: project
---

Copilot Control Tower M9 (Windows re-skin, P4 final): re-skin the shared Tauri core behind #[cfg(windows)] via a light platform-abstraction module (src-tauri/src/platform/) — traits where a mock is needed (PlatformForcedConfig/LoginItem/WatchdogSignal/SecretStore), cfg pub-use aliasing elsewhere; macOS impl is a THIN wrapper over existing proven code (pure refactor, macOS stays green). No Windows toolchain on this machine, so Windows build/test/EV-Authenticode-sign/GPO-Intune-push are OWNER-GATED; local DoD is macOS cargo test green + fmt/clippy clean + Windows code cfg-gated out. Two hardest invariant mappings, resolved not discovered: (Q1/#2) crash-only on Task Scheduler = failure-restart-with-cap wired to the reused updater::circuit_breaker + updater::heartbeat hang-detection, NEVER the periodic-repeat trigger; (Q2/#4) forced config from HKLM\Policies is honored ONLY when domain-joined/MDM-enrolled (dsregcmd), else fall to compiled-in trust root exactly as macOS does with no profile — because HKLM\Policies is not structurally distinct from a local admin on BYOD. PRD 8, tasks 70-80 (Stream-A..Z), ADR bundle in WP-52.
