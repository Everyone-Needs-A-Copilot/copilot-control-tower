---
id: f3174857-806a-4a38-857b-ffa4d9666030
type: decision
tags: []
created: 2026-07-08T05:09:08Z
updated: 2026-07-08T05:09:08Z
scope: project
---

Copilot Control Tower M7-S6/S7 (tasks 65/66, .copilot/wp/43.md): built the Admin-mode seed generator + red/green preflight as a NEW admin/ module (src-tauri/src/admin/{mod,seed,preflight}.rs) rather than ta's originally-planned separate seed/+preflight/ top-level dirs — per this session's explicit task instructions. seed.rs: EcosystemSeed/FoundationPin/ProductSpec(Topology)/DepartmentSpec/TelemetrySpec typed to docs/reference/ecosystem-architecture.md §4.2's worked YAML example + observability.md §6's telemetry{enabled,endpoint} block (telemetry OFF by default, both fields required together if enabled). Reuses mobileconfig::generator::looks_like_a_secret directly (no second copy) for a fail-closed pre-emit secret scan across every string field (org/host/foundation.mirror/root_key/key_set/policy_signers/department fields/telemetry.endpoint) - public keys never trip it. validate_shape (structural) + validate_no_secrets are separately public so preflight can report them as distinct checks. FLAGGED (G-M7-7, next available after G-M7-1..6): the ecosystem.yml seed shape is DOC-frozen only - claude-copilot/tools/cc has NO ecosystem.yml loader/schema today (verified via grep; only copilot.layers.yml's manifest.py exists, which is the DERIVE OUTPUT not this seed). Flagged for CLI/ecosystem owner to ratify a real schema. preflight.rs: run_preflight(seed_text, PreflightConfig) -> PreflightReport, 8 checks (seed_parses, seed_well_formed, no_secret_in_seed, dept_repos_exist, policy_signed_by_authorized_signer, managed_profile_complete_for_silent_path, foundation_pin_resolves, mirror_reachable), every external fact caller-supplied (parse-not-compute, no gh/network calls from this crate), every PreflightConfig field defaults to unchecked->Unknown (fail-closed, never fabricated Pass). PreflightReport carries ONLY {checks: [...]} - no aggregate score field, mirroring FF-M7-NOSCORE's FleetView precedent. Added PreflightReport/PreflightCheckResult/PreflightCheckStatus TS mirror (append-only) to src/types.ts. Fixtures: src-tauri/fixtures/seed/sample-acme-corp.ecosystem.yml generated via new examples/gen_seed_fixture.rs, drift-checked by tests/fitness_m7_seed_fixture_drift.rs (byte-identical + round-trip parse). 27 seed/preflight unit tests + 2 fixture-drift tests, all green. Verified full suite: cargo build/build --release/clippy --all-targets (debug+release, forced recompile)/fmt --check/test (639 lib+integration tests, 0 failures)/npm run build all clean at session end - no clippy issue found in sec's multisig.rs at this point in time (concurrent M7 streams were mid-landing).
