---
id: e0770584-b8f6-4c0a-9f71-70dbf33ad972
type: decision
tags: []
created: 2026-07-07T22:00:48Z
updated: 2026-07-07T22:00:48Z
scope: project
---

M3 S4/S5/S6 (WP-24/25/26): managed_flow::run() is a single blocking call driving Welcome->Detect->Materialize->Verify->Teach->Done end-to-end (0 questions); profile check distinguishes Absent (Path::exists false, settling-window retry) from Invalid (present, settings::validate::validate_layers fails) -- both route to the SAME Holding(ItConfigIncomplete) event. unmanaged_flow::UnmanagedFlow is a stateful struct (Tauri-managed, boxed in the WizardRuntime enum to satisfy clippy::large_enum_variant) driven one step at a time across IPC calls: choose_products/set_layers/begin_signin/poll_signin, always exactly 3 canonical steps (ChooseProducts/LayerSetup/SignIn matching StepKind's frozen 3 variants) with ChooseProducts auto-skipped (0 questions) when the product catalog is unambiguous (len==1). Sign-in's own terminal status (denied/expired/timeout) NEVER gates the phase machine -- poll_signin advances on ANY terminal status, and the next real doctor poll independently reports signed-out and holds on its own honest terms (reuses doctor's existing signed-out render). Both flows share support.rs's must_transition + drive_verify (CliStatus::WaitingForNetwork maps to the dedicated OfflineFoundationOnly event -> HoldingReason::WaitingForNetwork, not the generic VerifyFailed catch-all) so Done is reachable ONLY via a fresh Verified(Healthy) doctor poll in both flows. S6 IPC lives in commands.rs (WizardIpcState/WizardRuntime: NotStarted{mode}|Managed(WizardPhase)|Unmanaged(Box<UnmanagedFlow>)), wizard_advance/wizard_begin_signin/wizard_poll_signin route blocking work through spawn_blocking with a clone-before-move fallback to the unchanged prior snapshot on a join panic (never fabricate advance). New capabilities/wizard.json scoped to only wizard commands + the wizard window; build.rs's AppManifest.commands() list had to be extended for the new commands or tauri_build::try_build panics with 'Permission not found' (the permissions/autogenerated/*.toml files are generated FROM that list, not from the invoke_handler macro alone).
