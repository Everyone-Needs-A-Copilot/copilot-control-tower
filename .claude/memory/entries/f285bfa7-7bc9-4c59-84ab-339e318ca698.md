---
id: f285bfa7-7bc9-4c59-84ab-339e318ca698
type: decision
tags: []
created: 2026-07-07T23:31:26Z
updated: 2026-07-07T23:31:26Z
scope: project
---

M4 Stream-D (S6-S9, devops): implemented the distribution/signing/watchdog machinery. S6: packaging/launchd/com.everyoneneedsacopilot.controltower.plist (KeepAlive={SuccessfulExit:false} dict, RunAtLoad=false, ThrottleInterval=10, per-user gui/$UID only) + install-watchdog.sh/uninstall-watchdog.sh (launchctl bootstrap/bootout, never sudo/system daemon); src-tauri/src/updater/{mod,watchdog}.rs implements the staged-bundle layout (current/staged/last-known-good/poisoned/<version>) and the promote-or-rollback decision (fail-closed: only HeartbeatOutcome::Alive promotes, Timeout/Malformed both roll back per FF-M4-6), consumed via a local HeartbeatSource seam trait since sec's heartbeat.rs hadn't landed yet at write time (do not redefine once it lands — implement the trait against it). S7: scripts/sign.sh (inside-out codesign, hardened runtime, reads CT_SIGN_IDENTITY from env, deliberately has no re-sign-overwrite flag since it's a one-shot fresh-build signer) + scripts/notarize.sh (notarytool submit --wait + staple both .app and .dmg, keychain-profile or API-key env creds) + packaging/entitlements/controltower.entitlements.plist (empty/locked-down) + .github/workflows/release.yml (owner-gated, v*-tag-triggered, unexecuted). S8: packaging/cc/cc placeholder stub (exits 1, honest-failure not fake-success) + PINNED_SHA256 + scripts/verify-vendored-cc.sh (checksum gate always runs; codesign/spctl gated on  detecting Mach-O, skipped+warned for the placeholder) wired via tauri.conf.json bundle.resources {'../packaging/cc/cc':'cc'} into Contents/Resources/cc (the exact path cli::path::resolve() already computes). S9: tauri.conf.json plugins.updater config (active, dialog:false, endpoints placeholder on the .invalid TLD pending D-4-M4 feed-URL decision, pubkey placeholder pending sec's minisign custody/trust.rs) + bundle.macOS {entitlements, hardenedRuntime:true, minimumSystemVersion:13.0} with signingIdentity deliberately absent (Tauri's bundler reads APPLE_SIGNING_IDENTITY/APPLE_CERTIFICATE/APPLE_CERTIFICATE_PASSWORD env directly at CI build time, never hardcoded). Fitness tests added: src-tauri/tests/fitness_watchdog_plist.rs — FF-M4-1 (KeepAlive never bare true, text-scan of the plist), RunAtLoad-never-true, and FF-M4-2 (no --force/--skip-verify) SCOPED ONLY to this stream's own files (mod.rs/watchdog.rs, packaging/, scripts/) — NOT the whole src-tauri/src/updater directory, because sec's concurrently-landed trust.rs/verify.rs legitimately document the banned flags by name in prose; a directory-wide scan false-positived on that and had to be narrowed to an explicit file list. Windows-reskin seam: launchd->Task Scheduler restart-on-failure (open-question per windows-parity.md row 1, no ThrottleInterval-equivalent backoff there), codesign->Authenticode/signtool (row 5, no staple-equivalent — SmartScreen reputation is not deterministic/offline like notarization+staple), self-update mechanism itself is cross-platform-free via Tauri's updater (row 9, Designed-for). Verification: plutil -lint clean on both plists, bash -n clean on all 5 scripts, cargo build/clippy -D warnings/fmt --check/test all green (253 lib tests + 3 new fitness tests = 256; +7 watchdog unit tests included in the 253), verify-vendored-cc.sh actually run against the real placeholder in-repo (checksum+file-type branch both exercised). Owner-gated hand-off: (1) Apple Developer ID Application cert + notarization credentials (APPLE_SIGNING_IDENTITY/APPLE_CERTIFICATE/APPLE_CERTIFICATE_PASSWORD/APPLE_NOTARY_KEY_*) to actually run sign.sh/notarize.sh/release.yml; (2) the real signed/notarized/universal cc artifact + pinned SHA from claude-copilot CI (D-3) to replace packaging/cc/cc + PINNED_SHA256 wholesale; (3) minisign two-of-N signing custody (ADR-M4-003) to replace tauri.conf.json's pubkey placeholder and populate sec's trust.rs compiled-in const; (4) the real update-feed endpoint URL (D-4-M4, still undefined) to replace the .invalid placeholder in tauri.conf.json's updater.endpoints.
