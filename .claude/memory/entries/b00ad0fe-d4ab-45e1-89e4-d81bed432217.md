---
id: b00ad0fe-d4ab-45e1-89e4-d81bed432217
type: decision
tags: []
created: 2026-08-11T23:42:45Z
updated: 2026-08-11T23:42:45Z
scope: project
guard: clean
---

E-4 effectiveness check (resolve_attribution_matches_lock) now computes expected_today dynamically via _policy_blocked_reason (core/ecosystem/policy.py's EXECUTABLE_DIMENSIONS + a layer's own policy.allowed_signers) instead of a hardcoded subject list -- verdict stays FAIL for a real resolver/lock disagreement even when policy-blocked (never weakened to pass); only the expected/unexpected classification is computed. E-1 needed two stacked fixes: setup-project.md Step 6 rewritten to call resolve_claude_content() (was a flat cp from one hardcoded root), AND the conformance harness's own E-1 driver had two independent bugs (fixture built after bash already ran; product='effectiveness-probe' never matched resolve_claude_content()'s product=='claude' filter). E-6 hook wiring in pretool-check.sh is deliberately scoped to a small named agent roster (sd/cw/do/ind/uxd/uids/cco), not every agent -- cc extensions resolve is a real ~100-300ms subprocess and gating every @agent-X dispatch would tax the framework's most frequent operation for agents that only ever resolve to no_extension. E-3: deleted claude-copilot-internal's stale/empty commands/protocol.md org override rather than rebasing it (still would have been empty) or authoring Pablo's company content (out of scope) -- org tier now declares nothing there, honest fallthrough to foundation.
