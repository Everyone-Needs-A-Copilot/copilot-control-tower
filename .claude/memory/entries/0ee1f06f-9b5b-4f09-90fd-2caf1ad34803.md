---
id: 0ee1f06f-9b5b-4f09-90fd-2caf1ad34803
type: decision
tags: []
created: 2026-07-08T01:49:51Z
updated: 2026-07-08T01:49:51Z
scope: project
---

Copilot Control Tower M5 (MDM & Security, phase P2): (1) Consolidate M2 settings::managed + M4 updater::trust ad-hoc CFPreferences readers into ONE audited managed/forced.rs boundary (sole CFPreferencesCopyAppValue/IsForced FFI site; both delegate) + a frozen managed/keys.rs registry shared by reader and .mobileconfig generator. (2) Deprovision is render-only parse-not-compute: app parses cc deprovision --json (result wiped|partial|noop, removed{materialized,clones[]}, retained_dirty[], secrets_touched const 0) and renders; NO wipe/retain logic in Rust; build against MOCK cc, NEVER real ~/.claude; secrets_touched!=0 is an honest alarm. (3) Route-by-competence: forced Deprovisioned=true and doctor auth[].state=revoked are IT-routed (content-free signal to forced-domain AdminContact), never a Bob prompt. (4) Login-item = SMAppService launch-at-login + com.apple.servicemanagement managed payload, DISTINCT from M4 crash-only launchd watchdog (KeepAlive SuccessfulExit:false); two mechanisms one binary per-UID. Gaps: G-M5-1 domain-name inconsistency (docs dev.enac.controltower vs code com.everyoneneedsacopilot.controltower — generator must match reader), G-M5-2 unfrozen key schema, G-M5-3 does app render-vs-invoke cc deprovision (debounce is CLI-side), G-M5-4 removed.materialized semantics undefined, G-M5-5 login-item vs watchdog. Streams A(sec foundation) B..F parallel Z(qa integration); PRD 5, tasks 43-50.
