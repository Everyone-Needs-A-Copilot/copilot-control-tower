---
id: d1e7de25-e05f-425e-a38e-e0b522fb6f38
type: decision
tags: []
created: 2026-08-11T19:15:20Z
updated: 2026-08-11T19:15:20Z
scope: project
guard: clean
---

Closed the last 3 ecosystem-blocking items 2026-08-11: (1) claude-copilot RC-3/RC-5 test updates (test_layer2_stack.py, test_rc_regressions.py) were already committed on main by the time this session started (38459d3) -- pytest 2562/0/14 skipped confirmed green. (2) claude-copilot-internal PR #5 merged reversibly: enforce_admins toggled off just long enough for --admin merge, then restored; re-read protection JSON diffed byte-identical against the pre-change snapshot. (3) nocodb-crm-setup git-init'd with .env gitignored before first commit, then brought to the standard Claude+Codex install via cc workspace finish (both components verify ready). CRITICAL FINDING: mid-session, this repo's shared working tree (claude-copilot, NOT a worktree) was silently switched to a sibling session's branch (fix/tc-wp-store-type-validation) between a status check and a commit -- my conformance-fix commit landed on the wrong branch. Recovered via an isolated git worktree (cherry-pick onto main, pushed, then reset the sibling branch back to its exact prior tip) with zero data loss, but this is a real, repeatable hazard: never assume the branch you last saw is still checked out in a shared (non-worktree) directory if other claude processes may be running concurrently -- always re-check git status/branch immediately before any commit in a shared repo. Conformance baseline regeneration via generate_baseline.py REFUSED (correctly) -- 10 genuine PASS-to-FAIL regressions vs the 2026-08-10 baseline exist on repos outside this task's scope (preflight-copilot's dev branch is missing its whole Claude/Codex project-integration despite passing before; research-copilot mid-session dirty state; insights-copilot codex mirror/lock drift; copilot-control-tower's own CLAUDE.md lock checksum drift) -- did not bypass with --acknowledge-regression; baseline file left unchanged, flagged for separate follow-up. Live cc conformance check --json --full: 164 total fails (down from 192), 2 S0 (down from 6) -- both S0s are pre-existing tracked gaps (RC-1 fleet hook rollout percentage, RC-5 claude-organization now under-declaring 'plugins' dimension after PR #5 added real plugins/codex-copilot content without updating dimensions:) confirmed present as fail in the OLD baseline too, not new regressions.
