---
id: f72f0e09-8aa0-4d44-9b63-7ea24177cd24
type: decision
tags: []
created: 2026-07-16T17:32:14Z
updated: 2026-07-16T17:32:14Z
scope: project
---

WS-A Stream-D (update-slice): private-repo HTTPS token auth injected via git -c http.extraHeader on clone/fetch only (never written to .git/config), soft-dependency via authstore.read_identity->keychain.get_secret, gated strictly to https/http sources so ssh/anon/local-path mirror tests never touch Path.home(). Per-layer freshness added as opt-in build_per_layer_freshness()/build_freshness_report(per_layer=True) kwarg; existing CLI path (freshness_cmd calls build_freshness_report() with no args) is untouched -- regression-guarded by a byte-shape test. freshness.schema.json gained an additive optional layers[] array (source + vendored copy kept in sync, not committed in copilot-control-tower per instructions).
