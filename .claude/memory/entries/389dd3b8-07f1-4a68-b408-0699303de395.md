---
id: 389dd3b8-07f1-4a68-b408-0699303de395
type: decision
tags: []
created: 2026-07-07T21:06:57Z
updated: 2026-07-07T21:06:57Z
scope: project
---

Control Tower M3 S2 (wizard/persistence.rs): first-run completion + checkpoint persisted via cc config set wizard.completed / wizard.checkpoint (ADR-M3-004), reusing config_pointer.rs's exact dev-mockable CT_CLI_PATH seam and Command::new(cli_path) shape -- never a Rust JSON read-modify-write of ~/.claude/cc/config.json, so never-destroy of sibling keys (paths.*, layers.*) is structural, not tested-in Rust. WizardCheckpoint{mode, phase: CheckpointPhase::{Materialize,Holding}, answers: BTreeMap<String,String>} deliberately does NOT mirror the full WizardPhase (not Serialize by S1 design) -- only the two moments S1 named as checkpointable: Materialize's first entry (phase_name still empty) and any Holding terminal. resume_phase() reconstructs the resumed phase by driving the REAL state::transition() fn (Holding -> HoldingResolved -> Detect), never hand-constructing a phase. mark_complete(phase: &WizardPhase) refuses (Err(NotDone)) unless phase==Done -- can't itself fabricate completion (relies on the same trust boundary dto.rs's to_wizard_state already relies on: WizardPhase::Done is only ever legitimately constructed via transition()). is_first_run()/load_checkpoint() honest-degrade to 'not complete'/'None' when cc is unavailable or the read body is unreadable -- never a false 'already set up'. 11 new unit tests (temp dirs + a local mock-cc-config python script simulating cc config get/set --json), verified green via cargo test --lib with sec's concurrent wizard/signin.rs temporarily excluded (its own unrelated SigninState:Eq derive bug blocked whole-crate compilation at the time; not touched, not fixed by this task, flagged for sec/QA).
