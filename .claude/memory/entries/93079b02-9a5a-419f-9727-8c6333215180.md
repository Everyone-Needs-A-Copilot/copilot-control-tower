---
id: 93079b02-9a5a-419f-9727-8c6333215180
type: decision
tags: []
created: 2026-07-08T07:22:15Z
updated: 2026-07-08T07:22:15Z
scope: project
---

M9 Streams I+J (tasks 78/79) — Windows packaging + Authenticode/updater-verify. Per-user MSI: packaging/windows/wix/main.wxs sets InstallScope="perUser" + ALLUSERS="" (never "1"), wired via tauri.conf.json bundle.windows.wix.template. Never-orphan uninstall: standalone packaging/windows/wix/uninstall-customaction.rs (compiles standalone via rustc, verified on this machine) deletes both per-user scheduled tasks idempotently; retains app state dir, HKLM policy keys, and does NOT fabricate Credential Manager cleanup because platform::windows::secret_store is endpoint-reference-only (never writes real secrets) — task description's premise was stale, verified against actual Stream-G code before implementing. updater::verify: verify_staple is now #[cfg(target_os="macos")]-only (no off-macOS stub); added verify_authenticode (#[cfg(windows)], requires absolute CT_SIGNTOOL_PATH, never bare PATH lookup) and verify_pre_promote dispatcher (3 cfg arms) as the new single production seam; updater::check.rs's one call site switched from verify::verify_staple to verify::verify_pre_promote (surgical 1-line change, else Windows arm would be dead code). Added 3 new fitness tests (wix no-ALLUSERS=1, uninstall task-name cross-check, verify_pre_promote dispatch). All owner-gated: real WiX/MSI build, signtool execution, EV cert, SmartScreen allow-list validation, Windows cargo test. macOS cargo test/fmt/clippy all green throughout (860 tests, 0 failures).
