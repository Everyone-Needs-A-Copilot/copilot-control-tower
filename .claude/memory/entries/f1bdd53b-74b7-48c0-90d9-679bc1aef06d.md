---
id: f1bdd53b-74b7-48c0-90d9-679bc1aef06d
type: decision
tags: []
created: 2026-07-08T02:06:20Z
updated: 2026-07-08T02:06:20Z
scope: project
---

M5/S2 deprovision DTO+render (task 45): parse-not-compute, zero wipe logic. model/deprovision.rs = wire+domain+parse boundary (parse_deprovision_body): missing required fields -> ParseError; schema_version out of range -> SchemaOutOfRange; unrecognized result string -> InvalidContent (never fabricates Wiped); secrets_touched STRUCTURALLY ABSENT -> MissingSecurityField (fails closed), but secrets_touched PRESENT-AND-NONZERO parses to Trusted carrying the real count (never collapsed to Unreadable, since that would hide the alarm) -- render layer sets secrets_alarm=true and leads the sentence with ALARM text. deprovision/render.rs: render_deprovision(raw bytes)->DeprovisionView; retained_dirty always rendered (even empty); removed_count rendered with NEUTRAL 'N item(s) removed' copy only (G-M5-4, never files/trees). deprovision/mod.rs: run_deprovision(org) spawns its OWN Command for 'deprovision <org> --json' (does NOT extend cli::spawn, which is scoped to doctor per its own module doc) -- follows the wizard::signin precedent exactly (same rationale documented there) rather than widening the shared cli spawn seam. No exit-code-specific contract for deprovision (unlike doctor's 0/1/2) so any nonzero exit/timeout/spawn failure collapses uniformly to IoError. No Tauri command wired (route-by-competence invariant #5 -- trigger/routing is S6, never Bob-facing). Fixtures: src-tauri/fixtures/deprovision/{corpus,invalid}/ + mock-cc extended with a deprovision verb (default fixture 'wiped-clean', reuses CT_FIXTURE/--fixture convention, always exits 0 except CT_FIXTURE=exit-2 since deprovision has no per-body exit code). FF-M5-2 fitness test (tests/fitness_m5_no_wipe_logic.rs) source-scans src/deprovision/ + model/deprovision.rs for remove_dir_all/remove_file/fs::remove/rmdir/fs::write/git clean|reset|rm|checkout -- asserts none present. types.ts mirror added (DeprovisionView/DeprovisionOutcome/DeprovisionUnreadableReason), deliberately no exported command constant yet.
