# ADVERSARIAL FIXTURE (S3, `.copilot/wp/5.md`): a `source.repo` URL with a
# credential embedded directly in it (`scheme://user:token@host/...`). This
# must NEVER reach a write — `guard::scan_for_secrets` refuses it before any
# byte touches disk (invariant #6). Structurally VALID per S1's validator (a
# non-empty `source.repo` string, same shape as `secret-looking-auth.yml`) —
# catching the CONTENT is S3's job, not S1's.
version: 1
layers:
  - id: personal-pablo
    role: personal
    product: claude
    rank: 10
    source:
      repo: https://x:ghp_1234567890ABCDEFabcdef1234567890AB@github.com/pablitoalejo/claude-copilot-private.git
    auth: ssh-personal
    activation: always
