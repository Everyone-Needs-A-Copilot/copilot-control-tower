{
  "fixture_version": "1",
  "scenario_id": "managed-silent-holding-profile-absent",
  "title": "Managed run, MDM profile not yet delivered -> settling-window retry, then honest holding",
  "path": "managed",
  "env": {
    "CT_MANAGED_OVERRIDE": "1",
    "CT_CLI_PATH": "src-tauri/fixtures/mock-cc",
    "CT_FIXTURE": "setup-needed-first-run"
  },
  "managed_profile_fixture": null,
  "managed_profile_state": "absent",
  "wizard_auth_scenario": null,
  "wizard_answers": null,
  "questions_expected": 0,
  "expected": {
    "wizard_terminal": "Holding(ItConfigIncomplete)",
    "doctor_status": "setup-needed",
    "doctor_fixture": "setup-needed-first-run",
    "reaches_done": false
  },
  "notes": "S4: is_managed() is true (forced-domain keys present) but the layer-manifest profile itself has not landed yet (MDM push lag). The state machine retries across a settling window (test harness can simulate by having the profile 'appear' on a later poll -- not modeled here, this fixture represents the still-absent snapshot) and, absent within that window, holds honestly rather than silently falling back to an unmanaged flow or fabricating Healthy. doctor_fixture points at the existing setup-needed-first-run.json corpus entry as the closest analogous lifecycle state (nothing materialized yet)."
}
