# Invalid: this layer has no `auth` field at all — one of the seven
# required fields (`id, role, rank, product, source, auth, activation`).
version: 1
layers:
  - id: personal-pablo
    role: personal
    product: claude
    rank: 10
    source:
      repo: git@github-personal:pablitoalejo/claude-copilot-private.git
    activation: always
