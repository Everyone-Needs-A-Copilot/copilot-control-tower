# ADVERSARIAL FIXTURE (S3, `.copilot/wp/5.md`): a long high-entropy hex blob
# in `auth` — shaped like raw key material or a secret hash, not a reference
# name. `guard::scan_for_secrets` refuses this before any byte touches disk
# (invariant #6). Structurally VALID per S1's validator (a non-empty `auth`
# string) — catching the CONTENT is S3's job, not S1's.
version: 1
layers:
  - id: personal-pablo
    role: personal
    product: claude
    rank: 10
    source:
      repo: git@github-personal:pablitoalejo/claude-copilot-private.git
    auth: 9f8a1c2d3e4b5a6f7089abcdef0123456789abcdef0123456789abcdef01234
    activation: always
